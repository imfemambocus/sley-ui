{
  "version": "0.11.0",
  "items": [
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "tokens",
      "type": "registry:style",
      "title": "Tokens",
      "dependencies": [],
      "registryDependencies": [],
      "files": [
        {
          "path": "styles/tokens.css",
          "target": "styles/tokens.css",
          "type": "registry:file",
          "content": "@custom-variant dense (&:where(:root[data-density='dense'] *));\n\n/* every name comes from the loom. the semantic colours are dyes. */\n@theme {\n  --font-ui: 'Archivo', ui-sans-serif, system-ui, sans-serif;\n  --font-data: 'IBM Plex Mono', ui-monospace, SFMono-Regular, monospace;\n\n  --color-ground: var(--p-ground);\n  --color-raised: var(--p-raised);\n  --color-sunken: var(--p-sunken);\n  --color-shed: var(--p-shed);\n  --color-reed: var(--p-reed);\n  --color-reed-lit: var(--p-reed-lit);\n\n  --color-weft: var(--p-weft);\n  --color-weft-dim: var(--p-weft-dim);\n  --color-weft-faint: var(--p-weft-faint);\n\n  --color-indigo: var(--p-indigo);\n  --color-indigo-wash: var(--p-indigo-wash);\n  --color-jade: var(--p-jade);\n  --color-madder: var(--p-madder);\n  --color-weld: var(--p-weld);\n  --color-logwood: var(--p-logwood);\n\n  /* one pick is one pass of the weft, and the motif carries five of them */\n  --color-pick-1: var(--p-indigo);\n  --color-pick-2: var(--p-jade);\n  --color-pick-3: var(--p-weld);\n  --color-pick-4: var(--p-madder);\n  --color-pick-5: var(--p-logwood);\n}\n\n/* the shed is the opening a loom makes and closes: the row under the pointer */\n:root {\n  --p-ground: #0b0d14;\n  --p-raised: #12151f;\n  --p-sunken: #070910;\n  --p-shed: #191d29;\n  --p-reed: #1c2030;\n  --p-reed-lit: #2b3145;\n\n  --p-weft: #e9e5db;\n  --p-weft-dim: #9b9789;\n  --p-weft-faint: #6a6759;\n\n  --p-indigo: #5f72ef;\n  --p-indigo-wash: #1a1f42;\n  --p-jade: #4fa88b;\n  --p-madder: #d2624f;\n  --p-weld: #c9a227;\n  --p-logwood: #9b6bd6;\n\n  --shadow-layer: 0 18px 44px -16px rgb(0 0 0 / 0.62);\n}\n\n:root[data-theme='light'] {\n  --p-ground: #f2f1ec;\n  --p-raised: #faf9f5;\n  --p-sunken: #e8e6df;\n  --p-shed: #f1efe8;\n  --p-reed: #dcd9d0;\n  --p-reed-lit: #c2beb2;\n\n  --p-weft: #14161d;\n  --p-weft-dim: #5c594f;\n  --p-weft-faint: #8a8779;\n\n  --p-indigo: #3a49c4;\n  --p-indigo-wash: #e4e5f7;\n  --p-jade: #2f7a61;\n  --p-madder: #b04430;\n  --p-weld: #8a6f12;\n  --p-logwood: #7a3f9e;\n\n  --shadow-layer: 0 18px 44px -16px rgb(26 24 20 / 0.24);\n}\n\n/*\n * a control reads these and defines none of its own, so one attribute on the root\n * retunes the whole interface.\n */\n:root,\n:root[data-density='compact'] {\n  --row-h: 32px;\n  --cell-x: 10px;\n  --ui-text: 13px;\n  --ctl-h: 28px;\n  --stack: 8px;\n  --reed-pitch: 5px;\n}\n\n:root[data-density='comfortable'] {\n  --row-h: 40px;\n  --cell-x: 14px;\n  --ui-text: 14px;\n  --ctl-h: 34px;\n  --stack: 10px;\n  --reed-pitch: 6px;\n}\n\n:root[data-density='dense'] {\n  --row-h: 25px;\n  --cell-x: 7px;\n  --ui-text: 12px;\n  --ctl-h: 23px;\n  --stack: 6px;\n  --reed-pitch: 4px;\n}\n\n/*\n * a duration is named for what it moves. every layer that leaves the flow declares\n * its rank here: a sticky header makes a stacking context and paints over any layer\n * with no rank.\n */\n:root {\n  --reed-tick: 1px;\n  --ctl-box: 13px;\n\n  /*\n   * vertical-align: middle centres on the x-height. every value here is digits and\n   * capitals, so the band runs baseline to cap height. this is half the difference,\n   * for the data face.\n   */\n  --ctl-lift: 0.091em;\n\n  /* the advance of the data face. a column width can then be a character count. */\n  --data-adv: 0.6em;\n\n  --z-grip: 1;\n  --z-pinned: 5;\n  --z-sticky: 10;\n  /* the panel keeps the page usable: a popover opened inside it paints above it */\n  --z-panel: 20;\n  --z-popover: 30;\n  --z-backdrop: 40;\n  --z-modal: 50;\n\n  --dur-instant: 90ms;\n  --dur-local: 160ms;\n  --dur-overlay: 240ms;\n\n  --ease-beat: cubic-bezier(0.2, 0.9, 0.24, 1);\n  --ease-glide: cubic-bezier(0.33, 0, 0.15, 1);\n  --ease-exit: cubic-bezier(0.5, 0, 0.9, 0.4);\n}\n\n@media (prefers-reduced-motion: reduce) {\n  :root {\n    --dur-instant: 0ms;\n    --dur-local: 0ms;\n    --dur-overlay: 0ms;\n  }\n}\n\n@layer base {\n  body {\n    background-color: var(--color-ground);\n    color: var(--color-weft);\n    font-family: var(--font-ui);\n    font-size: var(--ui-text);\n    -webkit-font-smoothing: antialiased;\n  }\n\n  /* the second selector: a control whose focus lands on a child, like a field with an icon */\n  :focus-visible,\n  .focus-ring:has(:focus-visible) {\n    outline: 2px solid var(--color-indigo);\n    outline-offset: 1px;\n  }\n\n  ::selection {\n    background-color: var(--color-indigo);\n    color: #fff;\n  }\n}\n\n@utility tnum {\n  font-variant-numeric: tabular-nums;\n}\n\n/* a utility, for a state variant to dye a part that ark controls */\n@utility selvedge-on {\n  --selvedge: var(--color-indigo);\n}\n\n/*\n * ark positions its floating parts with an inline `z-index: var(--z-index)` beside\n * an inline `--z-index: auto`. an inline custom property outranks a stylesheet, so\n * this has to be important to reach it. parts ark does not position take a class.\n */\n[data-part='positioner']:not([data-scope='dialog']) {\n  --z-index: var(--z-popover) !important;\n}\n\n@layer components {\n  /* the reed: the evenly spaced slots that hold the warp on a loom */\n  .reed-edge {\n    position: relative;\n  }\n\n  .reed-edge::after {\n    content: '';\n    position: absolute;\n    inset-inline: 0;\n    bottom: 0;\n    height: 2px;\n    background-image: repeating-linear-gradient(\n      90deg,\n      var(--color-reed-lit) 0 var(--reed-tick),\n      transparent var(--reed-tick) var(--reed-pitch)\n    );\n  }\n\n  /*\n   * the same pitch turned a quarter. a cell takes the leading line and a pinned cell\n   * takes the trailing one: the next cell's leading line travels away under it.\n   */\n  .warp-line,\n  .warp-line-end {\n    position: relative;\n  }\n\n  .warp-line::before,\n  .warp-line-end::before {\n    content: '';\n    position: absolute;\n    inset-block: 0;\n    width: 1px;\n    background-image: repeating-linear-gradient(\n      0deg,\n      var(--color-reed) 0 var(--reed-tick),\n      transparent var(--reed-tick) var(--reed-pitch)\n    );\n  }\n\n  /* under the digits, never on the divider: a divider cannot name which value is low */\n  .reed-under {\n    background-image: repeating-linear-gradient(\n      90deg,\n      var(--color-madder) 0 var(--reed-tick),\n      transparent var(--reed-tick) var(--reed-pitch)\n    );\n    background-position: bottom;\n    background-size: 100% 1px;\n    background-repeat: no-repeat;\n  }\n\n  /* the selvedge is the finished edge of a cloth. it marks a choice; the shed marks a position. */\n  .selvedge {\n    position: relative;\n    --selvedge: transparent;\n  }\n\n  .selvedge::after {\n    content: '';\n    position: absolute;\n    inset-block: 0;\n    inset-inline-start: 0;\n    width: 2px;\n    background-image: repeating-linear-gradient(\n      0deg,\n      var(--selvedge) 0 var(--reed-tick),\n      transparent var(--reed-tick) var(--reed-pitch)\n    );\n  }\n\n  /* the same edge turned a quarter, for a list of choices that runs across */\n  .selvedge-bottom::after {\n    inset-block: auto 0;\n    inset-inline: 0;\n    width: auto;\n    height: 2px;\n    background-image: repeating-linear-gradient(\n      90deg,\n      var(--selvedge) 0 var(--reed-tick),\n      transparent var(--reed-tick) var(--reed-pitch)\n    );\n  }\n\n  /*\n   * a row cannot wear the base outline. its own cells paint over it, and a pinned cell\n   * covers what is left, so all that survives is a fragment of the bottom edge. the ring\n   * is drawn inside the cells instead, which is the one place a pinned cell cannot hide.\n   */\n  .focus-row:focus-visible {\n    outline: none;\n  }\n\n  .focus-row:focus-visible > td {\n    box-shadow:\n      inset 0 2px 0 var(--color-indigo),\n      inset 0 -2px 0 var(--color-indigo);\n  }\n\n  /* zag places the mark and reads these two names for the slide from tab to tab */\n  .tab-mark {\n    --transition-duration: var(--dur-local);\n    --transition-timing-function: var(--ease-beat);\n  }\n\n  /* every surface that leaves the flow draws the same edge and the same shadow */\n  .layer {\n    border: 1px solid var(--color-reed-lit);\n    background-color: var(--color-raised);\n    box-shadow: var(--shadow-layer);\n  }\n\n  /*\n   * zag gives a toast its offset, its height and its opacity as custom properties,\n   * and the transition between two of its positions belongs to the stylesheet.\n   */\n  .toast {\n    translate: var(--x) var(--y);\n    height: var(--height, auto);\n    opacity: var(--opacity);\n    transition:\n      translate var(--dur-overlay) var(--ease-beat),\n      height var(--dur-overlay) var(--ease-beat),\n      opacity var(--dur-overlay) var(--ease-beat);\n    will-change: translate, opacity;\n  }\n\n  /*\n   * three ticks, and the order of the heights is the direction. tighter than the\n   * pitch, because a head reserves this width and a narrow column cannot pay a full one.\n   */\n  .reed-sort {\n    display: inline-block;\n    width: 7px;\n    height: 7px;\n    background-image:\n      linear-gradient(currentColor, currentColor), linear-gradient(currentColor, currentColor),\n      linear-gradient(currentColor, currentColor);\n    background-size: var(--reed-tick) 3px, var(--reed-tick) 5px, var(--reed-tick) 7px;\n    background-position: left bottom, center bottom, right bottom;\n    background-repeat: no-repeat;\n  }\n\n  .reed-sort-down {\n    background-size: var(--reed-tick) 7px, var(--reed-tick) 5px, var(--reed-tick) 3px;\n  }\n\n  /* a control beside a row of values aligns on the cap band */\n  .ctl-align {\n    vertical-align: middle;\n    translate: 0 calc(-1 * var(--ctl-lift));\n  }\n\n  /* the surface that every control shares */\n  .ctl {\n    display: inline-flex;\n    align-items: center;\n    gap: 0.375rem;\n    border: 1px solid var(--color-reed);\n    background-color: var(--color-ground);\n    padding-inline: var(--cell-x);\n    color: var(--color-weft);\n    transition: border-color var(--dur-instant) var(--ease-beat);\n  }\n\n  .ctl:hover {\n    border-color: var(--color-reed-lit);\n  }\n\n  /* an input or a textarea, which draws the control surface across a whole line */\n  .ctl.ctl-entry {\n    display: block;\n    width: 100%;\n  }\n\n  .ctl-entry[data-invalid] {\n    border-color: var(--color-madder);\n  }\n\n  .ctl-entry:disabled {\n    cursor: not-allowed;\n    opacity: 0.5;\n  }\n\n  /* one tick of the reed stands where a value is absent */\n  .reed-mark {\n    display: inline-block;\n    width: var(--reed-tick);\n    height: var(--reed-pitch);\n    background-color: var(--color-reed-lit);\n    vertical-align: middle;\n  }\n\n  .warp-line::before {\n    left: 0;\n  }\n\n  .warp-line-end::before {\n    right: 0;\n  }\n\n  /*\n   * the grip straddles the column boundary, and the reed inside it is the divider the\n   * head would draw anyway. currentColor, because a gradient cannot transition.\n   */\n  .reed-grip {\n    position: absolute;\n    inset-block: 0;\n    right: -4px;\n    z-index: var(--z-grip);\n    width: 8px;\n    color: var(--color-reed-lit);\n    cursor: col-resize;\n    touch-action: none;\n    transition: color var(--dur-instant) var(--ease-beat);\n  }\n\n  .reed-grip::before {\n    content: '';\n    position: absolute;\n    inset-block: 0;\n    left: 4px;\n    width: 1px;\n    background-image: repeating-linear-gradient(\n      0deg,\n      currentColor 0 var(--reed-tick),\n      transparent var(--reed-tick) var(--reed-pitch)\n    );\n  }\n\n  .reed-grip:hover,\n  .reed-grip:focus-visible,\n  .reed-grip[data-dragging] {\n    color: var(--color-indigo);\n  }\n\n  /* an unwoven warp: the threads sit in the reed and no weft has arrived */\n  .reed-warp {\n    background-image: repeating-linear-gradient(\n      90deg,\n      var(--color-reed) 0 var(--reed-tick),\n      transparent var(--reed-tick) var(--reed-pitch)\n    );\n  }\n\n  /* only the loading state moves */\n  .reed-warp-beat {\n    animation: beat 1.2s var(--ease-glide) infinite;\n  }\n\n  /*\n   * the cover layer is attached to the content, so it arrives at the edge and hides\n   * the reed at the end of the list. a short list needs no cover: the scrollable area\n   * is then the padding box and the two layers land together.\n   */\n  .reed-scroll {\n    background-image:\n      linear-gradient(var(--color-raised), var(--color-raised)),\n      repeating-linear-gradient(\n        90deg,\n        var(--color-reed-lit) 0 var(--reed-tick),\n        transparent var(--reed-tick) var(--reed-pitch)\n      );\n    background-position: bottom;\n    background-size: 100% 2px;\n    background-repeat: no-repeat;\n    background-attachment: local, scroll;\n    scrollbar-width: thin;\n    scrollbar-color: var(--color-reed-lit) transparent;\n  }\n\n  /* the standard properties above win where supported, so these reach safari only */\n  .reed-scroll::-webkit-scrollbar {\n    width: 8px;\n    height: 8px;\n  }\n\n  .reed-scroll::-webkit-scrollbar-track {\n    background-color: transparent;\n  }\n\n  .reed-scroll::-webkit-scrollbar-thumb {\n    background-color: var(--color-reed-lit);\n  }\n\n  .reed-scroll::-webkit-scrollbar-thumb:hover {\n    background-color: var(--color-indigo);\n  }\n\n  .beat {\n    animation: beat 1.6s var(--ease-glide) infinite;\n  }\n\n  /*\n   * plot writes font-family, font-size and fill on the svg as presentation\n   * attributes, and any stylesheet rule outranks those. it names every axis part\n   * with an aria-label, which is the only stable handle on the generated markup.\n   */\n  .sley-plot {\n    color: var(--color-weft-dim);\n    font-family: var(--font-data);\n    font-size: var(--ui-text);\n    /*\n     * the halo behind a crosshair readout and the fill of a tip. plot declares it\n     * white from a <style> inside the svg, and an unlayered rule outranks a layered\n     * one whatever the specificity says, so this one has to shout.\n     */\n    --plot-background: var(--color-raised) !important;\n  }\n\n  .sley-plot [aria-label$='grid'] {\n    color: var(--color-reed);\n    stroke-opacity: 1;\n  }\n\n  .sley-plot [aria-label$='axis tick'] {\n    color: var(--color-reed-lit);\n  }\n\n  .sley-plot [aria-label$='tick label'] {\n    color: var(--color-weft-dim);\n  }\n\n  /* an axis label is a human phrase, so it leaves the data face */\n  .sley-plot [aria-label$='axis label'] {\n    color: var(--color-weft-dim);\n    font-family: var(--font-ui);\n  }\n\n  .sley-plot [aria-label='crosshair rule'] {\n    color: var(--color-reed-lit);\n    stroke-opacity: 1;\n  }\n\n  .sley-plot [aria-label='crosshair text'] {\n    color: var(--color-weft);\n  }\n\n  /*\n   * a readout stands where a tick label already is, and the halo behind it outlines\n   * each glyph rather than covering a line, so the tick reads through the gaps. the\n   * readout carries the exact value, so the axis stands down while it is showing. plot\n   * renders the readout only while the pointer is in the plot, which is the condition.\n   */\n  .sley-plot:has([aria-label='crosshair text'] text) [aria-label$='tick label'] {\n    opacity: 0;\n    transition: opacity var(--dur-instant) var(--ease-beat);\n  }\n\n  /* one weft thread travelling across the cloth, and each series follows the one above it */\n  .sley-plot .reed-draw {\n    stroke-dasharray: var(--draw);\n    animation: draw var(--dur-overlay) var(--ease-beat) var(--draw-delay) both;\n  }\n\n  /* the cloth builds column by column, each bar standing up off the axis */\n  .sley-plot .reed-build {\n    transform-origin: 0 var(--build-base);\n    animation: build var(--dur-overlay) var(--ease-beat) var(--build-delay) both;\n  }\n\n  .reed-brush-field {\n    fill: transparent;\n    cursor: crosshair;\n  }\n\n  /*\n   * chrome rings a focusable svg element on a plain :focus, where it rings a button\n   * only on :focus-visible, so a pointer drag would leave its own outline behind.\n   */\n  .reed-brush-field:focus {\n    outline: none;\n  }\n\n  .reed-brush-field:focus-visible {\n    outline: 2px solid var(--color-indigo);\n    outline-offset: 1px;\n  }\n\n  /* a brush window is what the reader chose, so it takes the selvedge wash. it\n   * paints over the marks, so the fill carries an alpha rather than the opaque wash. */\n  .reed-brush {\n    fill: color-mix(in srgb, var(--color-indigo) 16%, transparent);\n    stroke: var(--color-indigo);\n    stroke-width: 1px;\n    pointer-events: none;\n  }\n}\n\n@keyframes beat {\n  0%,\n  100% {\n    opacity: 1;\n  }\n  50% {\n    opacity: 0.25;\n  }\n}\n\n@keyframes draw {\n  from {\n    stroke-dashoffset: var(--draw);\n  }\n}\n\n@keyframes build {\n  from {\n    transform: scaleY(0);\n  }\n}\n\n@keyframes fade {\n  from {\n    opacity: 0;\n  }\n}\n\n@keyframes fade-out {\n  to {\n    opacity: 0;\n  }\n}\n\n@keyframes rise {\n  from {\n    opacity: 0;\n    transform: translateY(-6px) scale(0.99);\n  }\n}\n\n@keyframes rise-out {\n  to {\n    opacity: 0;\n    transform: translateY(-6px) scale(0.99);\n  }\n}\n\n/* the panel arrives from the edge that holds it, which sets --slide-from */\n@keyframes slide {\n  from {\n    opacity: 0;\n    transform: translateX(var(--slide-from));\n  }\n}\n\n/* it leaves the way it came, and it leaves faster, on the exit curve */\n@keyframes slide-out {\n  to {\n    opacity: 0;\n    transform: translateX(var(--slide-from));\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .beat,\n  .reed-warp-beat {\n    animation: none;\n  }\n}\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/0.11.0/tokens.json",
        "files": [
          {
            "path": "styles/tokens.css",
            "hash": "sha256-mG/bjjzBVdc7yT2tkOGlPsyiQCUIjnavDcEFqlwpXSg=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "cx",
      "type": "registry:lib",
      "title": "Cx",
      "dependencies": [
        "clsx@^2.1.1",
        "tailwind-merge@^3.6.0"
      ],
      "registryDependencies": [],
      "files": [
        {
          "path": "lib/cx.ts",
          "target": "lib/cx.ts",
          "type": "registry:lib",
          "content": "import { clsx, type ClassValue } from 'clsx'\nimport { twMerge } from 'tailwind-merge'\n\n/*\n * the stylesheet order decides between two utilities for one property, so a plain\n * join lets an override fail with no message.\n */\nexport function cx(...classes: ClassValue[]) {\n  return twMerge(clsx(classes))\n}\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/0.11.0/cx.json",
        "files": [
          {
            "path": "lib/cx.ts",
            "hash": "sha256-nCgIrhJMREr99PRsI+he7LXaUaFBBdwZSWpHWqK/JBo=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "button",
      "type": "registry:ui",
      "title": "Button",
      "dependencies": [],
      "registryDependencies": [
        "cx",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/button/Button.tsx",
          "target": "components/ui/button/Button.tsx",
          "type": "registry:ui",
          "content": "import type { ComponentPropsWithoutRef } from 'react'\nimport { cx } from '@/lib/cx'\n\nexport type ButtonVariant = 'default' | 'primary' | 'danger' | 'quiet'\n\ninterface ButtonProps extends ComponentPropsWithoutRef<'button'> {\n  readonly variant?: ButtonVariant\n}\n\nconst VARIANT: Record<ButtonVariant, string> = {\n  default: 'ctl',\n  primary: 'border border-indigo bg-indigo text-ground hover:bg-indigo/90',\n  danger: 'border border-madder bg-madder text-ground hover:bg-madder/90',\n  quiet: 'text-weft-dim hover:text-weft',\n}\n\nexport const Button = ({ variant = 'default', type = 'button', className, ...props }: ButtonProps) => (\n  <button\n    type={type}\n    className={cx(\n      'inline-flex h-(--ctl-h) cursor-pointer items-center justify-center gap-1.5 px-(--cell-x) transition-colors duration-(--dur-instant) ease-(--ease-beat) disabled:cursor-not-allowed disabled:opacity-50',\n      VARIANT[variant],\n      className,\n    )}\n    {...props}\n  />\n)\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/0.11.0/button.json",
        "files": [
          {
            "path": "components/ui/button/Button.tsx",
            "hash": "sha256-U7D99UXvaDnReNcv5X6HIclmrSIDrhLErZn8DP2vxEc=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "chart",
      "type": "registry:ui",
      "title": "Chart",
      "dependencies": [
        "@observablehq/plot@^0.6.17"
      ],
      "registryDependencies": [
        "cx",
        "empty-state",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/chart/Chart.tsx",
          "target": "components/ui/chart/Chart.tsx",
          "type": "registry:ui",
          "content": "import * as Plot from '@observablehq/plot'\nimport { useEffect, useRef, useState, type ReactNode } from 'react'\nimport { attachBrush } from '@/components/ui/chart/brush'\nimport { drawBars, drawLines } from '@/components/ui/chart/motion'\nimport { EmptyState } from '@/components/ui/empty-state/EmptyState'\nimport { cx } from '@/lib/cx'\n\n/* the stylesheet reaches the generated svg through this one name */\nconst PLOT_CLASS = 'sley-plot'\n\n/*\n * plot takes a margin as a number, so it cannot read a density token. these are\n * sized for the widest tick label at the comfortable text size, which leaves a\n * denser mode a little slack and spares every chart a re-render on the knob.\n */\nconst MARGIN = { marginTop: 16, marginRight: 16, marginBottom: 40, marginLeft: 56 }\n\n/* plot points a quantitative axis label with an arrow glyph, and no interface string here carries one */\nconst AXIS = { labelArrow: 'none' } as const\n\nexport type ChartOptions = Omit<Plot.PlotOptions, 'width' | 'height' | 'className'>\n\nexport interface ChartProps<X> {\n  readonly title: string\n  /* the unit belongs in the header, the way a column head carries it and a cell never does */\n  readonly unit?: string\n  /* hold this stable, or every parent render tears the plot down and builds it again */\n  readonly options: ChartOptions\n  readonly height?: number\n  readonly loading?: boolean\n  /* a chart cannot read the caller's marks, so an empty result is stated and never derived */\n  readonly empty?: boolean\n  readonly emptyMessage?: string\n  readonly actions?: ReactNode\n  /*\n   * a drag reports a range of the x scale, and an arrow key on the plot moves one edge\n   * of one by a tick of that axis. a single click reports null, and so does escape.\n   */\n  readonly onBrush?: (range: readonly [X, X] | null) => void\n  /* the window the chart paints. hold it in your own state, the way a table's selection works. */\n  readonly brush?: readonly [X, X] | null\n  readonly className?: string\n}\n\nexport function Chart<X = Date>({\n  title,\n  unit,\n  options,\n  height = 260,\n  loading = false,\n  empty = false,\n  emptyMessage = 'No data in this range.',\n  actions,\n  onBrush,\n  brush = null,\n  className,\n}: ChartProps<X>) {\n  /* the frame is measured rather than the host, because the host leaves while a state shows */\n  const frame = useRef<HTMLDivElement>(null)\n  const host = useRef<HTMLDivElement>(null)\n  const [width, setWidth] = useState(0)\n\n  /* the plot outlives a change of handler, so the newest one is read at report time */\n  const report = useRef(onBrush)\n  report.current = onBrush\n\n  const window = useRef<{ show: (range: readonly [X, X] | null) => void } | null>(null)\n\n  /* the plot is built in an effect that does not watch the window, so the first paint reads it here */\n  const brushed = useRef(brush)\n  brushed.current = brush\n\n  /* the lines draw in once. a resize rebuilds the plot, and replaying it there reads as a glitch. */\n  const drawn = useRef(false)\n\n  /* a rebuild takes the focused field down with it, so a resize would strand the keyboard */\n  const held = useRef(false)\n\n  /* loading wins: a chart still waiting on a fetch has nothing to call empty yet */\n  const showEmpty = empty && !loading\n  const plotting = !loading && !showEmpty\n\n  useEffect(() => {\n    window.current?.show(brush)\n  }, [brush])\n\n  useEffect(() => {\n    const node = frame.current\n    if (!node) return undefined\n\n    const observer = new ResizeObserver(([entry]) => setWidth(Math.floor(entry.contentRect.width)))\n    observer.observe(node)\n    return () => observer.disconnect()\n  }, [])\n\n  useEffect(() => {\n    const node = host.current\n    if (!node || width === 0) return undefined\n\n    const margins = { ...MARGIN, ...options }\n    const plot = Plot.plot({\n      ...margins,\n      ariaLabel: title,\n      x: { ...AXIS, ...options.x },\n      y: { ...AXIS, ...options.y },\n      width,\n      height,\n      className: PLOT_CLASS,\n    })\n    node.append(plot)\n\n    const svg = plot instanceof SVGSVGElement ? plot : plot.querySelector('svg')\n    if (svg && !drawn.current) {\n      drawn.current = true\n      drawLines(svg)\n      drawBars(svg)\n    }\n\n    const scale = plot.scale('x')\n    if (!svg || !scale?.invert) return () => plot.remove()\n\n    /* plot types every scale value as any, so both readings are named here once */\n    const toValue: (px: number) => X = scale.invert.bind(scale)\n    const toPixel: (value: X) => number = scale.apply.bind(scale)\n\n    const handle = attachBrush(\n      svg,\n      {\n        left: margins.marginLeft,\n        right: width - margins.marginRight,\n        top: margins.marginTop,\n        bottom: height - margins.marginBottom,\n      },\n      { toValue, toPixel },\n      (range) => report.current?.(range),\n    )\n    handle.show(brushed.current)\n    if (held.current) handle.restore()\n    window.current = handle\n\n    return () => {\n      held.current = handle.focused()\n      window.current = null\n      handle.destroy()\n      plot.remove()\n    }\n  }, [title, options, width, height, plotting])\n\n  return (\n    <section className={cx('@container border border-reed bg-raised', className)}>\n      <header className=\"flex items-center justify-between gap-(--stack) border-b border-reed px-(--cell-x) py-(--stack)\">\n        <h2 className=\"inline-flex items-baseline gap-1.5 font-medium\">\n          <span>{title}</span>\n          {unit && <span className=\"font-data font-normal text-weft-faint\">{unit}</span>}\n        </h2>\n        {actions && <div className=\"flex items-center gap-(--stack)\">{actions}</div>}\n      </header>\n\n      <div ref={frame} aria-busy={loading} className=\"px-(--cell-x) py-(--stack)\">\n        {plotting && <div ref={host} />}\n        {loading && <div className=\"reed-warp reed-warp-beat\" style={{ height }} />}\n        {showEmpty && (\n          <div style={{ height }}>\n            <EmptyState title={emptyMessage} className=\"h-full\" />\n          </div>\n        )}\n      </div>\n    </section>\n  )\n}\n"
        },
        {
          "path": "components/ui/chart/brush.ts",
          "target": "components/ui/chart/brush.ts",
          "type": "registry:ui",
          "content": "const SVG_NS = 'http://www.w3.org/2000/svg'\n\n/* a click that travels less than this is a click, and a click clears the window */\nconst MIN_DRAG = 3\n\n/* the resize grip moves 8px a press, and a step under that reads as nothing happening */\nconst MIN_STEP = 8\n\nconst KEYS = new Set(['ArrowLeft', 'ArrowRight', 'Home', 'End', 'Escape'])\n\n/* the field is the only surface that can announce the keys, so its name carries them */\nconst HOW = 'Move an edge with the arrow keys. Escape clears.'\n\nexport interface BrushFrame {\n  readonly left: number\n  readonly right: number\n  readonly top: number\n  readonly bottom: number\n}\n\n/*\n * plot re-renders a whole svg on every change, so the brush layer is appended to the\n * node it belongs to and dies with it. the caller owns no listener and no cleanup\n * beyond the teardown returned here.\n */\nexport interface BrushScale<T> {\n  readonly toValue: (px: number) => T\n  readonly toPixel: (value: T) => number\n}\n\n/* one press is one tick of the x axis: a distance the scale already draws for the reader */\nfunction stepOf(svg: SVGSVGElement, frame: BrushFrame) {\n  const ticks = svg.querySelectorAll<SVGGraphicsElement>('[aria-label=\"x-axis tick\"] path')\n  const xs = Array.from(ticks, (tick) => tick.transform.baseVal.consolidate()?.matrix.e ?? 0).sort(\n    (a, b) => a - b,\n  )\n  const gaps = xs.slice(1).map((x, index) => x - xs[index])\n  if (gaps.length === 0) return (frame.right - frame.left) / 20\n\n  gaps.sort((a, b) => a - b)\n  return Math.max(MIN_STEP, gaps[Math.floor(gaps.length / 2)])\n}\n\nexport function attachBrush<T>(\n  svg: SVGSVGElement,\n  frame: BrushFrame,\n  scale: BrushScale<T>,\n  report: (range: readonly [T, T] | null) => void,\n) {\n  const height = frame.bottom - frame.top\n  const step = stepOf(svg, frame)\n  const named = svg.getAttribute('aria-label')\n\n  const field = document.createElementNS(SVG_NS, 'rect')\n  field.setAttribute('class', 'reed-brush-field')\n  field.setAttribute('x', String(frame.left))\n  field.setAttribute('y', String(frame.top))\n  field.setAttribute('width', String(frame.right - frame.left))\n  field.setAttribute('height', String(height))\n  field.setAttribute('tabindex', '0')\n  field.setAttribute('aria-label', named ? `Select a range on ${named}. ${HOW}` : `Select a range. ${HOW}`)\n\n  const selection = document.createElementNS(SVG_NS, 'rect')\n  selection.setAttribute('class', 'reed-brush')\n  selection.setAttribute('aria-hidden', 'true')\n  selection.setAttribute('y', String(frame.top))\n  selection.setAttribute('height', String(height))\n  selection.setAttribute('width', '0')\n\n  /*\n   * the window paints over the marks and tints them through its own alpha. under\n   * them it is invisible against a filled mark, which a bar chart is made of.\n   */\n  svg.append(selection)\n  svg.append(field)\n\n  let anchor = 0\n  let edge = 0\n  let dragging = false\n  let live = false\n\n  /* raised for the one paint that answers a report, and read by show below */\n  let reported = false\n\n  const xOf = (event: PointerEvent) => clampX(event.clientX - svg.getBoundingClientRect().left)\n\n  const clampX = (value: number) => Math.max(frame.left, Math.min(frame.right, value))\n\n  const paint = (from: number, to: number) => {\n    selection.setAttribute('x', String(Math.min(from, to)))\n    selection.setAttribute('width', String(Math.abs(to - from)))\n  }\n\n  const clear = () => {\n    live = false\n    reported = false\n    paint(0, 0)\n  }\n\n  const onDown = (event: PointerEvent) => {\n    dragging = true\n    anchor = xOf(event)\n    edge = anchor\n    paint(anchor, anchor)\n    field.setPointerCapture(event.pointerId)\n  }\n\n  const onMove = (event: PointerEvent) => {\n    if (!dragging) return\n    edge = xOf(event)\n    paint(anchor, edge)\n  }\n\n  const onRelease = () => {\n    if (!dragging) return\n    dragging = false\n\n    if (Math.abs(edge - anchor) < MIN_DRAG) {\n      clear()\n      report(null)\n      return\n    }\n    live = true\n    reported = true\n    const from = Math.min(anchor, edge)\n    const to = Math.max(anchor, edge)\n    report([scale.toValue(from), scale.toValue(to)])\n  }\n\n  /*\n   * a drag anchors one edge and carries the other, and the keys do the same thing one\n   * step at a time. the first press has nothing to anchor, so it takes the edge of the\n   * frame it moves away from.\n   */\n  const moveEdge = (to: number) => {\n    edge = clampX(to)\n    live = true\n    reported = true\n    paint(anchor, edge)\n    report([scale.toValue(Math.min(anchor, edge)), scale.toValue(Math.max(anchor, edge))])\n  }\n\n  const onKey = (event: KeyboardEvent) => {\n    if (!KEYS.has(event.key)) return\n    event.preventDefault()\n\n    if (event.key === 'Escape') {\n      if (!live) return\n      clear()\n      report(null)\n      return\n    }\n\n    const back = event.key === 'ArrowLeft' || event.key === 'Home'\n    if (!live) {\n      anchor = back ? frame.right : frame.left\n      edge = anchor\n    }\n\n    if (event.key === 'Home') moveEdge(frame.left)\n    else if (event.key === 'End') moveEdge(frame.right)\n    else moveEdge(edge + (back ? -step : step))\n  }\n\n  field.addEventListener('pointerdown', onDown)\n  field.addEventListener('pointermove', onMove)\n  field.addEventListener('lostpointercapture', onRelease)\n  field.addEventListener('keydown', onKey)\n\n  return {\n    /*\n     * the caller owns the range, so a re-render paints the window it still holds\n     * rather than dropping it and leaving the two out of step.\n     */\n    show(range: readonly [T, T] | null) {\n      if (!range) {\n        clear()\n        return\n      }\n      const from = clampX(scale.toPixel(range[0]))\n      const to = clampX(scale.toPixel(range[1]))\n      live = true\n      paint(from, to)\n\n      /*\n       * a caller may widen what it was given, and the demo here rounds a range out to whole\n       * days. taking the moving edge from that value would add the widening to the next key\n       * press on top of the step, so the paint that answers a report moves no edge.\n       */\n      if (reported) {\n        reported = false\n        return\n      }\n\n      /* the moving edge stays the one the reader is moving, or the next key press reverses */\n      const forward = edge >= anchor\n      anchor = forward ? from : to\n      edge = forward ? to : from\n    },\n    focused() {\n      return document.activeElement === field\n    },\n    /* a rebuild draws a new field, and the reader is still on the old one */\n    restore() {\n      field.focus({ preventScroll: true })\n    },\n    destroy() {\n      field.removeEventListener('pointerdown', onDown)\n      field.removeEventListener('pointermove', onMove)\n      field.removeEventListener('lostpointercapture', onRelease)\n      field.removeEventListener('keydown', onKey)\n      selection.remove()\n      field.remove()\n    },\n  }\n}\n"
        },
        {
          "path": "components/ui/chart/crosshair.ts",
          "target": "components/ui/chart/crosshair.ts",
          "type": "registry:ui",
          "content": "import * as Plot from '@observablehq/plot'\n\nexport interface CrosshairXOptions<T> {\n  readonly x: Plot.ChannelValue\n  readonly y?: Plot.ChannelValue\n  readonly formatX?: (row: T) => string\n  readonly formatY?: (row: T) => string\n  readonly maxRadius?: number\n}\n\nconst RULE = { inset: -6, stroke: 'currentColor', strokeOpacity: 0.2 } as const\n\n/* the halo outlines each glyph against the surface the chart is drawn on */\nconst READOUT = { fill: 'currentColor', stroke: 'var(--plot-background)', strokeWidth: 5 } as const\n\n/* plot names the group of rendered elements from a field the class type does not declare */\nconst named = <M extends object>(mark: M, ariaLabel: string) => Object.assign(mark, { ariaLabel })\n\n/*\n * plot's own crosshair derives each readout from its source channel through an\n * initializer, and an initializer's channels are merged over the declared ones, so a\n * text the caller passes is discarded and a date reads as a full iso stamp. the pairs\n * are composed here from the same public pointer transform instead.\n */\nexport function crosshairX<T>(data: readonly T[], options: CrosshairXOptions<T>) {\n  const { x, y, formatX, formatY, maxRadius } = options\n  const point = { px: x, py: y, maxRadius }\n\n  const acrossRule = named(Plot.ruleX(data, Plot.pointerX({ ...point, x, ...RULE })), 'crosshair rule')\n\n  const acrossText = named(\n    Plot.text(\n      data,\n      Plot.pointerX({ ...point, x, text: formatX ?? x, dy: 9, frameAnchor: 'bottom', lineAnchor: 'top', ...READOUT }),\n    ),\n    'crosshair text',\n  )\n\n  if (y === undefined) return Plot.marks(acrossRule, acrossText)\n\n  const downRule = named(Plot.ruleY(data, Plot.pointerX({ ...point, y, ...RULE })), 'crosshair rule')\n\n  const downText = named(\n    Plot.text(\n      data,\n      Plot.pointerX({ ...point, y, text: formatY ?? y, dx: -9, frameAnchor: 'left', textAnchor: 'end', ...READOUT }),\n    ),\n    'crosshair text',\n  )\n\n  /* both rules go under both readouts, or the horizontal rule crosses the label at the foot */\n  return Plot.marks(acrossRule, downRule, acrossText, downText)\n}\n"
        },
        {
          "path": "components/ui/chart/downsample.ts",
          "target": "components/ui/chart/downsample.ts",
          "type": "registry:ui",
          "content": "/*\n * largest triangle three buckets. a line with more points than the frame has pixels\n * spends layout time on detail nobody can resolve, and taking every nth point flattens\n * a spike that falls in the gap. this keeps whichever point in a bucket makes the widest\n * triangle with the point already kept and the bucket ahead, which follows the shape of the\n * line. the extremes are not a guarantee: where a bucket boundary splits a peak, the peak\n * can lose to its own shoulder.\n */\nexport function downsample<T>(\n  rows: readonly T[],\n  x: (row: T) => number,\n  y: (row: T) => number,\n  target: number,\n): readonly T[] {\n  if (rows.length <= target || target < 3) return rows\n\n  const last = rows.length - 1\n  const every = (rows.length - 2) / (target - 2)\n  const kept: T[] = [rows[0]]\n  let anchor = 0\n\n  for (let bucket = 0; bucket < target - 2; bucket++) {\n    const start = Math.floor(bucket * every) + 1\n    const end = Math.floor((bucket + 1) * every) + 1\n\n    /* the bucket ahead stands in for its own points as one centre of mass */\n    const aheadEnd = Math.min(Math.floor((bucket + 2) * every) + 1, rows.length)\n    let aheadX = 0\n    let aheadY = 0\n    for (let i = end; i < aheadEnd; i++) {\n      aheadX += x(rows[i])\n      aheadY += y(rows[i])\n    }\n    aheadX /= aheadEnd - end\n    aheadY /= aheadEnd - end\n\n    const anchorX = x(rows[anchor])\n    const anchorY = y(rows[anchor])\n    let widest = -1\n    let best = start\n\n    for (let i = start; i < end; i++) {\n      const area = Math.abs(\n        (anchorX - aheadX) * (y(rows[i]) - anchorY) - (anchorX - x(rows[i])) * (aheadY - anchorY),\n      )\n      if (area > widest) {\n        widest = area\n        best = i\n      }\n    }\n    kept.push(rows[best])\n    anchor = best\n  }\n\n  kept.push(rows[last])\n  return kept\n}\n"
        },
        {
          "path": "components/ui/chart/motion.ts",
          "target": "components/ui/chart/motion.ts",
          "type": "registry:ui",
          "content": "/*\n * a dash cannot be given the length of its own path from a stylesheet, so the length\n * is measured here and handed to the animation as a custom property.\n */\nexport function drawLines(svg: SVGSVGElement) {\n  const paths = svg.querySelectorAll<SVGPathElement>('g[aria-label=\"line\"] path')\n\n  paths.forEach((path, index) => {\n    path.style.setProperty('--draw', `${path.getTotalLength()}px`)\n    path.style.setProperty('--draw-delay', `calc(var(--dur-instant) * ${index})`)\n    path.classList.add('reed-draw')\n  })\n\n  return paths.length\n}\n\n/*\n * a bar grows out of the axis rather than out of its own box, so a stacked column stays\n * whole on the way up. every rect scales about the same baseline, which is the lowest\n * edge any of them reaches.\n */\nexport function drawBars(svg: SVGSVGElement) {\n  const rects = [...svg.querySelectorAll<SVGRectElement>('g[aria-label=\"bar\"] rect')]\n  if (rects.length === 0) return 0\n\n  const base = Math.max(...rects.map((rect) => rect.y.baseVal.value + rect.height.baseVal.value))\n  const columns = [...new Set(rects.map((rect) => rect.x.baseVal.value))].sort((left, right) => left - right)\n  const order = new Map(columns.map((x, index) => [x, index]))\n\n  for (const rect of rects) {\n    const index = order.get(rect.x.baseVal.value) ?? 0\n    rect.style.setProperty('--build-base', `${base}px`)\n    /* the sweep across the columns fits inside one --dur-instant, whatever the count */\n    rect.style.setProperty('--build-delay', `calc(var(--dur-instant) * ${index / columns.length})`)\n    rect.classList.add('reed-build')\n  }\n\n  return rects.length\n}\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/0.11.0/chart.json",
        "files": [
          {
            "path": "components/ui/chart/Chart.tsx",
            "hash": "sha256-DyHTqRsOFyPsguU9PWRy4tyVakwrKZeLWUw3q2V/MTQ=",
            "client": true
          },
          {
            "path": "components/ui/chart/brush.ts",
            "hash": "sha256-CMIpDxzji8iWtJbagr2r5GmQ5zEno6le+jpCpvMynkg=",
            "client": false
          },
          {
            "path": "components/ui/chart/crosshair.ts",
            "hash": "sha256-zLJAHBmcbG6wi3RoMhwiNuPjk8Sh+ui7W5Hfi3/Fkw0=",
            "client": false
          },
          {
            "path": "components/ui/chart/downsample.ts",
            "hash": "sha256-zfYABCIrkZGVxTiQC4wJhE1pUu/bOfZvx/GdCEA+1Gc=",
            "client": false
          },
          {
            "path": "components/ui/chart/motion.ts",
            "hash": "sha256-d+T9WcxUcAMpmLN2FKT2OA+NLD8XFPBKsyxHUQFpMRA=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "checkbox",
      "type": "registry:ui",
      "title": "Checkbox",
      "dependencies": [
        "@ark-ui/react@^5.38.1"
      ],
      "registryDependencies": [
        "cx",
        "icons",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/checkbox/Checkbox.tsx",
          "target": "components/ui/checkbox/Checkbox.tsx",
          "type": "registry:ui",
          "content": "import { Checkbox as ArkCheckbox } from '@ark-ui/react/checkbox'\nimport type { ReactNode } from 'react'\nimport { CheckIcon } from '@/components/ui/icons/Icons'\nimport { cx } from '@/lib/cx'\n\nexport type CheckedState = boolean | 'indeterminate'\n\ninterface CheckboxProps {\n  readonly checked: CheckedState\n  readonly onCheckedChange: (checked: CheckedState) => void\n  readonly children?: ReactNode\n  /* names a box that shows no text */\n  readonly label?: string\n  readonly className?: string\n}\n\nconst CONTROL =\n  'grid size-(--ctl-box) shrink-0 place-items-center border border-reed-lit text-ground transition-colors duration-(--dur-instant) ease-(--ease-beat) data-[state=checked]:border-indigo data-[state=checked]:bg-indigo data-[state=indeterminate]:border-indigo data-[state=indeterminate]:bg-indigo'\n\n/* the ark root is a label element, so the text inside it toggles the box */\nexport const Checkbox = ({ checked, onCheckedChange, children, label, className }: CheckboxProps) => (\n  <ArkCheckbox.Root\n    checked={checked}\n    onCheckedChange={(details) => onCheckedChange(details.checked)}\n    className={cx('ctl-align inline-flex cursor-pointer items-center gap-2', className)}\n  >\n    <ArkCheckbox.Control className={CONTROL}>\n      <ArkCheckbox.Indicator>\n        <CheckIcon className=\"size-2.75\" />\n      </ArkCheckbox.Indicator>\n      <ArkCheckbox.Indicator indeterminate>\n        <span className=\"block h-px w-1.75 bg-current\" />\n      </ArkCheckbox.Indicator>\n    </ArkCheckbox.Control>\n    {children !== undefined && <ArkCheckbox.Label>{children}</ArkCheckbox.Label>}\n    <ArkCheckbox.HiddenInput aria-label={children === undefined ? label : undefined} />\n  </ArkCheckbox.Root>\n)\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/0.11.0/checkbox.json",
        "files": [
          {
            "path": "components/ui/checkbox/Checkbox.tsx",
            "hash": "sha256-KUJ+kUgBjws/gJiEecaRJXavYOLRUnXP5jGYTAUaf1o=",
            "client": true
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "command-palette",
      "type": "registry:ui",
      "title": "Command Palette",
      "dependencies": [
        "@ark-ui/react@^5.38.1"
      ],
      "registryDependencies": [
        "icons",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/command-palette/CommandPalette.tsx",
          "target": "components/ui/command-palette/CommandPalette.tsx",
          "type": "registry:ui",
          "content": "import { Dialog } from '@ark-ui/react/dialog'\nimport { Portal } from '@ark-ui/react/portal'\nimport { useEffect, useMemo, useRef, useState, type KeyboardEvent } from 'react'\nimport { SearchIcon } from '@/components/ui/icons/Icons'\n\nexport interface Command {\n  readonly id: string\n  readonly label: string\n  readonly group: string\n  readonly hint?: string\n  readonly run: () => void\n}\n\ninterface CommandPaletteProps {\n  readonly open: boolean\n  readonly onOpenChange: (open: boolean) => void\n  readonly commands: readonly Command[]\n  readonly placeholder?: string\n}\n\ninterface Entry {\n  readonly command: Command\n  readonly index: number\n}\n\nfunction groupCommands(commands: readonly Command[]) {\n  const groups = new Map<string, Entry[]>()\n  commands.forEach((command, index) => {\n    const bucket = groups.get(command.group)\n    if (bucket) {\n      bucket.push({ command, index })\n    } else {\n      groups.set(command.group, [{ command, index }])\n    }\n  })\n  return [...groups]\n}\n\nexport const CommandPalette = ({\n  open,\n  onOpenChange,\n  commands,\n  placeholder = 'Run a command',\n}: CommandPaletteProps) => {\n  const [query, setQuery] = useState('')\n  const [active, setActive] = useState(0)\n  const listRef = useRef<HTMLDivElement>(null)\n  const inputRef = useRef<HTMLInputElement>(null)\n\n  const matches = useMemo(() => {\n    const needle = query.trim().toLowerCase()\n    if (needle === '') return commands\n    return commands.filter((command) => `${command.group} ${command.label}`.toLowerCase().includes(needle))\n  }, [commands, query])\n\n  useEffect(() => {\n    setActive(0)\n  }, [query])\n\n  /* clears on the way in: the list must keep its matches while the dialog leaves */\n  useEffect(() => {\n    if (open) setQuery('')\n  }, [open])\n\n  // keyboard navigation moves no pointer, so nothing else scrolls the row into view\n  useEffect(() => {\n    listRef.current?.querySelector('[data-active]')?.scrollIntoView({ block: 'nearest' })\n  }, [active])\n\n  const choose = (command: Command) => {\n    command.run()\n    onOpenChange(false)\n  }\n\n  const onKeyDown = (event: KeyboardEvent<HTMLInputElement>) => {\n    if (matches.length === 0) return\n\n    if (event.key === 'ArrowDown') {\n      event.preventDefault()\n      setActive((current) => (current + 1) % matches.length)\n      return\n    }\n    if (event.key === 'ArrowUp') {\n      event.preventDefault()\n      setActive((current) => (current - 1 + matches.length) % matches.length)\n      return\n    }\n    if (event.key === 'Enter') {\n      event.preventDefault()\n      choose(matches[active])\n    }\n  }\n\n  return (\n    <Dialog.Root\n      open={open}\n      onOpenChange={(details) => onOpenChange(details.open)}\n      initialFocusEl={() => inputRef.current}\n      unmountOnExit\n      lazyMount\n    >\n      <Portal>\n        <Dialog.Backdrop className=\"fixed inset-0 z-(--z-backdrop) bg-sunken/70 backdrop-blur-[2px] data-[state=open]:animate-[fade_var(--dur-overlay)_var(--ease-beat)] data-[state=closed]:animate-[fade-out_var(--dur-local)_var(--ease-exit)]\" />\n        <Dialog.Positioner className=\"fixed inset-0 z-(--z-modal) grid place-items-start justify-items-center pt-[12vh]\">\n          <Dialog.Content className=\"layer w-[min(560px,92vw)] data-[state=open]:animate-[rise_var(--dur-overlay)_var(--ease-beat)] data-[state=closed]:animate-[rise-out_var(--dur-local)_var(--ease-exit)]\">\n            <Dialog.Title className=\"sr-only\">Command palette</Dialog.Title>\n\n            <div className=\"reed-edge flex items-center gap-2 px-3\" style={{ height: 'calc(var(--ctl-h) + 12px)' }}>\n              <SearchIcon className=\"size-4 text-weft-faint\" />\n              <input\n                ref={inputRef}\n                name=\"command\"\n                value={query}\n                onChange={(event) => setQuery(event.target.value)}\n                onKeyDown={onKeyDown}\n                placeholder={placeholder}\n                aria-label={placeholder}\n                className=\"w-full bg-transparent text-weft placeholder:text-weft-faint focus:outline-none\"\n              />\n              <kbd className=\"font-data text-[11px] text-weft-faint\">esc</kbd>\n            </div>\n\n            <div ref={listRef} className=\"max-h-[46vh] overflow-auto py-1\">\n              {matches.length === 0 && (\n                <p className=\"px-3 py-6 text-center text-weft-dim\">\n                  No command matches {`\"${query}\"`}. Try a shorter word.\n                </p>\n              )}\n\n              {groupCommands(matches).map(([group, items]) => (\n                <div key={group}>\n                  <p className=\"px-3 pt-2 pb-1 font-data text-[11px] tracking-wide text-weft-faint uppercase dense:sr-only\">\n                    {group}\n                  </p>\n                  {items.map(({ command, index }) => {\n                    const isActive = index === active\n                    return (\n                      <button\n                        key={command.id}\n                        type=\"button\"\n                        data-active={isActive ? '' : undefined}\n                        onPointerMove={() => setActive(index)}\n                        onClick={() => choose(command)}\n                        className=\"flex w-full cursor-pointer items-center justify-between gap-3 px-3 text-left text-weft-dim transition-colors duration-(--dur-instant) ease-(--ease-beat) data-active:bg-shed data-active:text-weft\"\n                        style={{ height: 'var(--row-h)' }}\n                      >\n                        <span className=\"truncate\">{command.label}</span>\n                        {command.hint && <kbd className=\"font-data text-[11px] text-weft-faint\">{command.hint}</kbd>}\n                      </button>\n                    )\n                  })}\n                </div>\n              ))}\n            </div>\n          </Dialog.Content>\n        </Dialog.Positioner>\n      </Portal>\n    </Dialog.Root>\n  )\n}\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/0.11.0/command-palette.json",
        "files": [
          {
            "path": "components/ui/command-palette/CommandPalette.tsx",
            "hash": "sha256-fdQd7Fg5S9ZV5cYVKmpCxcoPYD/vitLvMwwpFFNLrbo=",
            "client": true
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "dialog",
      "type": "registry:ui",
      "title": "Dialog",
      "dependencies": [
        "@ark-ui/react@^5.38.1"
      ],
      "registryDependencies": [
        "cx",
        "icons",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/dialog/Dialog.tsx",
          "target": "components/ui/dialog/Dialog.tsx",
          "type": "registry:ui",
          "content": "import { Dialog as ArkDialog, type DialogRootProps } from '@ark-ui/react/dialog'\nimport { Portal } from '@ark-ui/react/portal'\nimport type { ReactNode } from 'react'\nimport { CloseIcon } from '@/components/ui/icons/Icons'\nimport { cx } from '@/lib/cx'\n\n/* ark removes the node when the exit animation ends. both mount options default on. */\nexport const Dialog = ({ unmountOnExit = true, lazyMount = true, ...props }: DialogRootProps) => (\n  <ArkDialog.Root unmountOnExit={unmountOnExit} lazyMount={lazyMount} {...props} />\n)\n\nexport const DialogTrigger = ArkDialog.Trigger\n\ninterface PartProps {\n  readonly children: ReactNode\n  readonly className?: string\n}\n\n/* ark names the dialog from its title, so every dialog carries one */\nexport const DialogContent = ({ children, className }: PartProps) => (\n  <Portal>\n    <ArkDialog.Backdrop className=\"fixed inset-0 z-(--z-backdrop) bg-sunken/70 backdrop-blur-[2px] data-[state=open]:animate-[fade_var(--dur-overlay)_var(--ease-beat)] data-[state=closed]:animate-[fade-out_var(--dur-local)_var(--ease-exit)]\" />\n    <ArkDialog.Positioner className=\"fixed inset-0 z-(--z-modal) grid place-items-center p-6\">\n      <ArkDialog.Content\n        className={cx(\n          'layer flex w-[min(480px,92vw)] flex-col data-[state=open]:animate-[rise_var(--dur-overlay)_var(--ease-beat)] data-[state=closed]:animate-[rise-out_var(--dur-local)_var(--ease-exit)]',\n          className,\n        )}\n      >\n        {children}\n      </ArkDialog.Content>\n    </ArkDialog.Positioner>\n  </Portal>\n)\n\nexport const DialogHeader = ({ children, className }: PartProps) => (\n  <header\n    className={cx('reed-edge flex items-start justify-between gap-4 px-(--cell-x) py-(--stack)', className)}\n  >\n    <div className=\"flex flex-col gap-1\">{children}</div>\n    <ArkDialog.CloseTrigger\n      aria-label=\"Close\"\n      className=\"-mr-1 cursor-pointer p-1 text-weft-faint transition-colors duration-(--dur-instant) ease-(--ease-beat) hover:text-weft\"\n    >\n      <CloseIcon className=\"size-3.5\" />\n    </ArkDialog.CloseTrigger>\n  </header>\n)\n\nexport const DialogTitle = ({ children, className }: PartProps) => (\n  <ArkDialog.Title className={cx('font-medium', className)}>{children}</ArkDialog.Title>\n)\n\nexport const DialogDescription = ({ children, className }: PartProps) => (\n  <ArkDialog.Description className={cx('text-weft-dim', className)}>{children}</ArkDialog.Description>\n)\n\nexport const DialogBody = ({ children, className }: PartProps) => (\n  <div className={cx('flex flex-col gap-(--stack) px-(--cell-x) py-4', className)}>{children}</div>\n)\n\nexport const DialogFooter = ({ children, className }: PartProps) => (\n  <footer\n    className={cx('flex items-center justify-end gap-(--stack) border-t border-reed px-(--cell-x) py-(--stack)', className)}\n  >\n    {children}\n  </footer>\n)\n\nexport const DialogClose = ArkDialog.CloseTrigger\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/0.11.0/dialog.json",
        "files": [
          {
            "path": "components/ui/dialog/Dialog.tsx",
            "hash": "sha256-WYJQSXoupoL8kXZyk/yfr91HkuKPwvJBbNGxLuJjVv4=",
            "client": true
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "empty-state",
      "type": "registry:ui",
      "title": "Empty State",
      "dependencies": [],
      "registryDependencies": [
        "cx",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/empty-state/EmptyState.tsx",
          "target": "components/ui/empty-state/EmptyState.tsx",
          "type": "registry:ui",
          "content": "import type { ReactNode } from 'react'\nimport { cx } from '@/lib/cx'\n\ninterface EmptyStateProps {\n  readonly title: string\n  readonly description?: string\n  readonly action?: ReactNode\n  readonly className?: string\n}\n\n/* the text sits on a patch of cloth. no warp runs behind it. */\nexport const EmptyState = ({ title, description, action, className }: EmptyStateProps) => (\n  <div\n    className={cx('reed-warp flex flex-col items-center justify-center gap-(--stack) p-(--cell-x) text-center', className)}\n    style={{ minHeight: 'calc(var(--row-h) * 4)' }}\n  >\n    <div className=\"flex flex-col gap-1 bg-raised px-(--cell-x)\">\n      <p className=\"text-weft-dim\">{title}</p>\n      {description && <p className=\"text-weft-faint\">{description}</p>}\n    </div>\n    {action}\n  </div>\n)\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/0.11.0/empty-state.json",
        "files": [
          {
            "path": "components/ui/empty-state/EmptyState.tsx",
            "hash": "sha256-Bag74bmIj81j/gjAWETKZdiA20VRuDnQWHsAzca0Nu4=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "field",
      "type": "registry:ui",
      "title": "Field",
      "dependencies": [
        "@ark-ui/react@^5.38.1"
      ],
      "registryDependencies": [
        "cx",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/field/Field.tsx",
          "target": "components/ui/field/Field.tsx",
          "type": "registry:ui",
          "content": "import { Field as ArkField, type FieldRootProps } from '@ark-ui/react/field'\nimport { Fieldset } from '@ark-ui/react/fieldset'\nimport type { ComponentPropsWithoutRef, ReactNode } from 'react'\nimport { cx } from '@/lib/cx'\n\nexport const Field = ({ className, ...props }: FieldRootProps) => (\n  <ArkField.Root className={cx('flex flex-col gap-1', className)} {...props} />\n)\n\ninterface PartProps {\n  readonly children: ReactNode\n  readonly className?: string\n}\n\nexport const FieldLabel = ({ children, className }: PartProps) => (\n  <ArkField.Label className={cx('flex items-center gap-1 text-weft-dim', className)}>\n    {children}\n    <ArkField.RequiredIndicator className=\"text-weft-faint\" />\n  </ArkField.Label>\n)\n\nexport const FieldInput = ({ className, ...props }: ComponentPropsWithoutRef<'input'>) => (\n  <ArkField.Input className={cx('ctl ctl-entry h-(--ctl-h)', className)} {...props} />\n)\n\nexport const FieldTextarea = ({ className, ...props }: ComponentPropsWithoutRef<'textarea'>) => (\n  <ArkField.Textarea className={cx('ctl ctl-entry min-h-20 resize-y py-(--stack) leading-relaxed', className)} {...props} />\n)\n\nexport const FieldHint = ({ children, className }: PartProps) => (\n  <ArkField.HelperText className={cx('text-weft-faint dense:sr-only', className)}>{children}</ArkField.HelperText>\n)\n\nexport const FieldError = ({ children, className }: PartProps) => (\n  <ArkField.ErrorText className={cx('text-madder', className)}>{children}</ArkField.ErrorText>\n)\n\ninterface FieldSetProps {\n  readonly legend: string\n  readonly children: ReactNode\n  readonly disabled?: boolean\n  readonly className?: string\n}\n\nexport const FieldSet = ({ legend, children, disabled, className }: FieldSetProps) => (\n  <Fieldset.Root disabled={disabled} className={cx('flex flex-col gap-(--stack)', className)}>\n    <Fieldset.Legend className=\"reed-edge w-full pb-(--stack) font-medium\">{legend}</Fieldset.Legend>\n    {children}\n  </Fieldset.Root>\n)\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/0.11.0/field.json",
        "files": [
          {
            "path": "components/ui/field/Field.tsx",
            "hash": "sha256-QrSdLrV8aG3/U/YZqZTD+ZaqJVOLnTcq2lxYzIOD1C0=",
            "client": true
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "figure",
      "type": "registry:ui",
      "title": "Figure",
      "dependencies": [],
      "registryDependencies": [
        "cx",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/figure/Figure.tsx",
          "target": "components/ui/figure/Figure.tsx",
          "type": "registry:ui",
          "content": "import { cx } from '@/lib/cx'\n\ninterface FigureProps {\n  readonly value: number\n  readonly digits?: number\n  readonly low?: boolean\n  readonly className?: string\n}\n\n/* anything that is not a digit reads one step back */\nexport const Figure = ({ value, digits = 1, low = false, className }: FigureProps) => {\n  if (value === 0) return <span className={cx('reed-mark', className)} aria-hidden=\"true\" />\n\n  const [whole, fraction] = value.toFixed(digits).split('.')\n  return (\n    <span className={cx(low && 'reed-under', className)}>\n      {whole}\n      {fraction !== undefined && <span className=\"text-weft-faint\">.{fraction}</span>}\n      {low && <span className=\"sr-only\"> below threshold</span>}\n    </span>\n  )\n}\n\ninterface ElapsedProps {\n  readonly minutes: number\n  readonly className?: string\n}\n\n/* a head cannot hold two units, so a duration carries them in the cell */\nexport const Elapsed = ({ minutes, className }: ElapsedProps) => (\n  <span className={className}>\n    {Math.trunc(minutes / 60)}\n    <span className=\"text-weft-faint\">h </span>\n    {String(minutes % 60).padStart(2, '0')}\n    <span className=\"text-weft-faint\">m</span>\n  </span>\n)\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/0.11.0/figure.json",
        "files": [
          {
            "path": "components/ui/figure/Figure.tsx",
            "hash": "sha256-5jAVPu0perAN109/cZwmsWbtqYiptBwoMGs5Cj+TXng=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "filter-bar",
      "type": "registry:ui",
      "title": "Filter Bar",
      "dependencies": [],
      "registryDependencies": [
        "cx",
        "icons",
        "select",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/filter-bar/FilterBar.tsx",
          "target": "components/ui/filter-bar/FilterBar.tsx",
          "type": "registry:ui",
          "content": "import { useMemo } from 'react'\nimport { CloseIcon, SearchIcon } from '@/components/ui/icons/Icons'\nimport {\n  Select,\n  SelectContent,\n  SelectOption,\n  SelectTrigger,\n  createListCollection,\n} from '@/components/ui/select/Select'\nimport { cx } from '@/lib/cx'\n\nexport interface FilterGroup {\n  readonly key: string\n  readonly label: string\n  readonly options: readonly string[]\n}\n\nexport type FilterValues = Readonly<Record<string, readonly string[]>>\n\ninterface FilterSelectProps {\n  readonly group: FilterGroup\n  readonly selected: readonly string[]\n  readonly onSelect: (next: readonly string[]) => void\n}\n\nconst FilterSelect = ({ group, selected, onSelect }: FilterSelectProps) => {\n  const collection = useMemo(\n    () => createListCollection({ items: group.options.map((value) => ({ label: value, value })) }),\n    [group.options],\n  )\n\n  return (\n    <Select\n      collection={collection}\n      multiple\n      value={[...selected]}\n      onValueChange={(details) => onSelect(details.value)}\n    >\n      <SelectTrigger>\n        <span className=\"inline-flex items-baseline gap-1.5\">\n          <span className={selected.length > 0 ? 'text-weft' : 'text-weft-dim'}>{group.label}</span>\n          {selected.length > 0 && <span className=\"tnum font-data text-indigo\">{selected.length}</span>}\n        </span>\n      </SelectTrigger>\n      <SelectContent>\n        {collection.items.map((item) => (\n          <SelectOption key={item.value} item={item} />\n        ))}\n      </SelectContent>\n    </Select>\n  )\n}\n\ninterface Chip {\n  readonly group: FilterGroup\n  readonly value: string\n}\n\ninterface FilterBarProps {\n  readonly query: string\n  readonly onQueryChange: (next: string) => void\n  readonly groups: readonly FilterGroup[]\n  readonly values: FilterValues\n  readonly onValuesChange: (next: FilterValues) => void\n  readonly searchLabel?: string\n  readonly placeholder?: string\n  readonly className?: string\n}\n\nexport const FilterBar = ({\n  query,\n  onQueryChange,\n  groups,\n  values,\n  onValuesChange,\n  searchLabel = 'Search',\n  placeholder = 'Search',\n  className,\n}: FilterBarProps) => {\n  const chips: readonly Chip[] = groups.flatMap((group) =>\n    (values[group.key] ?? []).map((value) => ({ group, value })),\n  )\n\n  const setGroup = (key: string, next: readonly string[]) => onValuesChange({ ...values, [key]: next })\n\n  const removeChip = (chip: Chip) => {\n    setGroup(\n      chip.group.key,\n      (values[chip.group.key] ?? []).filter((value) => value !== chip.value),\n    )\n  }\n\n  return (\n    <search className={cx('flex flex-col gap-(--stack)', className)}>\n      <div className=\"flex flex-wrap items-center gap-(--stack)\">\n        <div className=\"ctl focus-ring h-(--ctl-h) min-w-55 flex-1\">\n          <SearchIcon className=\"size-3.5 text-weft-faint\" />\n          <input\n            type=\"search\"\n            name=\"search\"\n            value={query}\n            onChange={(event) => onQueryChange(event.target.value)}\n            placeholder={placeholder}\n            aria-label={searchLabel}\n            className=\"w-full bg-transparent py-0 placeholder:text-weft-faint focus:outline-none\"\n          />\n        </div>\n\n        {groups.map((group) => (\n          <FilterSelect\n            key={group.key}\n            group={group}\n            selected={values[group.key] ?? []}\n            onSelect={(next) => setGroup(group.key, next)}\n          />\n        ))}\n      </div>\n\n      {chips.length > 0 && (\n        <ul className=\"flex flex-wrap items-center gap-(--stack)\">\n          {chips.map((chip) => (\n            <li key={`${chip.group.key}:${chip.value}`}>\n              <button\n                type=\"button\"\n                onClick={() => removeChip(chip)}\n                className=\"inline-flex h-(--ctl-h) cursor-pointer items-center gap-1.5 border border-indigo/40 bg-indigo-wash px-(--cell-x) text-weft transition-colors duration-(--dur-instant) ease-(--ease-beat) hover:border-indigo\"\n              >\n                <span className=\"inline-flex items-baseline gap-1.5\">\n                  <span className=\"font-data text-weft-dim\">{chip.group.label.toLowerCase()}</span>\n                  {chip.value}\n                </span>\n                <CloseIcon className=\"size-3 text-weft-faint\" />\n              </button>\n            </li>\n          ))}\n          <li>\n            <button\n              type=\"button\"\n              onClick={() => onValuesChange({})}\n              className=\"h-(--ctl-h) cursor-pointer px-(--cell-x) text-weft-dim underline-offset-4 hover:text-weft hover:underline\"\n            >\n              Clear filters\n            </button>\n          </li>\n        </ul>\n      )}\n    </search>\n  )\n}\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/0.11.0/filter-bar.json",
        "files": [
          {
            "path": "components/ui/filter-bar/FilterBar.tsx",
            "hash": "sha256-lAJgLydXWTY8ooTOU5VnsHEYwlKiFXM8AZ5R2VyOlFM=",
            "client": true
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "icons",
      "type": "registry:ui",
      "title": "Icons",
      "dependencies": [],
      "registryDependencies": [
        "cx",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/icons/Icons.tsx",
          "target": "components/ui/icons/Icons.tsx",
          "type": "registry:ui",
          "content": "import { cx } from '@/lib/cx'\n\ninterface IconProps {\n  readonly className?: string\n}\n\nconst base = 'shrink-0'\n\nexport const CheckIcon = ({ className }: IconProps) => (\n  <svg className={cx(base, className)} viewBox=\"0 0 16 16\" fill=\"none\" aria-hidden=\"true\" focusable=\"false\">\n    <path d=\"M3.5 8.5 6.5 11.5 12.5 5\" stroke=\"currentColor\" strokeWidth=\"2\" strokeLinecap=\"square\" />\n  </svg>\n)\n\nexport const SearchIcon = ({ className }: IconProps) => (\n  <svg className={cx(base, className)} viewBox=\"0 0 16 16\" fill=\"none\" aria-hidden=\"true\" focusable=\"false\">\n    <circle cx=\"7\" cy=\"7\" r=\"4.25\" stroke=\"currentColor\" strokeWidth=\"1.5\" />\n    <path d=\"M10.5 10.5 14 14\" stroke=\"currentColor\" strokeWidth=\"1.5\" strokeLinecap=\"round\" />\n  </svg>\n)\n\nexport const ChevronIcon = ({ className }: IconProps) => (\n  <svg className={cx(base, className)} viewBox=\"0 0 16 16\" fill=\"none\" aria-hidden=\"true\" focusable=\"false\">\n    <path d=\"m4 6.5 4 4 4-4\" stroke=\"currentColor\" strokeWidth=\"1.5\" strokeLinecap=\"round\" strokeLinejoin=\"round\" />\n  </svg>\n)\n\nexport const CloseIcon = ({ className }: IconProps) => (\n  <svg className={cx(base, className)} viewBox=\"0 0 16 16\" fill=\"none\" aria-hidden=\"true\" focusable=\"false\">\n    <path d=\"m4 4 8 8M12 4l-8 8\" stroke=\"currentColor\" strokeWidth=\"1.5\" strokeLinecap=\"round\" />\n  </svg>\n)\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/0.11.0/icons.json",
        "files": [
          {
            "path": "components/ui/icons/Icons.tsx",
            "hash": "sha256-tKgwm260H+guNo4nhWzwTzjxDl4T+yhQlKJrL/0sqto=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "panel",
      "type": "registry:ui",
      "title": "Panel",
      "dependencies": [
        "@ark-ui/react@^5.38.1"
      ],
      "registryDependencies": [
        "cx",
        "icons",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/panel/Panel.tsx",
          "target": "components/ui/panel/Panel.tsx",
          "type": "registry:ui",
          "content": "import { Dialog } from '@ark-ui/react/dialog'\nimport { Portal } from '@ark-ui/react/portal'\nimport { useEffect, useRef, useState, type ReactNode } from 'react'\nimport { CloseIcon } from '@/components/ui/icons/Icons'\nimport { cx } from '@/lib/cx'\n\nexport type PanelSide = 'start' | 'end'\n\n/* below this the panel covers the page and has to behave as a modal */\nconst SHEET = '(max-width: 640px)'\n\nfunction useSheet() {\n  const [sheet, setSheet] = useState(false)\n\n  useEffect(() => {\n    const query = window.matchMedia(SHEET)\n    const apply = () => setSheet(query.matches)\n    apply()\n    query.addEventListener('change', apply)\n    return () => query.removeEventListener('change', apply)\n  }, [])\n\n  return sheet\n}\n\n/*\n * zag returns focus from inside its focus trap, and it only runs that trap when the dialog is\n * modal. so a panel that leaves the page live drops the reader on the body when it closes, and\n * has to put focus back itself. closing is always started from inside the panel here, by escape\n * or by the close button, so there is no other claim on focus to weigh.\n */\nfunction useReturnFocus(open: boolean, trapped: boolean) {\n  const origin = useRef<HTMLElement | null>(null)\n\n  useEffect(() => {\n    if (trapped) return\n\n    if (open) {\n      const active = document.activeElement\n      origin.current = active instanceof HTMLElement ? active : null\n      return\n    }\n\n    const element = origin.current\n    origin.current = null\n    if (element?.isConnected) element.focus()\n  }, [open, trapped])\n}\n\n/* the positioner holds the edge; the border belongs to the part that animates */\nconst SIDE: Record<PanelSide, string> = {\n  start: 'start-0 [--slide-from:-100%]',\n  end: 'end-0 [--slide-from:100%]',\n}\n\ninterface PanelProps {\n  readonly open: boolean\n  readonly onOpenChange: (open: boolean) => void\n  readonly title: string\n  readonly description?: string\n  readonly footer?: ReactNode\n  readonly side?: PanelSide\n  readonly children: ReactNode\n  readonly className?: string\n}\n\n/*\n * not modal while the table stays live: no focus trap, and a click outside picks the\n * next row rather than closing. on a screen it covers, it locks the page.\n */\nexport const Panel = ({\n  open,\n  onOpenChange,\n  title,\n  description,\n  footer,\n  side = 'end',\n  children,\n  className,\n}: PanelProps) => {\n  const sheet = useSheet()\n  useReturnFocus(open, sheet)\n\n  return (\n    <Dialog.Root\n      open={open}\n      onOpenChange={(details) => onOpenChange(details.open)}\n      modal={sheet}\n      closeOnInteractOutside={false}\n      unmountOnExit\n      lazyMount\n    >\n      <Portal>\n        <Dialog.Positioner className={cx('fixed inset-y-0 z-(--z-panel) flex', SIDE[side])}>\n          <Dialog.Content\n            className={cx(\n              'layer flex h-full w-[min(420px,100vw)] flex-col border-reed-lit data-[state=open]:animate-[slide_var(--dur-overlay)_var(--ease-beat)] data-[state=closed]:animate-[slide-out_var(--dur-local)_var(--ease-exit)]',\n              className,\n            )}\n          >\n            <header className=\"reed-edge flex items-start justify-between gap-4 px-(--cell-x) py-(--stack)\">\n              <div className=\"flex min-w-0 flex-col gap-0.5\">\n                <Dialog.Title className=\"truncate font-medium\">{title}</Dialog.Title>\n                {description && (\n                  <Dialog.Description className=\"truncate text-weft-dim\">{description}</Dialog.Description>\n                )}\n              </div>\n              <Dialog.CloseTrigger\n                aria-label=\"Close the panel\"\n                className=\"-mr-1 cursor-pointer p-1 text-weft-faint transition-colors duration-(--dur-instant) ease-(--ease-beat) hover:text-weft\"\n              >\n                <CloseIcon className=\"size-3.5\" />\n              </Dialog.CloseTrigger>\n            </header>\n\n            <div className=\"reed-scroll flex-1 overflow-auto\">{children}</div>\n\n            {footer && (\n              <footer className=\"flex items-center justify-end gap-(--stack) border-t border-reed px-(--cell-x) py-(--stack)\">\n                {footer}\n              </footer>\n            )}\n          </Dialog.Content>\n        </Dialog.Positioner>\n      </Portal>\n    </Dialog.Root>\n  )\n}\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/0.11.0/panel.json",
        "files": [
          {
            "path": "components/ui/panel/Panel.tsx",
            "hash": "sha256-1uHoIk8MoLHO0UVDkXQusuo/BRhViuFamafhAWtTaTM=",
            "client": true
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "popover",
      "type": "registry:ui",
      "title": "Popover",
      "dependencies": [
        "@ark-ui/react@^5.38.1"
      ],
      "registryDependencies": [
        "cx",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/popover/Popover.tsx",
          "target": "components/ui/popover/Popover.tsx",
          "type": "registry:ui",
          "content": "import { Popover as ArkPopover } from '@ark-ui/react/popover'\nimport { Portal } from '@ark-ui/react/portal'\nimport type { ReactNode } from 'react'\nimport { cx } from '@/lib/cx'\n\nexport const Popover = ArkPopover.Root\nexport const PopoverTrigger = ArkPopover.Trigger\n\ninterface PartProps {\n  readonly children: ReactNode\n  readonly className?: string\n}\n\n/* the positioner has no rank here: the stylesheet reaches ark's inline value */\nexport const PopoverContent = ({ children, className }: PartProps) => (\n  <Portal>\n    <ArkPopover.Positioner>\n      <ArkPopover.Content\n        className={cx(\n          'layer min-w-52 py-1 focus:outline-none data-[state=open]:animate-[rise_var(--dur-local)_var(--ease-beat)]',\n          className,\n        )}\n      >\n        {children}\n      </ArkPopover.Content>\n    </ArkPopover.Positioner>\n  </Portal>\n)\n\nexport const PopoverTitle = ({ children, className }: PartProps) => (\n  <ArkPopover.Title className={cx('reed-edge px-(--cell-x) pt-1 pb-(--stack) font-medium', className)}>\n    {children}\n  </ArkPopover.Title>\n)\n\nexport const PopoverDescription = ({ children, className }: PartProps) => (\n  <ArkPopover.Description className={cx('px-(--cell-x) py-1 text-weft-dim', className)}>\n    {children}\n  </ArkPopover.Description>\n)\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/0.11.0/popover.json",
        "files": [
          {
            "path": "components/ui/popover/Popover.tsx",
            "hash": "sha256-FG3l9NEZyEnPjfggxNvFja7v6cz6d3N5S21G1EAXaIM=",
            "client": true
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "select",
      "type": "registry:ui",
      "title": "Select",
      "dependencies": [
        "@ark-ui/react@^5.38.1"
      ],
      "registryDependencies": [
        "cx",
        "icons",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/select/Select.tsx",
          "target": "components/ui/select/Select.tsx",
          "type": "registry:ui",
          "content": "import { Portal } from '@ark-ui/react/portal'\nimport { Select as ArkSelect } from '@ark-ui/react/select'\nimport type { ReactNode } from 'react'\nimport { CheckIcon, ChevronIcon } from '@/components/ui/icons/Icons'\nimport { cx } from '@/lib/cx'\n\nexport { createListCollection } from '@ark-ui/react/select'\n\nexport interface SelectOptionItem {\n  readonly label: string\n  readonly value: string\n}\n\nexport const Select = ArkSelect.Root\n\ninterface PartProps {\n  readonly children: ReactNode\n  readonly className?: string\n}\n\nexport const SelectTrigger = ({ children, className }: PartProps) => (\n  <ArkSelect.Control>\n    <ArkSelect.Trigger className={cx('ctl h-(--ctl-h) cursor-pointer', className)}>\n      {children}\n      <ChevronIcon className=\"size-3 text-weft-faint\" />\n    </ArkSelect.Trigger>\n  </ArkSelect.Control>\n)\n\n/* rank comes from the stylesheet; ark sets it inline on the positioner */\nexport const SelectContent = ({ children, className }: PartProps) => (\n  <Portal>\n    <ArkSelect.Positioner>\n      <ArkSelect.Content className={cx('layer min-w-42 py-1 focus:outline-none', className)}>\n        {children}\n      </ArkSelect.Content>\n    </ArkSelect.Positioner>\n  </Portal>\n)\n\ninterface SelectOptionProps {\n  readonly item: SelectOptionItem\n  readonly className?: string\n}\n\n/* the shed follows the keyboard, the selvedge marks the choice */\nexport const SelectOption = ({ item, className }: SelectOptionProps) => (\n  <ArkSelect.Item\n    item={item}\n    className={cx(\n      'selvedge flex cursor-pointer items-center justify-between gap-3 px-(--cell-x) py-1 text-weft-dim data-highlighted:bg-shed data-highlighted:text-weft data-[state=checked]:bg-indigo-wash data-[state=checked]:text-weft data-[state=checked]:selvedge-on',\n      className,\n    )}\n  >\n    <ArkSelect.ItemText>{item.label}</ArkSelect.ItemText>\n    <ArkSelect.ItemIndicator>\n      <CheckIcon className=\"size-3 text-indigo\" />\n    </ArkSelect.ItemIndicator>\n  </ArkSelect.Item>\n)\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/0.11.0/select.json",
        "files": [
          {
            "path": "components/ui/select/Select.tsx",
            "hash": "sha256-lk5MRYwtnhdIrV+B4J2zCVZUWK3NPwxoFXMDnAaJjqM=",
            "client": true
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "table",
      "type": "registry:ui",
      "title": "Table",
      "dependencies": [],
      "registryDependencies": [
        "checkbox",
        "cx",
        "empty-state",
        "tokens",
        "tooltip"
      ],
      "files": [
        {
          "path": "components/ui/table/Table.tsx",
          "target": "components/ui/table/Table.tsx",
          "type": "registry:ui",
          "content": "import {\n  useEffect,\n  useMemo,\n  useRef,\n  useState,\n  type KeyboardEvent,\n  type MouseEvent,\n  type PointerEvent,\n  type ReactNode,\n  type UIEvent,\n} from 'react'\nimport { Checkbox, type CheckedState } from '@/components/ui/checkbox/Checkbox'\nimport { EmptyState } from '@/components/ui/empty-state/EmptyState'\nimport { Tooltip } from '@/components/ui/tooltip/Tooltip'\nimport { cx } from '@/lib/cx'\n\nexport interface Column<T> {\n  readonly key: string\n  readonly label: string\n  readonly unit?: string\n  readonly hint?: string\n  /* the widest plausible value, in characters. the density turns it into a width. */\n  readonly chars: number\n  readonly numeric?: boolean\n  readonly sortValue?: (row: T) => string | number\n  readonly render: (row: T) => ReactNode\n}\n\ntype Direction = 'asc' | 'desc'\n\ninterface Sort {\n  readonly key: string\n  readonly direction: Direction\n}\n\nconst MIN_WIDTH = 56\nconst MAX_WIDTH = 420\nconst KEY_STEP = 8\n\n/* the 7px mark and its 6px gap: two characters cover both in every density */\nconst SORT_CHARS = 2\n\n/*\n * below this many rows the whole body is rendered. the widest viewport shows about 20\n * rows at the dense height, so this leaves several screens before a window is worth it.\n */\nconst WINDOW_MIN = 100\nconst OVERSCAN = 6\n\n/*\n * an effect supplies the row height, which leaves the first commit of a long batch without\n * one. 40 rows covers the 520px body cap at the dense height with the overscan on top.\n */\nconst FIRST_ROWS = 40\n\n/* doubles as the left offset of the pinned column beside it */\nconst GUTTER = 'calc(var(--cell-x) * 2 + var(--ctl-box))'\n\n/*\n * a pinned cell hides what travels under it, so it needs an opaque background and a\n * transition of its own. a parent transition does not animate an inherited value.\n */\nconst PINNED = 'warp-line-end bg-inherit px-(--cell-x) transition-colors duration-(--dur-instant) ease-(--ease-beat)'\n\nfunction compare(a: string | number, b: string | number) {\n  if (typeof a === 'number' && typeof b === 'number') return a - b\n  return String(a).localeCompare(String(b))\n}\n\nfunction ariaSort<T>(sort: Sort | null, column: Column<T>) {\n  if (column.sortValue === undefined) return undefined\n  if (sort?.key !== column.key) return 'none'\n  return sort.direction === 'asc' ? 'ascending' : 'descending'\n}\n\n/* a plural noun alone reads \"1 rows\" at a count of one, so a caller can give both forms */\nfunction countNoun(noun: string | readonly [one: string, many: string], count: number) {\n  if (typeof noun === 'string') return noun\n  return count === 1 ? noun[0] : noun[1]\n}\n\nfunction clampWidth(value: number) {\n  return Math.min(MAX_WIDTH, Math.max(MIN_WIDTH, Math.round(value)))\n}\n\n/*\n * the head counts too, because a label and its unit can run longer than the value\n * under it. the half character covers the rounding a table layout applies.\n *\n * the head is drawn in the interface face and the value under it in the data face,\n * so the head takes whichever of the two advances is wider. `1ch` is the interface\n * face's own digit advance, read off this cell. where the data face is the wider of\n * the pair the expression collapses to what it always was.\n */\nfunction intrinsicWidth<T>(column: Column<T>, sorted: boolean) {\n  const unit = column.unit ? column.unit.length + 1 : 0\n  const head = column.label.length + unit + (sorted ? SORT_CHARS : 0)\n  const value = `${column.chars + 0.5} * var(--data-adv)`\n  const label = `${head + 0.5} * max(var(--data-adv), 1ch)`\n  return `calc(max(${value}, ${label}) + var(--cell-x) * 2)`\n}\n\ninterface ColumnGripProps {\n  readonly label: string\n  readonly onResize: (next: number) => void\n}\n\n/* the width on screen. a column that never moved holds no px of its own. */\nconst cellWidth = (grip: HTMLButtonElement) => grip.parentElement?.offsetWidth ?? 0\n\nconst ColumnGrip = ({ label, onResize }: ColumnGripProps) => {\n  const originX = useRef(0)\n  const originWidth = useRef(0)\n  const [dragging, setDragging] = useState(false)\n\n  const onPointerDown = (event: PointerEvent<HTMLButtonElement>) => {\n    event.currentTarget.setPointerCapture(event.pointerId)\n    originX.current = event.clientX\n    originWidth.current = cellWidth(event.currentTarget)\n    setDragging(true)\n  }\n\n  const onPointerMove = (event: PointerEvent<HTMLButtonElement>) => {\n    if (!dragging) return\n    onResize(originWidth.current + event.clientX - originX.current)\n  }\n\n  const onKeyDown = (event: KeyboardEvent<HTMLButtonElement>) => {\n    if (event.key !== 'ArrowLeft' && event.key !== 'ArrowRight') return\n    event.preventDefault()\n    const from = cellWidth(event.currentTarget)\n    onResize(event.key === 'ArrowLeft' ? from - KEY_STEP : from + KEY_STEP)\n  }\n\n  return (\n    <button\n      type=\"button\"\n      aria-label={`Resize the ${label} column`}\n      className=\"reed-grip\"\n      data-dragging={dragging ? '' : undefined}\n      onPointerDown={onPointerDown}\n      onPointerMove={onPointerMove}\n      onLostPointerCapture={() => setDragging(false)}\n      onKeyDown={onKeyDown}\n    />\n  )\n}\n\nconst SortMark = ({ direction }: { readonly direction: Direction }) => (\n  <span className={cx('reed-sort text-indigo', direction === 'desc' && 'reed-sort-down')} aria-hidden=\"true\" />\n)\n\ninterface ColumnHeadProps<T> {\n  readonly column: Column<T>\n  readonly sort: Sort | null\n  readonly onSort: () => void\n}\n\nconst ColumnHead = <T,>({ column, sort, onSort }: ColumnHeadProps<T>) => {\n  const shell = cx('flex h-full w-full items-center gap-1.5 text-weft-dim', column.numeric && 'justify-end')\n  /* the mark sits inside the baseline group, standing on the baseline of the label */\n  const label = (\n    <span className=\"inline-flex min-w-0 items-baseline gap-1.5\">\n      <span className=\"truncate\">{column.label}</span>\n      {column.unit && <span className=\"font-data text-weft-faint\">{column.unit}</span>}\n      {sort?.key === column.key && <SortMark direction={sort.direction} />}\n    </span>\n  )\n\n  const head = column.sortValue ? (\n    <button\n      type=\"button\"\n      onClick={onSort}\n      className={cx(shell, 'cursor-pointer transition-colors duration-(--dur-instant) ease-(--ease-beat) hover:text-weft')}\n    >\n      {label}\n    </button>\n  ) : (\n    <div className={shell}>{label}</div>\n  )\n\n  if (column.hint === undefined) return head\n  return <Tooltip content={column.hint}>{head}</Tooltip>\n}\n\nconst SKELETON_IDS: readonly string[] = ['a', 'b', 'c', 'd', 'e', 'f', 'g', 'h'].map((id) => `warp-${id}`)\n\nconst WarpRows = ({ span }: { readonly span: number }) => (\n  <>\n    {SKELETON_IDS.map((id, index) => (\n      <tr key={id}>\n        <td\n          colSpan={span}\n          className=\"reed-warp reed-warp-beat\"\n          style={{ height: 'var(--row-h)', animationDelay: `calc(var(--dur-instant) * ${index})` }}\n        />\n      </tr>\n    ))}\n  </>\n)\n\n/* the rows outside the window still take their height, so the scrollbar tells the truth */\nconst Spacer = ({ span, height }: { readonly span: number; readonly height: number }) => (\n  <tr aria-hidden=\"true\">\n    <td colSpan={span} style={{ height, padding: 0, border: 0 }} />\n  </tr>\n)\n\ninterface EmptyRowProps {\n  readonly span: number\n  readonly message: string\n}\n\nconst EmptyRow = ({ span, message }: EmptyRowProps) => (\n  <tr>\n    <td colSpan={span} className=\"border-t border-reed/60 p-0\">\n      <EmptyState title={message} />\n    </td>\n  </tr>\n)\n\nconst NAV_KEYS: ReadonlySet<string> = new Set(['ArrowDown', 'ArrowUp', 'Home', 'End'])\n\n/* a control in a cell owns its own click, and the checkbox sits inside a label */\nconst INTERACTIVE = 'a, button, input, select, textarea, label'\n\ninterface RowProps<T> {\n  readonly row: T\n  readonly id: string\n  readonly columns: readonly Column<T>[]\n  readonly selected: boolean\n  readonly onToggle: (id: string) => void\n  readonly rowIndex: number\n  readonly position: number\n  readonly cursor: boolean\n  readonly onNavigate: (from: number, key: string) => void\n  readonly onActivate?: (row: T) => void\n}\n\nconst Row = <T,>({\n  row,\n  id,\n  columns,\n  selected,\n  onToggle,\n  rowIndex,\n  position,\n  cursor,\n  onNavigate,\n  onActivate,\n}: RowProps<T>) => {\n  const onClick = (event: MouseEvent<HTMLTableRowElement>) => {\n    if (onActivate === undefined) return\n    if (event.target instanceof Element && event.target.closest(INTERACTIVE) !== null) return\n    onActivate(row)\n  }\n\n  const onKeyDown = (event: KeyboardEvent<HTMLTableRowElement>) => {\n    /* a key pressed on the checkbox inside the row belongs to the checkbox */\n    if (event.target !== event.currentTarget) return\n\n    if (event.key === ' ') {\n      event.preventDefault()\n      onToggle(id)\n      return\n    }\n\n    if (event.key === 'Enter' && onActivate !== undefined) {\n      event.preventDefault()\n      onActivate(row)\n      return\n    }\n\n    if (!NAV_KEYS.has(event.key)) return\n    event.preventDefault()\n    onNavigate(position, event.key)\n  }\n\n  return (\n    <tr\n      aria-rowindex={rowIndex}\n      tabIndex={cursor ? 0 : -1}\n      onKeyDown={onKeyDown}\n      onClick={onClick}\n      className={cx(\n        'focus-row bg-raised transition-colors duration-(--dur-instant) ease-(--ease-beat) hover:bg-shed data-selected:bg-indigo-wash data-selected:hover:bg-indigo-wash',\n        onActivate !== undefined && 'cursor-pointer',\n      )}\n      data-selected={selected ? '' : undefined}\n    >\n      <td\n        className={cx('selvedge sticky left-0 z-(--z-pinned) border-t border-reed/60', PINNED, selected && 'selvedge-on')}\n        style={{ height: 'var(--row-h)' }}\n      >\n        <Checkbox checked={selected} onCheckedChange={() => onToggle(id)} label={`Select row ${id}`} />\n      </td>\n      {columns.map((column, index) => (\n        <td\n          key={column.key}\n          className={cx(\n            'truncate border-t border-reed/60 px-(--cell-x) text-weft-dim',\n            index === 0 && `sticky z-(--z-pinned) ${PINNED}`,\n            index > 1 && 'warp-line',\n            column.numeric && 'tnum text-right font-data text-weft',\n          )}\n          style={{ height: 'var(--row-h)', left: index === 0 ? GUTTER : undefined }}\n        >\n          {column.render(row)}\n        </td>\n      ))}\n      <td className=\"warp-line border-t border-reed/60\" />\n    </tr>\n  )\n}\n\ninterface TableProps<T> {\n  readonly rows: readonly T[]\n  readonly columns: readonly Column<T>[]\n  readonly rowId: (row: T) => string\n  readonly title: string\n  readonly noun?: string | readonly [one: string, many: string]\n  readonly emptyMessage?: string\n  readonly loading?: boolean\n  readonly actions?: ReactNode\n  readonly onSelectionChange?: (selected: ReadonlySet<string>) => void\n  /* the row draws a pointer and answers Enter once this is given */\n  readonly onRowActivate?: (row: T) => void\n  readonly className?: string\n}\n\nexport const Table = <T,>({\n  rows,\n  columns,\n  rowId,\n  title,\n  noun = 'rows',\n  emptyMessage = 'No row matches the filters.',\n  loading = false,\n  actions,\n  onSelectionChange,\n  onRowActivate,\n  className,\n}: TableProps<T>) => {\n  const [selected, setSelected] = useState<ReadonlySet<string>>(new Set())\n  /* only a dragged column holds a px width; the rest follow the density */\n  const [widths, setWidths] = useState<Record<string, number | undefined>>({})\n  const [sort, setSort] = useState<Sort | null>(null)\n  const [cursor, setCursor] = useState<string | null>(null)\n\n  /* raised by a key press, so the focus chase below ignores a scroll the pointer made */\n  const chasing = useRef(false)\n\n  const span = columns.length + 2\n\n  const resizeColumn = (key: string, next: number) => {\n    setWidths((current) => ({ ...current, [key]: clampWidth(next) }))\n  }\n\n  /* third click restores the order the rows arrived in */\n  const toggleSort = (key: string) => {\n    setSort((current) => {\n      if (current?.key !== key) return { key, direction: 'asc' }\n      if (current.direction === 'asc') return { key, direction: 'desc' }\n      return null\n    })\n  }\n\n  const ordered = useMemo(() => {\n    const column = columns.find((entry) => entry.key === sort?.key)\n    const value = column?.sortValue\n    if (sort === null || value === undefined) return rows\n    const factor = sort.direction === 'asc' ? 1 : -1\n    return [...rows].sort((a, b) => factor * compare(value(a), value(b)))\n  }, [rows, columns, sort])\n\n  /*\n   * a filter can take a selected row off the screen. the count and the head control\n   * read only what is on screen, and the set keeps the rest for when it comes back.\n   */\n  const onScreen = useMemo(() => new Set(rows.map(rowId)), [rows, rowId])\n  const active = useMemo(() => new Set([...selected].filter((id) => onScreen.has(id))), [selected, onScreen])\n\n  const headerState = useMemo<CheckedState>(() => {\n    if (active.size === 0) return false\n    if (active.size === rows.length) return true\n    return 'indeterminate'\n  }, [active, rows.length])\n\n  /*\n   * every row is exactly --row-h, so the window is arithmetic and needs no per row\n   * measurement. the height is read off a real row rather than the token, which keeps\n   * it right when the density changes under the component.\n   */\n  const scroller = useRef<HTMLDivElement>(null)\n  const headRow = useRef<HTMLTableRowElement>(null)\n  const [metrics, setMetrics] = useState({ rowHeight: 0, viewport: 0 })\n  const [scrollTop, setScrollTop] = useState(0)\n\n  const long = ordered.length > WINDOW_MIN\n\n  /*\n   * the head row carries the same --row-h and keeps its identity for the life of the\n   * table, so it is the one element that can be observed. a body row is re-keyed on\n   * every scroll, which leaves the observer watching a detached node.\n   */\n  useEffect(() => {\n    const box = scroller.current\n    const head = headRow.current\n    if (!box || !head || !long) return undefined\n\n    const read = () => setMetrics({ rowHeight: head.getBoundingClientRect().height, viewport: box.clientHeight })\n    read()\n\n    const observer = new ResizeObserver(read)\n    observer.observe(box)\n    observer.observe(head)\n    return () => observer.disconnect()\n  }, [long])\n\n  const view = useMemo(() => {\n    if (!long) return { start: 0, end: ordered.length, before: 0, after: 0 }\n    /* the spacer height is unknown until the row height arrives, and a short body corrects itself */\n    if (metrics.rowHeight === 0) return { start: 0, end: Math.min(ordered.length, FIRST_ROWS), before: 0, after: 0 }\n\n    const visible = Math.ceil(metrics.viewport / metrics.rowHeight)\n    const start = Math.max(0, Math.floor(scrollTop / metrics.rowHeight) - OVERSCAN)\n    const end = Math.min(ordered.length, start + visible + OVERSCAN * 2)\n    return {\n      start,\n      end,\n      before: start * metrics.rowHeight,\n      after: (ordered.length - end) * metrics.rowHeight,\n    }\n  }, [long, metrics, scrollTop, ordered.length])\n\n  const onScroll = (event: UIEvent<HTMLDivElement>) => {\n    if (long) setScrollTop(event.currentTarget.scrollTop)\n  }\n\n  const cursorIndex = useMemo(() => {\n    if (cursor === null) return -1\n    return ordered.findIndex((row) => rowId(row) === cursor)\n  }, [cursor, ordered, rowId])\n\n  /*\n   * the row the cursor is on holds the only tab stop in the body, so leaving the table\n   * and coming back returns to it. a pointer scroll can carry that row out of the\n   * window, and the first rendered row takes the stop until an arrow moves it again.\n   */\n  const stop = cursorIndex >= view.start && cursorIndex < view.end ? cursorIndex : view.start\n\n  const moveCursor = (index: number) => {\n    const next = ordered[Math.min(Math.max(index, 0), ordered.length - 1)]\n    if (next === undefined) return\n    chasing.current = true\n    setCursor(rowId(next))\n  }\n\n  const navigate = (from: number, key: string) => {\n    if (key === 'ArrowDown') moveCursor(from + 1)\n    if (key === 'ArrowUp') moveCursor(from - 1)\n    if (key === 'Home') moveCursor(0)\n    if (key === 'End') moveCursor(ordered.length - 1)\n  }\n\n  /*\n   * a row outside the window does not exist to focus, so the scroll goes first and the\n   * effect runs again on the view it produces. `chasing` keeps it off a pointer scroll,\n   * which must not pull focus.\n   */\n  useEffect(() => {\n    if (!chasing.current || cursorIndex === -1) return\n\n    const box = scroller.current\n    const row = box?.querySelector<HTMLTableRowElement>(`tbody tr[aria-rowindex='${cursorIndex + 2}']`)\n    if (row) {\n      chasing.current = false\n      row.focus({ preventScroll: true })\n      row.scrollIntoView({ block: 'nearest' })\n      return\n    }\n\n    if (box && metrics.rowHeight > 0) {\n      const top = cursorIndex * metrics.rowHeight\n      const above = top < box.scrollTop\n      box.scrollTop = above ? top : top - metrics.viewport + metrics.rowHeight\n    }\n  }, [cursorIndex, view, metrics])\n\n  const applySelection = (next: ReadonlySet<string>) => {\n    setSelected(next)\n    onSelectionChange?.(new Set([...next].filter((id) => onScreen.has(id))))\n  }\n\n  const toggleRow = (id: string) => {\n    const next = new Set(selected)\n    if (!next.delete(id)) next.add(id)\n    applySelection(next)\n  }\n\n  const toggleAll = () => {\n    const next = new Set(selected)\n    if (active.size === rows.length) {\n      onScreen.forEach((id) => next.delete(id))\n    } else {\n      onScreen.forEach((id) => next.add(id))\n    }\n    applySelection(next)\n  }\n\n  const renderBody = () => {\n    if (loading) return <WarpRows span={span} />\n    if (ordered.length === 0) return <EmptyRow span={span} message={emptyMessage} />\n    return (\n      <>\n        {view.before > 0 && <Spacer span={span} height={view.before} />}\n        {ordered.slice(view.start, view.end).map((row, offset) => {\n          const id = rowId(row)\n          return (\n            <Row\n              key={id}\n              /* the head is row 1, and aria counts from there whatever the window shows */\n              rowIndex={view.start + offset + 2}\n              position={view.start + offset}\n              cursor={view.start + offset === stop}\n              onNavigate={navigate}\n              row={row}\n              id={id}\n              columns={columns}\n              selected={selected.has(id)}\n              onToggle={toggleRow}\n              onActivate={onRowActivate}\n            />\n          )\n        })}\n        {view.after > 0 && <Spacer span={span} height={view.after} />}\n      </>\n    )\n  }\n\n  return (\n    <section className={cx('@container isolate border border-reed bg-raised', className)}>\n      <header className=\"flex items-center justify-between gap-(--stack) border-b border-reed px-(--cell-x) py-(--stack)\">\n        <h2 className=\"font-medium\">{title}</h2>\n        <div className=\"flex items-center gap-(--stack)\">\n          {!loading && (\n            <p className=\"text-weft-dim tnum\">\n              {active.size > 0\n                ? `${active.size} of ${rows.length} selected`\n                : `${rows.length} ${countNoun(noun, rows.length)}`}\n            </p>\n          )}\n          {actions}\n        </div>\n      </header>\n\n      <div ref={scroller} onScroll={onScroll} className=\"reed-scroll max-h-[var(--table-body,32.5rem)] overflow-auto\">\n        {/* separate borders: a pinned cell paints its background over a collapsed one */}\n        <table\n          aria-busy={loading}\n          aria-rowcount={ordered.length + 1}\n          className=\"w-full table-fixed border-separate border-spacing-0 text-left\"\n        >\n          <thead className=\"sticky top-0 z-(--z-sticky)\">\n            <tr ref={headRow} aria-rowindex={1} className=\"bg-raised select-none\">\n              <th\n                scope=\"col\"\n                className=\"reed-edge warp-line-end sticky left-0 z-(--z-pinned) bg-raised px-(--cell-x)\"\n                style={{ height: 'var(--row-h)', width: GUTTER }}\n              >\n                <Checkbox checked={headerState} onCheckedChange={toggleAll} label={`Select all ${countNoun(noun, 2)}`} />\n              </th>\n              {columns.map((column, index) => (\n                <th\n                  key={column.key}\n                  scope=\"col\"\n                  aria-sort={ariaSort(sort, column)}\n                  className={cx(\n                    'reed-edge px-(--cell-x) font-medium',\n                    index === 0 && 'warp-line-end sticky z-(--z-pinned) bg-raised',\n                  )}\n                  style={{\n                    height: 'var(--row-h)',\n                    left: index === 0 ? GUTTER : undefined,\n                    width: widths[column.key] ?? intrinsicWidth(column, sort?.key === column.key),\n                  }}\n                >\n                  <ColumnHead column={column} sort={sort} onSort={() => toggleSort(column.key)} />\n                  <ColumnGrip label={column.label} onResize={(next) => resizeColumn(column.key, next)} />\n                </th>\n              ))}\n              <th scope=\"col\" className=\"reed-edge\">\n                <span className=\"sr-only\">Spare width</span>\n              </th>\n            </tr>\n          </thead>\n\n          <tbody>{renderBody()}</tbody>\n        </table>\n      </div>\n    </section>\n  )\n}\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/0.11.0/table.json",
        "files": [
          {
            "path": "components/ui/table/Table.tsx",
            "hash": "sha256-bK2q7NreY/UF84hJtb9BEhPhjCrCsoIWbAOyHOcPeOI=",
            "client": true
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "tabs",
      "type": "registry:ui",
      "title": "Tabs",
      "dependencies": [
        "@ark-ui/react@^5.38.1"
      ],
      "registryDependencies": [
        "cx",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/tabs/Tabs.tsx",
          "target": "components/ui/tabs/Tabs.tsx",
          "type": "registry:ui",
          "content": "import { Tabs as ArkTabs } from '@ark-ui/react/tabs'\nimport type { ReactNode } from 'react'\nimport { cx } from '@/lib/cx'\n\nexport const Tabs = ArkTabs.Root\n\ninterface PartProps {\n  readonly children: ReactNode\n  readonly className?: string\n}\n\n/* ark moves the mark between tabs, and the list is the frame it is placed against */\nexport const TabsList = ({ children, className }: PartProps) => (\n  <ArkTabs.List className={cx('relative flex items-center border-b border-reed', className)}>\n    {children}\n    <ArkTabs.Indicator className=\"selvedge selvedge-bottom selvedge-on tab-mark bottom-0 h-0.5 w-(--width)\" />\n  </ArkTabs.List>\n)\n\ninterface TabsTabProps extends PartProps {\n  readonly value: string\n  readonly disabled?: boolean\n}\n\nexport const TabsTab = ({ value, disabled, children, className }: TabsTabProps) => (\n  <ArkTabs.Trigger\n    value={value}\n    disabled={disabled}\n    className={cx(\n      'h-(--ctl-h) cursor-pointer px-(--cell-x) text-weft-dim transition-colors duration-(--dur-instant) ease-(--ease-beat) hover:bg-shed hover:text-weft data-selected:text-weft disabled:cursor-not-allowed disabled:opacity-50',\n      className,\n    )}\n  >\n    {children}\n  </ArkTabs.Trigger>\n)\n\ninterface TabsPanelProps extends PartProps {\n  readonly value: string\n}\n\nexport const TabsPanel = ({ value, children, className }: TabsPanelProps) => (\n  <ArkTabs.Content value={value} className={cx('py-(--stack) focus:outline-none', className)}>\n    {children}\n  </ArkTabs.Content>\n)\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/0.11.0/tabs.json",
        "files": [
          {
            "path": "components/ui/tabs/Tabs.tsx",
            "hash": "sha256-A/oKlo9cXHOryZ6k13PtQ8U9JIOudDxJyfD+KrkeTFA=",
            "client": true
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "toast",
      "type": "registry:ui",
      "title": "Toast",
      "dependencies": [
        "@ark-ui/react@^5.38.1"
      ],
      "registryDependencies": [
        "cx",
        "icons",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/toast/Toast.tsx",
          "target": "components/ui/toast/Toast.tsx",
          "type": "registry:ui",
          "content": "import { Toast as ArkToast, Toaster as ArkToaster, type CreateToasterReturn } from '@ark-ui/react/toast'\nimport { CloseIcon } from '@/components/ui/icons/Icons'\nimport { cx } from '@/lib/cx'\n\nexport { createToaster } from '@ark-ui/react/toast'\nexport type { CreateToasterReturn }\n\n/* zag names these five types */\nconst TONE: Record<string, string> = {\n  success: 'text-jade',\n  error: 'text-madder',\n  warning: 'text-weld',\n  info: 'text-indigo',\n  loading: 'text-weft-dim',\n}\n\ninterface ToasterProps {\n  readonly toaster: CreateToasterReturn\n  readonly className?: string\n}\n\n/* zag gives the group the highest rank in the document. no token ranks it here. */\nexport const Toaster = ({ toaster, className }: ToasterProps) => (\n  <ArkToaster toaster={toaster}>\n    {(toast) => (\n      <ArkToast.Root className={cx('toast layer flex w-[min(22rem,90vw)] items-start gap-2 p-(--cell-x)', className)}>\n        <div className=\"flex min-w-0 flex-1 flex-col gap-0.5\">\n          <div className={cx('flex items-center gap-1.5', TONE[toast.type ?? 'info'])}>\n            <span className={cx('size-1.25 shrink-0 rounded-full bg-current', toast.type === 'loading' && 'beat')} />\n            <ArkToast.Title className=\"truncate text-weft\">{toast.title}</ArkToast.Title>\n          </div>\n          {toast.description && (\n            <ArkToast.Description className=\"pl-2.75 text-weft-dim\">{toast.description}</ArkToast.Description>\n          )}\n        </div>\n        <ArkToast.CloseTrigger\n          aria-label=\"Dismiss\"\n          className=\"cursor-pointer p-0.5 text-weft-faint transition-colors duration-(--dur-instant) ease-(--ease-beat) hover:text-weft\"\n        >\n          <CloseIcon className=\"size-3\" />\n        </ArkToast.CloseTrigger>\n      </ArkToast.Root>\n    )}\n  </ArkToaster>\n)\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/0.11.0/toast.json",
        "files": [
          {
            "path": "components/ui/toast/Toast.tsx",
            "hash": "sha256-pQV9K43M3v3NqflQEF/Pr9tOy1SL1DTAlI+2rGxRI8Y=",
            "client": true
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "tooltip",
      "type": "registry:ui",
      "title": "Tooltip",
      "dependencies": [
        "@ark-ui/react@^5.38.1"
      ],
      "registryDependencies": [
        "cx",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/tooltip/Tooltip.tsx",
          "target": "components/ui/tooltip/Tooltip.tsx",
          "type": "registry:ui",
          "content": "import { Tooltip as ArkTooltip } from '@ark-ui/react/tooltip'\nimport { Portal } from '@ark-ui/react/portal'\nimport type { ReactElement, ReactNode } from 'react'\nimport { cx } from '@/lib/cx'\n\ninterface TooltipProps {\n  readonly content: ReactNode\n  /* asChild: the trigger becomes this element */\n  readonly children: ReactElement\n  readonly openDelay?: number\n  readonly closeDelay?: number\n  readonly className?: string\n}\n\n/* faster than the ark default, for an interface with many small questions */\nexport const Tooltip = ({ content, children, openDelay = 200, closeDelay = 80, className }: TooltipProps) => (\n  <ArkTooltip.Root openDelay={openDelay} closeDelay={closeDelay}>\n    <ArkTooltip.Trigger asChild>{children}</ArkTooltip.Trigger>\n    <Portal>\n      <ArkTooltip.Positioner>\n        <ArkTooltip.Content\n          className={cx(\n            'layer max-w-64 px-2 py-1 text-weft data-[state=open]:animate-[fade_var(--dur-instant)_var(--ease-beat)]',\n            className,\n          )}\n        >\n          {content}\n        </ArkTooltip.Content>\n      </ArkTooltip.Positioner>\n    </Portal>\n  </ArkTooltip.Root>\n)\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/0.11.0/tooltip.json",
        "files": [
          {
            "path": "components/ui/tooltip/Tooltip.tsx",
            "hash": "sha256-r6s5FyUl7r+CXujAMPuhbs3F8zQ6UzVJmPVa2tyq0qA=",
            "client": true
          }
        ]
      }
    }
  ],
  "vue": [
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "tokens",
      "type": "registry:style",
      "title": "Tokens",
      "dependencies": [],
      "registryDependencies": [],
      "files": [
        {
          "path": "styles/tokens.css",
          "target": "styles/tokens.css",
          "type": "registry:file",
          "content": "@custom-variant dense (&:where(:root[data-density='dense'] *));\n\n/* every name comes from the loom. the semantic colours are dyes. */\n@theme {\n  --font-ui: 'Archivo', ui-sans-serif, system-ui, sans-serif;\n  --font-data: 'IBM Plex Mono', ui-monospace, SFMono-Regular, monospace;\n\n  --color-ground: var(--p-ground);\n  --color-raised: var(--p-raised);\n  --color-sunken: var(--p-sunken);\n  --color-shed: var(--p-shed);\n  --color-reed: var(--p-reed);\n  --color-reed-lit: var(--p-reed-lit);\n\n  --color-weft: var(--p-weft);\n  --color-weft-dim: var(--p-weft-dim);\n  --color-weft-faint: var(--p-weft-faint);\n\n  --color-indigo: var(--p-indigo);\n  --color-indigo-wash: var(--p-indigo-wash);\n  --color-jade: var(--p-jade);\n  --color-madder: var(--p-madder);\n  --color-weld: var(--p-weld);\n  --color-logwood: var(--p-logwood);\n\n  /* one pick is one pass of the weft, and the motif carries five of them */\n  --color-pick-1: var(--p-indigo);\n  --color-pick-2: var(--p-jade);\n  --color-pick-3: var(--p-weld);\n  --color-pick-4: var(--p-madder);\n  --color-pick-5: var(--p-logwood);\n}\n\n/* the shed is the opening a loom makes and closes: the row under the pointer */\n:root {\n  --p-ground: #0b0d14;\n  --p-raised: #12151f;\n  --p-sunken: #070910;\n  --p-shed: #191d29;\n  --p-reed: #1c2030;\n  --p-reed-lit: #2b3145;\n\n  --p-weft: #e9e5db;\n  --p-weft-dim: #9b9789;\n  --p-weft-faint: #6a6759;\n\n  --p-indigo: #5f72ef;\n  --p-indigo-wash: #1a1f42;\n  --p-jade: #4fa88b;\n  --p-madder: #d2624f;\n  --p-weld: #c9a227;\n  --p-logwood: #9b6bd6;\n\n  --shadow-layer: 0 18px 44px -16px rgb(0 0 0 / 0.62);\n}\n\n:root[data-theme='light'] {\n  --p-ground: #f2f1ec;\n  --p-raised: #faf9f5;\n  --p-sunken: #e8e6df;\n  --p-shed: #f1efe8;\n  --p-reed: #dcd9d0;\n  --p-reed-lit: #c2beb2;\n\n  --p-weft: #14161d;\n  --p-weft-dim: #5c594f;\n  --p-weft-faint: #8a8779;\n\n  --p-indigo: #3a49c4;\n  --p-indigo-wash: #e4e5f7;\n  --p-jade: #2f7a61;\n  --p-madder: #b04430;\n  --p-weld: #8a6f12;\n  --p-logwood: #7a3f9e;\n\n  --shadow-layer: 0 18px 44px -16px rgb(26 24 20 / 0.24);\n}\n\n/*\n * a control reads these and defines none of its own, so one attribute on the root\n * retunes the whole interface.\n */\n:root,\n:root[data-density='compact'] {\n  --row-h: 32px;\n  --cell-x: 10px;\n  --ui-text: 13px;\n  --ctl-h: 28px;\n  --stack: 8px;\n  --reed-pitch: 5px;\n}\n\n:root[data-density='comfortable'] {\n  --row-h: 40px;\n  --cell-x: 14px;\n  --ui-text: 14px;\n  --ctl-h: 34px;\n  --stack: 10px;\n  --reed-pitch: 6px;\n}\n\n:root[data-density='dense'] {\n  --row-h: 25px;\n  --cell-x: 7px;\n  --ui-text: 12px;\n  --ctl-h: 23px;\n  --stack: 6px;\n  --reed-pitch: 4px;\n}\n\n/*\n * a duration is named for what it moves. every layer that leaves the flow declares\n * its rank here: a sticky header makes a stacking context and paints over any layer\n * with no rank.\n */\n:root {\n  --reed-tick: 1px;\n  --ctl-box: 13px;\n\n  /*\n   * vertical-align: middle centres on the x-height. every value here is digits and\n   * capitals, so the band runs baseline to cap height. this is half the difference,\n   * for the data face.\n   */\n  --ctl-lift: 0.091em;\n\n  /* the advance of the data face. a column width can then be a character count. */\n  --data-adv: 0.6em;\n\n  --z-grip: 1;\n  --z-pinned: 5;\n  --z-sticky: 10;\n  /* the panel keeps the page usable: a popover opened inside it paints above it */\n  --z-panel: 20;\n  --z-popover: 30;\n  --z-backdrop: 40;\n  --z-modal: 50;\n\n  --dur-instant: 90ms;\n  --dur-local: 160ms;\n  --dur-overlay: 240ms;\n\n  --ease-beat: cubic-bezier(0.2, 0.9, 0.24, 1);\n  --ease-glide: cubic-bezier(0.33, 0, 0.15, 1);\n  --ease-exit: cubic-bezier(0.5, 0, 0.9, 0.4);\n}\n\n@media (prefers-reduced-motion: reduce) {\n  :root {\n    --dur-instant: 0ms;\n    --dur-local: 0ms;\n    --dur-overlay: 0ms;\n  }\n}\n\n@layer base {\n  body {\n    background-color: var(--color-ground);\n    color: var(--color-weft);\n    font-family: var(--font-ui);\n    font-size: var(--ui-text);\n    -webkit-font-smoothing: antialiased;\n  }\n\n  /* the second selector: a control whose focus lands on a child, like a field with an icon */\n  :focus-visible,\n  .focus-ring:has(:focus-visible) {\n    outline: 2px solid var(--color-indigo);\n    outline-offset: 1px;\n  }\n\n  ::selection {\n    background-color: var(--color-indigo);\n    color: #fff;\n  }\n}\n\n@utility tnum {\n  font-variant-numeric: tabular-nums;\n}\n\n/* a utility, for a state variant to dye a part that ark controls */\n@utility selvedge-on {\n  --selvedge: var(--color-indigo);\n}\n\n/*\n * ark positions its floating parts with an inline `z-index: var(--z-index)` beside\n * an inline `--z-index: auto`. an inline custom property outranks a stylesheet, so\n * this has to be important to reach it. parts ark does not position take a class.\n */\n[data-part='positioner']:not([data-scope='dialog']) {\n  --z-index: var(--z-popover) !important;\n}\n\n@layer components {\n  /* the reed: the evenly spaced slots that hold the warp on a loom */\n  .reed-edge {\n    position: relative;\n  }\n\n  .reed-edge::after {\n    content: '';\n    position: absolute;\n    inset-inline: 0;\n    bottom: 0;\n    height: 2px;\n    background-image: repeating-linear-gradient(\n      90deg,\n      var(--color-reed-lit) 0 var(--reed-tick),\n      transparent var(--reed-tick) var(--reed-pitch)\n    );\n  }\n\n  /*\n   * the same pitch turned a quarter. a cell takes the leading line and a pinned cell\n   * takes the trailing one: the next cell's leading line travels away under it.\n   */\n  .warp-line,\n  .warp-line-end {\n    position: relative;\n  }\n\n  .warp-line::before,\n  .warp-line-end::before {\n    content: '';\n    position: absolute;\n    inset-block: 0;\n    width: 1px;\n    background-image: repeating-linear-gradient(\n      0deg,\n      var(--color-reed) 0 var(--reed-tick),\n      transparent var(--reed-tick) var(--reed-pitch)\n    );\n  }\n\n  /* under the digits, never on the divider: a divider cannot name which value is low */\n  .reed-under {\n    background-image: repeating-linear-gradient(\n      90deg,\n      var(--color-madder) 0 var(--reed-tick),\n      transparent var(--reed-tick) var(--reed-pitch)\n    );\n    background-position: bottom;\n    background-size: 100% 1px;\n    background-repeat: no-repeat;\n  }\n\n  /* the selvedge is the finished edge of a cloth. it marks a choice; the shed marks a position. */\n  .selvedge {\n    position: relative;\n    --selvedge: transparent;\n  }\n\n  .selvedge::after {\n    content: '';\n    position: absolute;\n    inset-block: 0;\n    inset-inline-start: 0;\n    width: 2px;\n    background-image: repeating-linear-gradient(\n      0deg,\n      var(--selvedge) 0 var(--reed-tick),\n      transparent var(--reed-tick) var(--reed-pitch)\n    );\n  }\n\n  /* the same edge turned a quarter, for a list of choices that runs across */\n  .selvedge-bottom::after {\n    inset-block: auto 0;\n    inset-inline: 0;\n    width: auto;\n    height: 2px;\n    background-image: repeating-linear-gradient(\n      90deg,\n      var(--selvedge) 0 var(--reed-tick),\n      transparent var(--reed-tick) var(--reed-pitch)\n    );\n  }\n\n  /*\n   * a row cannot wear the base outline. its own cells paint over it, and a pinned cell\n   * covers what is left, so all that survives is a fragment of the bottom edge. the ring\n   * is drawn inside the cells instead, which is the one place a pinned cell cannot hide.\n   */\n  .focus-row:focus-visible {\n    outline: none;\n  }\n\n  .focus-row:focus-visible > td {\n    box-shadow:\n      inset 0 2px 0 var(--color-indigo),\n      inset 0 -2px 0 var(--color-indigo);\n  }\n\n  /* zag places the mark and reads these two names for the slide from tab to tab */\n  .tab-mark {\n    --transition-duration: var(--dur-local);\n    --transition-timing-function: var(--ease-beat);\n  }\n\n  /* every surface that leaves the flow draws the same edge and the same shadow */\n  .layer {\n    border: 1px solid var(--color-reed-lit);\n    background-color: var(--color-raised);\n    box-shadow: var(--shadow-layer);\n  }\n\n  /*\n   * zag gives a toast its offset, its height and its opacity as custom properties,\n   * and the transition between two of its positions belongs to the stylesheet.\n   */\n  .toast {\n    translate: var(--x) var(--y);\n    height: var(--height, auto);\n    opacity: var(--opacity);\n    transition:\n      translate var(--dur-overlay) var(--ease-beat),\n      height var(--dur-overlay) var(--ease-beat),\n      opacity var(--dur-overlay) var(--ease-beat);\n    will-change: translate, opacity;\n  }\n\n  /*\n   * three ticks, and the order of the heights is the direction. tighter than the\n   * pitch, because a head reserves this width and a narrow column cannot pay a full one.\n   */\n  .reed-sort {\n    display: inline-block;\n    width: 7px;\n    height: 7px;\n    background-image:\n      linear-gradient(currentColor, currentColor), linear-gradient(currentColor, currentColor),\n      linear-gradient(currentColor, currentColor);\n    background-size: var(--reed-tick) 3px, var(--reed-tick) 5px, var(--reed-tick) 7px;\n    background-position: left bottom, center bottom, right bottom;\n    background-repeat: no-repeat;\n  }\n\n  .reed-sort-down {\n    background-size: var(--reed-tick) 7px, var(--reed-tick) 5px, var(--reed-tick) 3px;\n  }\n\n  /* a control beside a row of values aligns on the cap band */\n  .ctl-align {\n    vertical-align: middle;\n    translate: 0 calc(-1 * var(--ctl-lift));\n  }\n\n  /* the surface that every control shares */\n  .ctl {\n    display: inline-flex;\n    align-items: center;\n    gap: 0.375rem;\n    border: 1px solid var(--color-reed);\n    background-color: var(--color-ground);\n    padding-inline: var(--cell-x);\n    color: var(--color-weft);\n    transition: border-color var(--dur-instant) var(--ease-beat);\n  }\n\n  .ctl:hover {\n    border-color: var(--color-reed-lit);\n  }\n\n  /* an input or a textarea, which draws the control surface across a whole line */\n  .ctl.ctl-entry {\n    display: block;\n    width: 100%;\n  }\n\n  .ctl-entry[data-invalid] {\n    border-color: var(--color-madder);\n  }\n\n  .ctl-entry:disabled {\n    cursor: not-allowed;\n    opacity: 0.5;\n  }\n\n  /* one tick of the reed stands where a value is absent */\n  .reed-mark {\n    display: inline-block;\n    width: var(--reed-tick);\n    height: var(--reed-pitch);\n    background-color: var(--color-reed-lit);\n    vertical-align: middle;\n  }\n\n  .warp-line::before {\n    left: 0;\n  }\n\n  .warp-line-end::before {\n    right: 0;\n  }\n\n  /*\n   * the grip straddles the column boundary, and the reed inside it is the divider the\n   * head would draw anyway. currentColor, because a gradient cannot transition.\n   */\n  .reed-grip {\n    position: absolute;\n    inset-block: 0;\n    right: -4px;\n    z-index: var(--z-grip);\n    width: 8px;\n    color: var(--color-reed-lit);\n    cursor: col-resize;\n    touch-action: none;\n    transition: color var(--dur-instant) var(--ease-beat);\n  }\n\n  .reed-grip::before {\n    content: '';\n    position: absolute;\n    inset-block: 0;\n    left: 4px;\n    width: 1px;\n    background-image: repeating-linear-gradient(\n      0deg,\n      currentColor 0 var(--reed-tick),\n      transparent var(--reed-tick) var(--reed-pitch)\n    );\n  }\n\n  .reed-grip:hover,\n  .reed-grip:focus-visible,\n  .reed-grip[data-dragging] {\n    color: var(--color-indigo);\n  }\n\n  /* an unwoven warp: the threads sit in the reed and no weft has arrived */\n  .reed-warp {\n    background-image: repeating-linear-gradient(\n      90deg,\n      var(--color-reed) 0 var(--reed-tick),\n      transparent var(--reed-tick) var(--reed-pitch)\n    );\n  }\n\n  /* only the loading state moves */\n  .reed-warp-beat {\n    animation: beat 1.2s var(--ease-glide) infinite;\n  }\n\n  /*\n   * the cover layer is attached to the content, so it arrives at the edge and hides\n   * the reed at the end of the list. a short list needs no cover: the scrollable area\n   * is then the padding box and the two layers land together.\n   */\n  .reed-scroll {\n    background-image:\n      linear-gradient(var(--color-raised), var(--color-raised)),\n      repeating-linear-gradient(\n        90deg,\n        var(--color-reed-lit) 0 var(--reed-tick),\n        transparent var(--reed-tick) var(--reed-pitch)\n      );\n    background-position: bottom;\n    background-size: 100% 2px;\n    background-repeat: no-repeat;\n    background-attachment: local, scroll;\n    scrollbar-width: thin;\n    scrollbar-color: var(--color-reed-lit) transparent;\n  }\n\n  /* the standard properties above win where supported, so these reach safari only */\n  .reed-scroll::-webkit-scrollbar {\n    width: 8px;\n    height: 8px;\n  }\n\n  .reed-scroll::-webkit-scrollbar-track {\n    background-color: transparent;\n  }\n\n  .reed-scroll::-webkit-scrollbar-thumb {\n    background-color: var(--color-reed-lit);\n  }\n\n  .reed-scroll::-webkit-scrollbar-thumb:hover {\n    background-color: var(--color-indigo);\n  }\n\n  .beat {\n    animation: beat 1.6s var(--ease-glide) infinite;\n  }\n\n  /*\n   * plot writes font-family, font-size and fill on the svg as presentation\n   * attributes, and any stylesheet rule outranks those. it names every axis part\n   * with an aria-label, which is the only stable handle on the generated markup.\n   */\n  .sley-plot {\n    color: var(--color-weft-dim);\n    font-family: var(--font-data);\n    font-size: var(--ui-text);\n    /*\n     * the halo behind a crosshair readout and the fill of a tip. plot declares it\n     * white from a <style> inside the svg, and an unlayered rule outranks a layered\n     * one whatever the specificity says, so this one has to shout.\n     */\n    --plot-background: var(--color-raised) !important;\n  }\n\n  .sley-plot [aria-label$='grid'] {\n    color: var(--color-reed);\n    stroke-opacity: 1;\n  }\n\n  .sley-plot [aria-label$='axis tick'] {\n    color: var(--color-reed-lit);\n  }\n\n  .sley-plot [aria-label$='tick label'] {\n    color: var(--color-weft-dim);\n  }\n\n  /* an axis label is a human phrase, so it leaves the data face */\n  .sley-plot [aria-label$='axis label'] {\n    color: var(--color-weft-dim);\n    font-family: var(--font-ui);\n  }\n\n  .sley-plot [aria-label='crosshair rule'] {\n    color: var(--color-reed-lit);\n    stroke-opacity: 1;\n  }\n\n  .sley-plot [aria-label='crosshair text'] {\n    color: var(--color-weft);\n  }\n\n  /*\n   * a readout stands where a tick label already is, and the halo behind it outlines\n   * each glyph rather than covering a line, so the tick reads through the gaps. the\n   * readout carries the exact value, so the axis stands down while it is showing. plot\n   * renders the readout only while the pointer is in the plot, which is the condition.\n   */\n  .sley-plot:has([aria-label='crosshair text'] text) [aria-label$='tick label'] {\n    opacity: 0;\n    transition: opacity var(--dur-instant) var(--ease-beat);\n  }\n\n  /* one weft thread travelling across the cloth, and each series follows the one above it */\n  .sley-plot .reed-draw {\n    stroke-dasharray: var(--draw);\n    animation: draw var(--dur-overlay) var(--ease-beat) var(--draw-delay) both;\n  }\n\n  /* the cloth builds column by column, each bar standing up off the axis */\n  .sley-plot .reed-build {\n    transform-origin: 0 var(--build-base);\n    animation: build var(--dur-overlay) var(--ease-beat) var(--build-delay) both;\n  }\n\n  .reed-brush-field {\n    fill: transparent;\n    cursor: crosshair;\n  }\n\n  /*\n   * chrome rings a focusable svg element on a plain :focus, where it rings a button\n   * only on :focus-visible, so a pointer drag would leave its own outline behind.\n   */\n  .reed-brush-field:focus {\n    outline: none;\n  }\n\n  .reed-brush-field:focus-visible {\n    outline: 2px solid var(--color-indigo);\n    outline-offset: 1px;\n  }\n\n  /* a brush window is what the reader chose, so it takes the selvedge wash. it\n   * paints over the marks, so the fill carries an alpha rather than the opaque wash. */\n  .reed-brush {\n    fill: color-mix(in srgb, var(--color-indigo) 16%, transparent);\n    stroke: var(--color-indigo);\n    stroke-width: 1px;\n    pointer-events: none;\n  }\n}\n\n@keyframes beat {\n  0%,\n  100% {\n    opacity: 1;\n  }\n  50% {\n    opacity: 0.25;\n  }\n}\n\n@keyframes draw {\n  from {\n    stroke-dashoffset: var(--draw);\n  }\n}\n\n@keyframes build {\n  from {\n    transform: scaleY(0);\n  }\n}\n\n@keyframes fade {\n  from {\n    opacity: 0;\n  }\n}\n\n@keyframes fade-out {\n  to {\n    opacity: 0;\n  }\n}\n\n@keyframes rise {\n  from {\n    opacity: 0;\n    transform: translateY(-6px) scale(0.99);\n  }\n}\n\n@keyframes rise-out {\n  to {\n    opacity: 0;\n    transform: translateY(-6px) scale(0.99);\n  }\n}\n\n/* the panel arrives from the edge that holds it, which sets --slide-from */\n@keyframes slide {\n  from {\n    opacity: 0;\n    transform: translateX(var(--slide-from));\n  }\n}\n\n/* it leaves the way it came, and it leaves faster, on the exit curve */\n@keyframes slide-out {\n  to {\n    opacity: 0;\n    transform: translateX(var(--slide-from));\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .beat,\n  .reed-warp-beat {\n    animation: none;\n  }\n}\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/vue/0.11.0/tokens.json",
        "files": [
          {
            "path": "styles/tokens.css",
            "hash": "sha256-mG/bjjzBVdc7yT2tkOGlPsyiQCUIjnavDcEFqlwpXSg=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "cx",
      "type": "registry:lib",
      "title": "Cx",
      "dependencies": [
        "clsx@^2.1.1",
        "tailwind-merge@^3.6.0"
      ],
      "registryDependencies": [],
      "files": [
        {
          "path": "lib/cx.ts",
          "target": "lib/cx.ts",
          "type": "registry:lib",
          "content": "import { clsx, type ClassValue } from 'clsx'\nimport { twMerge } from 'tailwind-merge'\n\n/*\n * the stylesheet order decides between two utilities for one property, so a plain\n * join lets an override fail with no message.\n */\nexport function cx(...classes: ClassValue[]) {\n  return twMerge(clsx(classes))\n}\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/vue/0.11.0/cx.json",
        "files": [
          {
            "path": "lib/cx.ts",
            "hash": "sha256-nCgIrhJMREr99PRsI+he7LXaUaFBBdwZSWpHWqK/JBo=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "button",
      "type": "registry:ui",
      "title": "Button",
      "dependencies": [],
      "registryDependencies": [
        "cx",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/button/Button.vue",
          "target": "components/ui/button/Button.vue",
          "type": "registry:ui",
          "content": "<script lang=\"ts\">\nexport type ButtonVariant = 'default' | 'primary' | 'danger' | 'quiet'\n</script>\n\n<script setup lang=\"ts\">\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\n/* everything else, `disabled` and the listeners included, reaches the button as an attribute */\nconst props = withDefaults(\n  defineProps<{\n    variant?: ButtonVariant\n    type?: 'button' | 'submit' | 'reset'\n    class?: HTMLAttributes['class']\n  }>(),\n  { variant: 'default', type: 'button' },\n)\n\nconst VARIANT: Record<ButtonVariant, string> = {\n  default: 'ctl',\n  primary: 'border border-indigo bg-indigo text-ground hover:bg-indigo/90',\n  danger: 'border border-madder bg-madder text-ground hover:bg-madder/90',\n  quiet: 'text-weft-dim hover:text-weft',\n}\n</script>\n\n<template>\n  <button\n    :type=\"props.type\"\n    :class=\"\n      cx(\n        'inline-flex h-(--ctl-h) cursor-pointer items-center justify-center gap-1.5 px-(--cell-x) transition-colors duration-(--dur-instant) ease-(--ease-beat) disabled:cursor-not-allowed disabled:opacity-50',\n        VARIANT[props.variant],\n        props.class,\n      )\n    \"\n  >\n    <slot />\n  </button>\n</template>\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/vue/0.11.0/button.json",
        "files": [
          {
            "path": "components/ui/button/Button.vue",
            "hash": "sha256-jICcULhckOg5goU43LUaIyF1EikyFHOtXSgMjcOMAOc=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "chart",
      "type": "registry:ui",
      "title": "Chart",
      "dependencies": [
        "@observablehq/plot@^0.6.17"
      ],
      "registryDependencies": [
        "cx",
        "empty-state",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/chart/Chart.vue",
          "target": "components/ui/chart/Chart.vue",
          "type": "registry:ui",
          "content": "<script lang=\"ts\">\nexport type ChartOptions = Omit<Plot.PlotOptions, 'width' | 'height' | 'className'>\n</script>\n\n<script setup lang=\"ts\" generic=\"X = Date\">\nimport * as Plot from '@observablehq/plot'\nimport { computed, ref, watch, watchEffect, type HTMLAttributes } from 'vue'\nimport { attachBrush } from '@/components/ui/chart/brush'\nimport { drawBars, drawLines } from '@/components/ui/chart/motion'\nimport EmptyState from '@/components/ui/empty-state/EmptyState.vue'\nimport { cx } from '@/lib/cx'\n\nconst props = withDefaults(\n  defineProps<{\n    title: string\n    /* the unit belongs in the header, the way a column head carries it and a cell never does */\n    unit?: string\n    /* hold this stable, or every parent render tears the plot down and builds it again */\n    options: ChartOptions\n    height?: number\n    loading?: boolean\n    /* a chart cannot read the caller's marks, so an empty result is stated and never derived */\n    empty?: boolean\n    emptyMessage?: string\n    /* the window the chart paints. hold it in your own state, the way a table's selection works. */\n    brush?: readonly [X, X] | null\n    class?: HTMLAttributes['class']\n  }>(),\n  { height: 260, loading: false, empty: false, emptyMessage: 'No data in this range.', brush: null },\n)\n\n/*\n * a drag reports a range of the x scale, and an arrow key on the plot moves one edge of\n * one by a tick of that axis. a single click reports null, and so does escape.\n */\nconst emit = defineEmits<{ 'update:brush': [range: readonly [X, X] | null] }>()\n\n/* the stylesheet reaches the generated svg through this one name */\nconst PLOT_CLASS = 'sley-plot'\n\n/*\n * plot takes a margin as a number, so it cannot read a density token. these are\n * sized for the widest tick label at the comfortable text size, which leaves a\n * denser mode a little slack and spares every chart a re-render on the knob.\n */\nconst MARGIN = { marginTop: 16, marginRight: 16, marginBottom: 40, marginLeft: 56 }\n\n/* plot points a quantitative axis label with an arrow glyph, and no interface string here carries one */\nconst AXIS = { labelArrow: 'none' } as const\n\n/* the frame is measured rather than the host, because the host leaves while a state shows */\nconst frame = ref<HTMLDivElement | null>(null)\nconst host = ref<HTMLDivElement | null>(null)\nconst width = ref(0)\n\n/* plain holders rather than refs: the effect below would rebuild the plot on every brush move */\nlet windowHandle: { show: (range: readonly [X, X] | null) => void } | null = null\nlet brushed = props.brush\n\n/* the lines draw in once. a resize rebuilds the plot, and replaying it there reads as a glitch. */\nlet drawn = false\n\n/* a rebuild takes the focused field down with it, so a resize would strand the keyboard */\nlet held = false\n\n/* loading wins: a chart still waiting on a fetch has nothing to call empty yet */\nconst showEmpty = computed(() => props.empty && !props.loading)\nconst plotting = computed(() => !props.loading && !showEmpty.value)\n\nwatch(\n  () => props.brush,\n  (range) => {\n    brushed = range\n    windowHandle?.show(range)\n  },\n)\n\nwatchEffect((onCleanup) => {\n  const node = frame.value\n  if (!node) return\n\n  const observer = new ResizeObserver(([entry]) => {\n    width.value = Math.floor(entry.contentRect.width)\n  })\n  observer.observe(node)\n  onCleanup(() => observer.disconnect())\n})\n\nwatchEffect(\n  (onCleanup) => {\n    const node = host.value\n    if (!node || width.value === 0) return\n\n    const margins = { ...MARGIN, ...props.options }\n    const plot = Plot.plot({\n      ...margins,\n      ariaLabel: props.title,\n      x: { ...AXIS, ...props.options.x },\n      y: { ...AXIS, ...props.options.y },\n      width: width.value,\n      height: props.height,\n      className: PLOT_CLASS,\n    })\n    node.append(plot)\n\n    const svg = plot instanceof SVGSVGElement ? plot : plot.querySelector('svg')\n    if (svg && !drawn) {\n      drawn = true\n      drawLines(svg)\n      drawBars(svg)\n    }\n\n    const scale = plot.scale('x')\n    if (!svg || !scale?.invert) {\n      onCleanup(() => plot.remove())\n      return\n    }\n\n    /* plot types every scale value as any, so both readings are named here once */\n    const toValue: (px: number) => X = scale.invert.bind(scale)\n    const toPixel: (value: X) => number = scale.apply.bind(scale)\n\n    const handle = attachBrush(\n      svg,\n      {\n        left: margins.marginLeft,\n        right: width.value - margins.marginRight,\n        top: margins.marginTop,\n        bottom: props.height - margins.marginBottom,\n      },\n      { toValue, toPixel },\n      (range) => emit('update:brush', range),\n    )\n    handle.show(brushed)\n    if (held) handle.restore()\n    windowHandle = handle\n\n    onCleanup(() => {\n      held = handle.focused()\n      windowHandle = null\n      handle.destroy()\n      plot.remove()\n    })\n  },\n  { flush: 'post' },\n)\n</script>\n\n<template>\n  <section :class=\"cx('@container border border-reed bg-raised', props.class)\">\n    <header class=\"flex items-center justify-between gap-(--stack) border-b border-reed px-(--cell-x) py-(--stack)\">\n      <h2 class=\"inline-flex items-baseline gap-1.5 font-medium\">\n        <span>{{ props.title }}</span>\n        <span v-if=\"props.unit\" class=\"font-data font-normal text-weft-faint\">{{ props.unit }}</span>\n      </h2>\n      <div v-if=\"$slots.actions\" class=\"flex items-center gap-(--stack)\">\n        <slot name=\"actions\" />\n      </div>\n    </header>\n\n    <div ref=\"frame\" :aria-busy=\"props.loading\" class=\"px-(--cell-x) py-(--stack)\">\n      <div v-if=\"plotting\" ref=\"host\" />\n      <div v-if=\"props.loading\" class=\"reed-warp reed-warp-beat\" :style=\"{ height: `${props.height}px` }\" />\n      <div v-if=\"showEmpty\" :style=\"{ height: `${props.height}px` }\">\n        <EmptyState :title=\"props.emptyMessage\" class=\"h-full\" />\n      </div>\n    </div>\n  </section>\n</template>\n"
        },
        {
          "path": "components/ui/chart/brush.ts",
          "target": "components/ui/chart/brush.ts",
          "type": "registry:ui",
          "content": "const SVG_NS = 'http://www.w3.org/2000/svg'\n\n/* a click that travels less than this is a click, and a click clears the window */\nconst MIN_DRAG = 3\n\n/* the resize grip moves 8px a press, and a step under that reads as nothing happening */\nconst MIN_STEP = 8\n\nconst KEYS = new Set(['ArrowLeft', 'ArrowRight', 'Home', 'End', 'Escape'])\n\n/* the field is the only surface that can announce the keys, so its name carries them */\nconst HOW = 'Move an edge with the arrow keys. Escape clears.'\n\nexport interface BrushFrame {\n  readonly left: number\n  readonly right: number\n  readonly top: number\n  readonly bottom: number\n}\n\n/*\n * plot re-renders a whole svg on every change, so the brush layer is appended to the\n * node it belongs to and dies with it. the caller owns no listener and no cleanup\n * beyond the teardown returned here.\n */\nexport interface BrushScale<T> {\n  readonly toValue: (px: number) => T\n  readonly toPixel: (value: T) => number\n}\n\n/* one press is one tick of the x axis: a distance the scale already draws for the reader */\nfunction stepOf(svg: SVGSVGElement, frame: BrushFrame) {\n  const ticks = svg.querySelectorAll<SVGGraphicsElement>('[aria-label=\"x-axis tick\"] path')\n  const xs = Array.from(ticks, (tick) => tick.transform.baseVal.consolidate()?.matrix.e ?? 0).sort(\n    (a, b) => a - b,\n  )\n  const gaps = xs.slice(1).map((x, index) => x - xs[index])\n  if (gaps.length === 0) return (frame.right - frame.left) / 20\n\n  gaps.sort((a, b) => a - b)\n  return Math.max(MIN_STEP, gaps[Math.floor(gaps.length / 2)])\n}\n\nexport function attachBrush<T>(\n  svg: SVGSVGElement,\n  frame: BrushFrame,\n  scale: BrushScale<T>,\n  report: (range: readonly [T, T] | null) => void,\n) {\n  const height = frame.bottom - frame.top\n  const step = stepOf(svg, frame)\n  const named = svg.getAttribute('aria-label')\n\n  const field = document.createElementNS(SVG_NS, 'rect')\n  field.setAttribute('class', 'reed-brush-field')\n  field.setAttribute('x', String(frame.left))\n  field.setAttribute('y', String(frame.top))\n  field.setAttribute('width', String(frame.right - frame.left))\n  field.setAttribute('height', String(height))\n  field.setAttribute('tabindex', '0')\n  field.setAttribute('aria-label', named ? `Select a range on ${named}. ${HOW}` : `Select a range. ${HOW}`)\n\n  const selection = document.createElementNS(SVG_NS, 'rect')\n  selection.setAttribute('class', 'reed-brush')\n  selection.setAttribute('aria-hidden', 'true')\n  selection.setAttribute('y', String(frame.top))\n  selection.setAttribute('height', String(height))\n  selection.setAttribute('width', '0')\n\n  /*\n   * the window paints over the marks and tints them through its own alpha. under\n   * them it is invisible against a filled mark, which a bar chart is made of.\n   */\n  svg.append(selection)\n  svg.append(field)\n\n  let anchor = 0\n  let edge = 0\n  let dragging = false\n  let live = false\n\n  /* raised for the one paint that answers a report, and read by show below */\n  let reported = false\n\n  const xOf = (event: PointerEvent) => clampX(event.clientX - svg.getBoundingClientRect().left)\n\n  const clampX = (value: number) => Math.max(frame.left, Math.min(frame.right, value))\n\n  const paint = (from: number, to: number) => {\n    selection.setAttribute('x', String(Math.min(from, to)))\n    selection.setAttribute('width', String(Math.abs(to - from)))\n  }\n\n  const clear = () => {\n    live = false\n    reported = false\n    paint(0, 0)\n  }\n\n  const onDown = (event: PointerEvent) => {\n    dragging = true\n    anchor = xOf(event)\n    edge = anchor\n    paint(anchor, anchor)\n    field.setPointerCapture(event.pointerId)\n  }\n\n  const onMove = (event: PointerEvent) => {\n    if (!dragging) return\n    edge = xOf(event)\n    paint(anchor, edge)\n  }\n\n  const onRelease = () => {\n    if (!dragging) return\n    dragging = false\n\n    if (Math.abs(edge - anchor) < MIN_DRAG) {\n      clear()\n      report(null)\n      return\n    }\n    live = true\n    reported = true\n    const from = Math.min(anchor, edge)\n    const to = Math.max(anchor, edge)\n    report([scale.toValue(from), scale.toValue(to)])\n  }\n\n  /*\n   * a drag anchors one edge and carries the other, and the keys do the same thing one\n   * step at a time. the first press has nothing to anchor, so it takes the edge of the\n   * frame it moves away from.\n   */\n  const moveEdge = (to: number) => {\n    edge = clampX(to)\n    live = true\n    reported = true\n    paint(anchor, edge)\n    report([scale.toValue(Math.min(anchor, edge)), scale.toValue(Math.max(anchor, edge))])\n  }\n\n  const onKey = (event: KeyboardEvent) => {\n    if (!KEYS.has(event.key)) return\n    event.preventDefault()\n\n    if (event.key === 'Escape') {\n      if (!live) return\n      clear()\n      report(null)\n      return\n    }\n\n    const back = event.key === 'ArrowLeft' || event.key === 'Home'\n    if (!live) {\n      anchor = back ? frame.right : frame.left\n      edge = anchor\n    }\n\n    if (event.key === 'Home') moveEdge(frame.left)\n    else if (event.key === 'End') moveEdge(frame.right)\n    else moveEdge(edge + (back ? -step : step))\n  }\n\n  field.addEventListener('pointerdown', onDown)\n  field.addEventListener('pointermove', onMove)\n  field.addEventListener('lostpointercapture', onRelease)\n  field.addEventListener('keydown', onKey)\n\n  return {\n    /*\n     * the caller owns the range, so a re-render paints the window it still holds\n     * rather than dropping it and leaving the two out of step.\n     */\n    show(range: readonly [T, T] | null) {\n      if (!range) {\n        clear()\n        return\n      }\n      const from = clampX(scale.toPixel(range[0]))\n      const to = clampX(scale.toPixel(range[1]))\n      live = true\n      paint(from, to)\n\n      /*\n       * a caller may widen what it was given, and the demo here rounds a range out to whole\n       * days. taking the moving edge from that value would add the widening to the next key\n       * press on top of the step, so the paint that answers a report moves no edge.\n       */\n      if (reported) {\n        reported = false\n        return\n      }\n\n      /* the moving edge stays the one the reader is moving, or the next key press reverses */\n      const forward = edge >= anchor\n      anchor = forward ? from : to\n      edge = forward ? to : from\n    },\n    focused() {\n      return document.activeElement === field\n    },\n    /* a rebuild draws a new field, and the reader is still on the old one */\n    restore() {\n      field.focus({ preventScroll: true })\n    },\n    destroy() {\n      field.removeEventListener('pointerdown', onDown)\n      field.removeEventListener('pointermove', onMove)\n      field.removeEventListener('lostpointercapture', onRelease)\n      field.removeEventListener('keydown', onKey)\n      selection.remove()\n      field.remove()\n    },\n  }\n}\n"
        },
        {
          "path": "components/ui/chart/crosshair.ts",
          "target": "components/ui/chart/crosshair.ts",
          "type": "registry:ui",
          "content": "import * as Plot from '@observablehq/plot'\n\nexport interface CrosshairXOptions<T> {\n  readonly x: Plot.ChannelValue\n  readonly y?: Plot.ChannelValue\n  readonly formatX?: (row: T) => string\n  readonly formatY?: (row: T) => string\n  readonly maxRadius?: number\n}\n\nconst RULE = { inset: -6, stroke: 'currentColor', strokeOpacity: 0.2 } as const\n\n/* the halo outlines each glyph against the surface the chart is drawn on */\nconst READOUT = { fill: 'currentColor', stroke: 'var(--plot-background)', strokeWidth: 5 } as const\n\n/* plot names the group of rendered elements from a field the class type does not declare */\nconst named = <M extends object>(mark: M, ariaLabel: string) => Object.assign(mark, { ariaLabel })\n\n/*\n * plot's own crosshair derives each readout from its source channel through an\n * initializer, and an initializer's channels are merged over the declared ones, so a\n * text the caller passes is discarded and a date reads as a full iso stamp. the pairs\n * are composed here from the same public pointer transform instead.\n */\nexport function crosshairX<T>(data: readonly T[], options: CrosshairXOptions<T>) {\n  const { x, y, formatX, formatY, maxRadius } = options\n  const point = { px: x, py: y, maxRadius }\n\n  const acrossRule = named(Plot.ruleX(data, Plot.pointerX({ ...point, x, ...RULE })), 'crosshair rule')\n\n  const acrossText = named(\n    Plot.text(\n      data,\n      Plot.pointerX({ ...point, x, text: formatX ?? x, dy: 9, frameAnchor: 'bottom', lineAnchor: 'top', ...READOUT }),\n    ),\n    'crosshair text',\n  )\n\n  if (y === undefined) return Plot.marks(acrossRule, acrossText)\n\n  const downRule = named(Plot.ruleY(data, Plot.pointerX({ ...point, y, ...RULE })), 'crosshair rule')\n\n  const downText = named(\n    Plot.text(\n      data,\n      Plot.pointerX({ ...point, y, text: formatY ?? y, dx: -9, frameAnchor: 'left', textAnchor: 'end', ...READOUT }),\n    ),\n    'crosshair text',\n  )\n\n  /* both rules go under both readouts, or the horizontal rule crosses the label at the foot */\n  return Plot.marks(acrossRule, downRule, acrossText, downText)\n}\n"
        },
        {
          "path": "components/ui/chart/downsample.ts",
          "target": "components/ui/chart/downsample.ts",
          "type": "registry:ui",
          "content": "/*\n * largest triangle three buckets. a line with more points than the frame has pixels\n * spends layout time on detail nobody can resolve, and taking every nth point flattens\n * a spike that falls in the gap. this keeps whichever point in a bucket makes the widest\n * triangle with the point already kept and the bucket ahead, which follows the shape of the\n * line. the extremes are not a guarantee: where a bucket boundary splits a peak, the peak\n * can lose to its own shoulder.\n */\nexport function downsample<T>(\n  rows: readonly T[],\n  x: (row: T) => number,\n  y: (row: T) => number,\n  target: number,\n): readonly T[] {\n  if (rows.length <= target || target < 3) return rows\n\n  const last = rows.length - 1\n  const every = (rows.length - 2) / (target - 2)\n  const kept: T[] = [rows[0]]\n  let anchor = 0\n\n  for (let bucket = 0; bucket < target - 2; bucket++) {\n    const start = Math.floor(bucket * every) + 1\n    const end = Math.floor((bucket + 1) * every) + 1\n\n    /* the bucket ahead stands in for its own points as one centre of mass */\n    const aheadEnd = Math.min(Math.floor((bucket + 2) * every) + 1, rows.length)\n    let aheadX = 0\n    let aheadY = 0\n    for (let i = end; i < aheadEnd; i++) {\n      aheadX += x(rows[i])\n      aheadY += y(rows[i])\n    }\n    aheadX /= aheadEnd - end\n    aheadY /= aheadEnd - end\n\n    const anchorX = x(rows[anchor])\n    const anchorY = y(rows[anchor])\n    let widest = -1\n    let best = start\n\n    for (let i = start; i < end; i++) {\n      const area = Math.abs(\n        (anchorX - aheadX) * (y(rows[i]) - anchorY) - (anchorX - x(rows[i])) * (aheadY - anchorY),\n      )\n      if (area > widest) {\n        widest = area\n        best = i\n      }\n    }\n    kept.push(rows[best])\n    anchor = best\n  }\n\n  kept.push(rows[last])\n  return kept\n}\n"
        },
        {
          "path": "components/ui/chart/motion.ts",
          "target": "components/ui/chart/motion.ts",
          "type": "registry:ui",
          "content": "/*\n * a dash cannot be given the length of its own path from a stylesheet, so the length\n * is measured here and handed to the animation as a custom property.\n */\nexport function drawLines(svg: SVGSVGElement) {\n  const paths = svg.querySelectorAll<SVGPathElement>('g[aria-label=\"line\"] path')\n\n  paths.forEach((path, index) => {\n    path.style.setProperty('--draw', `${path.getTotalLength()}px`)\n    path.style.setProperty('--draw-delay', `calc(var(--dur-instant) * ${index})`)\n    path.classList.add('reed-draw')\n  })\n\n  return paths.length\n}\n\n/*\n * a bar grows out of the axis rather than out of its own box, so a stacked column stays\n * whole on the way up. every rect scales about the same baseline, which is the lowest\n * edge any of them reaches.\n */\nexport function drawBars(svg: SVGSVGElement) {\n  const rects = [...svg.querySelectorAll<SVGRectElement>('g[aria-label=\"bar\"] rect')]\n  if (rects.length === 0) return 0\n\n  const base = Math.max(...rects.map((rect) => rect.y.baseVal.value + rect.height.baseVal.value))\n  const columns = [...new Set(rects.map((rect) => rect.x.baseVal.value))].sort((left, right) => left - right)\n  const order = new Map(columns.map((x, index) => [x, index]))\n\n  for (const rect of rects) {\n    const index = order.get(rect.x.baseVal.value) ?? 0\n    rect.style.setProperty('--build-base', `${base}px`)\n    /* the sweep across the columns fits inside one --dur-instant, whatever the count */\n    rect.style.setProperty('--build-delay', `calc(var(--dur-instant) * ${index / columns.length})`)\n    rect.classList.add('reed-build')\n  }\n\n  return rects.length\n}\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/vue/0.11.0/chart.json",
        "files": [
          {
            "path": "components/ui/chart/Chart.vue",
            "hash": "sha256-Mn8vnlEdX33U/XUaWSAMMsi52INoQnB+MEesIUFw+xk=",
            "client": false
          },
          {
            "path": "components/ui/chart/brush.ts",
            "hash": "sha256-CMIpDxzji8iWtJbagr2r5GmQ5zEno6le+jpCpvMynkg=",
            "client": false
          },
          {
            "path": "components/ui/chart/crosshair.ts",
            "hash": "sha256-zLJAHBmcbG6wi3RoMhwiNuPjk8Sh+ui7W5Hfi3/Fkw0=",
            "client": false
          },
          {
            "path": "components/ui/chart/downsample.ts",
            "hash": "sha256-zfYABCIrkZGVxTiQC4wJhE1pUu/bOfZvx/GdCEA+1Gc=",
            "client": false
          },
          {
            "path": "components/ui/chart/motion.ts",
            "hash": "sha256-d+T9WcxUcAMpmLN2FKT2OA+NLD8XFPBKsyxHUQFpMRA=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "checkbox",
      "type": "registry:ui",
      "title": "Checkbox",
      "dependencies": [
        "@ark-ui/vue@^5.38.2"
      ],
      "registryDependencies": [
        "cx",
        "icons",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/checkbox/Checkbox.vue",
          "target": "components/ui/checkbox/Checkbox.vue",
          "type": "registry:ui",
          "content": "<script lang=\"ts\">\nexport type CheckedState = boolean | 'indeterminate'\n</script>\n\n<script setup lang=\"ts\">\nimport { Checkbox as ArkCheckbox } from '@ark-ui/vue/checkbox'\nimport { useSlots, type HTMLAttributes } from 'vue'\nimport CheckIcon from '@/components/ui/icons/CheckIcon.vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{\n  /* names a box that shows no text */\n  label?: string\n  class?: HTMLAttributes['class']\n}>()\n\nconst checked = defineModel<CheckedState>('checked', { required: true })\n\nconst slots = useSlots()\n\nconst CONTROL =\n  'grid size-(--ctl-box) shrink-0 place-items-center border border-reed-lit text-ground transition-colors duration-(--dur-instant) ease-(--ease-beat) data-[state=checked]:border-indigo data-[state=checked]:bg-indigo data-[state=indeterminate]:border-indigo data-[state=indeterminate]:bg-indigo'\n</script>\n\n<!-- the ark root is a label element, so the text inside it toggles the box -->\n<template>\n  <ArkCheckbox.Root\n    v-model:checked=\"checked\"\n    :class=\"cx('ctl-align inline-flex cursor-pointer items-center gap-2', props.class)\"\n  >\n    <ArkCheckbox.Control :class=\"CONTROL\">\n      <ArkCheckbox.Indicator>\n        <CheckIcon class=\"size-2.75\" />\n      </ArkCheckbox.Indicator>\n      <ArkCheckbox.Indicator indeterminate>\n        <span class=\"block h-px w-1.75 bg-current\" />\n      </ArkCheckbox.Indicator>\n    </ArkCheckbox.Control>\n    <ArkCheckbox.Label v-if=\"slots.default\">\n      <slot />\n    </ArkCheckbox.Label>\n    <ArkCheckbox.HiddenInput :aria-label=\"slots.default ? undefined : props.label\" />\n  </ArkCheckbox.Root>\n</template>\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/vue/0.11.0/checkbox.json",
        "files": [
          {
            "path": "components/ui/checkbox/Checkbox.vue",
            "hash": "sha256-VNnw5iZiEj8C0UlrI7uTGTk0QP1IcQrQcuohH+rT51E=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "command-palette",
      "type": "registry:ui",
      "title": "Command Palette",
      "dependencies": [
        "@ark-ui/vue@^5.38.2"
      ],
      "registryDependencies": [
        "icons",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/command-palette/CommandPalette.vue",
          "target": "components/ui/command-palette/CommandPalette.vue",
          "type": "registry:ui",
          "content": "<script lang=\"ts\">\nexport interface Command {\n  readonly id: string\n  readonly label: string\n  readonly group: string\n  readonly hint?: string\n  readonly run: () => void\n}\n</script>\n\n<script setup lang=\"ts\">\nimport { Dialog } from '@ark-ui/vue/dialog'\nimport { computed, ref, watch } from 'vue'\nimport SearchIcon from '@/components/ui/icons/SearchIcon.vue'\n\nconst props = withDefaults(\n  defineProps<{\n    commands: readonly Command[]\n    placeholder?: string\n  }>(),\n  { placeholder: 'Run a command' },\n)\n\nconst open = defineModel<boolean>('open', { required: true })\n\nconst query = ref('')\nconst active = ref(0)\nconst list = ref<HTMLDivElement | null>(null)\nconst input = ref<HTMLInputElement | null>(null)\n\nconst matches = computed(() => {\n  const needle = query.value.trim().toLowerCase()\n  if (needle === '') return props.commands\n  return props.commands.filter((command) => `${command.group} ${command.label}`.toLowerCase().includes(needle))\n})\n\nconst groups = computed(() => {\n  const buckets = new Map<string, { command: Command; index: number }[]>()\n  matches.value.forEach((command, index) => {\n    const bucket = buckets.get(command.group)\n    if (bucket) {\n      bucket.push({ command, index })\n    } else {\n      buckets.set(command.group, [{ command, index }])\n    }\n  })\n  return [...buckets]\n})\n\nwatch(query, () => {\n  active.value = 0\n})\n\n/* clears on the way in: the list must keep its matches while the dialog leaves */\nwatch(open, (isOpen) => {\n  if (isOpen) query.value = ''\n})\n\n// keyboard navigation moves no pointer, so nothing else scrolls the row into view\nwatch(\n  active,\n  () => {\n    list.value?.querySelector('[data-active]')?.scrollIntoView({ block: 'nearest' })\n  },\n  { flush: 'post' },\n)\n\nconst choose = (command: Command) => {\n  command.run()\n  open.value = false\n}\n\nconst onKeyDown = (event: KeyboardEvent) => {\n  if (matches.value.length === 0) return\n\n  if (event.key === 'ArrowDown') {\n    event.preventDefault()\n    active.value = (active.value + 1) % matches.value.length\n    return\n  }\n  if (event.key === 'ArrowUp') {\n    event.preventDefault()\n    active.value = (active.value - 1 + matches.value.length) % matches.value.length\n    return\n  }\n  if (event.key === 'Enter') {\n    event.preventDefault()\n    choose(matches.value[active.value])\n  }\n}\n</script>\n\n<template>\n  <Dialog.Root v-model:open=\"open\" :initial-focus-el=\"() => input\" unmount-on-exit lazy-mount>\n    <Teleport to=\"body\">\n      <Dialog.Backdrop\n        class=\"fixed inset-0 z-(--z-backdrop) bg-sunken/70 backdrop-blur-[2px] data-[state=open]:animate-[fade_var(--dur-overlay)_var(--ease-beat)] data-[state=closed]:animate-[fade-out_var(--dur-local)_var(--ease-exit)]\"\n      />\n      <Dialog.Positioner class=\"fixed inset-0 z-(--z-modal) grid place-items-start justify-items-center pt-[12vh]\">\n        <Dialog.Content\n          class=\"layer w-[min(560px,92vw)] data-[state=open]:animate-[rise_var(--dur-overlay)_var(--ease-beat)] data-[state=closed]:animate-[rise-out_var(--dur-local)_var(--ease-exit)]\"\n        >\n          <Dialog.Title class=\"sr-only\">Command palette</Dialog.Title>\n\n          <div class=\"reed-edge flex items-center gap-2 px-3\" :style=\"{ height: 'calc(var(--ctl-h) + 12px)' }\">\n            <SearchIcon class=\"size-4 text-weft-faint\" />\n            <input\n              ref=\"input\"\n              v-model=\"query\"\n              name=\"command\"\n              :placeholder=\"props.placeholder\"\n              :aria-label=\"props.placeholder\"\n              class=\"w-full bg-transparent text-weft placeholder:text-weft-faint focus:outline-none\"\n              @keydown=\"onKeyDown\"\n            />\n            <kbd class=\"font-data text-[11px] text-weft-faint\">esc</kbd>\n          </div>\n\n          <div ref=\"list\" class=\"max-h-[46vh] overflow-auto py-1\">\n            <p v-if=\"matches.length === 0\" class=\"px-3 py-6 text-center text-weft-dim\">\n              No command matches \"{{ query }}\". Try a shorter word.\n            </p>\n\n            <div v-for=\"[group, items] in groups\" :key=\"group\">\n              <p\n                class=\"px-3 pt-2 pb-1 font-data text-[11px] tracking-wide text-weft-faint uppercase dense:sr-only\"\n              >\n                {{ group }}\n              </p>\n              <button\n                v-for=\"item in items\"\n                :key=\"item.command.id\"\n                type=\"button\"\n                :data-active=\"item.index === active ? '' : undefined\"\n                class=\"flex w-full cursor-pointer items-center justify-between gap-3 px-3 text-left text-weft-dim transition-colors duration-(--dur-instant) ease-(--ease-beat) data-active:bg-shed data-active:text-weft\"\n                :style=\"{ height: 'var(--row-h)' }\"\n                @pointermove=\"active = item.index\"\n                @click=\"choose(item.command)\"\n              >\n                <span class=\"truncate\">{{ item.command.label }}</span>\n                <kbd v-if=\"item.command.hint\" class=\"font-data text-[11px] text-weft-faint\">\n                  {{ item.command.hint }}\n                </kbd>\n              </button>\n            </div>\n          </div>\n        </Dialog.Content>\n      </Dialog.Positioner>\n    </Teleport>\n  </Dialog.Root>\n</template>\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/vue/0.11.0/command-palette.json",
        "files": [
          {
            "path": "components/ui/command-palette/CommandPalette.vue",
            "hash": "sha256-5IhkHFyyREdZ8fV3gpbYEENUW1s8Ka7bphmcE2ACHMI=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "dialog",
      "type": "registry:ui",
      "title": "Dialog",
      "dependencies": [
        "@ark-ui/vue@^5.38.2"
      ],
      "registryDependencies": [
        "cx",
        "icons",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/dialog/Dialog.vue",
          "target": "components/ui/dialog/Dialog.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Dialog as ArkDialog } from '@ark-ui/vue/dialog'\n\n/* ark removes the node when the exit animation ends. both mount options default on. */\nconst props = withDefaults(\n  defineProps<{\n    unmountOnExit?: boolean\n    lazyMount?: boolean\n  }>(),\n  { unmountOnExit: true, lazyMount: true },\n)\n</script>\n\n<!-- `v-model:open` and `role`: attributes, forwarded -->\n<template>\n  <ArkDialog.Root :unmount-on-exit=\"props.unmountOnExit\" :lazy-mount=\"props.lazyMount\">\n    <slot />\n  </ArkDialog.Root>\n</template>\n"
        },
        {
          "path": "components/ui/dialog/DialogBody.vue",
          "target": "components/ui/dialog/DialogBody.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n</script>\n\n<template>\n  <div :class=\"cx('flex flex-col gap-(--stack) px-(--cell-x) py-4', props.class)\">\n    <slot />\n  </div>\n</template>\n"
        },
        {
          "path": "components/ui/dialog/DialogClose.vue",
          "target": "components/ui/dialog/DialogClose.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Dialog as ArkDialog } from '@ark-ui/vue/dialog'\n</script>\n\n<template>\n  <ArkDialog.CloseTrigger>\n    <slot />\n  </ArkDialog.CloseTrigger>\n</template>\n"
        },
        {
          "path": "components/ui/dialog/DialogContent.vue",
          "target": "components/ui/dialog/DialogContent.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Dialog as ArkDialog } from '@ark-ui/vue/dialog'\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n</script>\n\n<!-- ark names the dialog from its title, so every dialog carries one -->\n<template>\n  <Teleport to=\"body\">\n    <ArkDialog.Backdrop\n      class=\"fixed inset-0 z-(--z-backdrop) bg-sunken/70 backdrop-blur-[2px] data-[state=open]:animate-[fade_var(--dur-overlay)_var(--ease-beat)] data-[state=closed]:animate-[fade-out_var(--dur-local)_var(--ease-exit)]\"\n    />\n    <ArkDialog.Positioner class=\"fixed inset-0 z-(--z-modal) grid place-items-center p-6\">\n      <ArkDialog.Content\n        :class=\"\n          cx(\n            'layer flex w-[min(480px,92vw)] flex-col data-[state=open]:animate-[rise_var(--dur-overlay)_var(--ease-beat)] data-[state=closed]:animate-[rise-out_var(--dur-local)_var(--ease-exit)]',\n            props.class,\n          )\n        \"\n      >\n        <slot />\n      </ArkDialog.Content>\n    </ArkDialog.Positioner>\n  </Teleport>\n</template>\n"
        },
        {
          "path": "components/ui/dialog/DialogDescription.vue",
          "target": "components/ui/dialog/DialogDescription.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Dialog as ArkDialog } from '@ark-ui/vue/dialog'\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n</script>\n\n<template>\n  <ArkDialog.Description :class=\"cx('text-weft-dim', props.class)\">\n    <slot />\n  </ArkDialog.Description>\n</template>\n"
        },
        {
          "path": "components/ui/dialog/DialogFooter.vue",
          "target": "components/ui/dialog/DialogFooter.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n</script>\n\n<template>\n  <footer\n    :class=\"cx('flex items-center justify-end gap-(--stack) border-t border-reed px-(--cell-x) py-(--stack)', props.class)\"\n  >\n    <slot />\n  </footer>\n</template>\n"
        },
        {
          "path": "components/ui/dialog/DialogHeader.vue",
          "target": "components/ui/dialog/DialogHeader.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Dialog as ArkDialog } from '@ark-ui/vue/dialog'\nimport type { HTMLAttributes } from 'vue'\nimport CloseIcon from '@/components/ui/icons/CloseIcon.vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n</script>\n\n<template>\n  <header :class=\"cx('reed-edge flex items-start justify-between gap-4 px-(--cell-x) py-(--stack)', props.class)\">\n    <div class=\"flex flex-col gap-1\">\n      <slot />\n    </div>\n    <ArkDialog.CloseTrigger\n      aria-label=\"Close\"\n      class=\"-mr-1 cursor-pointer p-1 text-weft-faint transition-colors duration-(--dur-instant) ease-(--ease-beat) hover:text-weft\"\n    >\n      <CloseIcon class=\"size-3.5\" />\n    </ArkDialog.CloseTrigger>\n  </header>\n</template>\n"
        },
        {
          "path": "components/ui/dialog/DialogTitle.vue",
          "target": "components/ui/dialog/DialogTitle.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Dialog as ArkDialog } from '@ark-ui/vue/dialog'\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n</script>\n\n<template>\n  <ArkDialog.Title :class=\"cx('font-medium', props.class)\">\n    <slot />\n  </ArkDialog.Title>\n</template>\n"
        },
        {
          "path": "components/ui/dialog/DialogTrigger.vue",
          "target": "components/ui/dialog/DialogTrigger.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Dialog as ArkDialog } from '@ark-ui/vue/dialog'\n</script>\n\n<template>\n  <ArkDialog.Trigger>\n    <slot />\n  </ArkDialog.Trigger>\n</template>\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/vue/0.11.0/dialog.json",
        "files": [
          {
            "path": "components/ui/dialog/Dialog.vue",
            "hash": "sha256-xNz/Bn3YeULHMVq4N4FkzbmcIbFauWfYgOYXmFsxHPA=",
            "client": false
          },
          {
            "path": "components/ui/dialog/DialogBody.vue",
            "hash": "sha256-fMMCE2PJNrrPQr4yW+bxmBspeQ7IXHVsQcSFrfQ/m2I=",
            "client": false
          },
          {
            "path": "components/ui/dialog/DialogClose.vue",
            "hash": "sha256-EFqLHKc21q4TgCqbY8vCWnCOdOgEKEmSjjzERM8Euws=",
            "client": false
          },
          {
            "path": "components/ui/dialog/DialogContent.vue",
            "hash": "sha256-g8HwG2F5YJd86Cj8Xn1ahXM4oRow8kosD2iF8t54gOE=",
            "client": false
          },
          {
            "path": "components/ui/dialog/DialogDescription.vue",
            "hash": "sha256-HzES2t0DV9oQ49n70zJEAjICylVpFHfPvhUPmzTswbI=",
            "client": false
          },
          {
            "path": "components/ui/dialog/DialogFooter.vue",
            "hash": "sha256-5xy5EbKtHPG+yikhh61H4OGspEPFTJCEgBRxP8/WuRY=",
            "client": false
          },
          {
            "path": "components/ui/dialog/DialogHeader.vue",
            "hash": "sha256-XJ4l1wZJ3X5MqzlaRltlm/3BAo3dGaSeiswsPfS06gs=",
            "client": false
          },
          {
            "path": "components/ui/dialog/DialogTitle.vue",
            "hash": "sha256-CuDFjFCgjPyJmFgch9CR9xwRTtURJ6gW1Uy4g5zZETg=",
            "client": false
          },
          {
            "path": "components/ui/dialog/DialogTrigger.vue",
            "hash": "sha256-xtNAqD1tOig//ckhYa0KMnpXKtngc1ij99KJZ0bL1q0=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "empty-state",
      "type": "registry:ui",
      "title": "Empty State",
      "dependencies": [],
      "registryDependencies": [
        "cx",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/empty-state/EmptyState.vue",
          "target": "components/ui/empty-state/EmptyState.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{\n  title: string\n  description?: string\n  class?: HTMLAttributes['class']\n}>()\n</script>\n\n<!-- the text sits on a patch of cloth. no warp runs behind it. -->\n<template>\n  <div\n    :class=\"\n      cx('reed-warp flex flex-col items-center justify-center gap-(--stack) p-(--cell-x) text-center', props.class)\n    \"\n    :style=\"{ minHeight: 'calc(var(--row-h) * 4)' }\"\n  >\n    <div class=\"flex flex-col gap-1 bg-raised px-(--cell-x)\">\n      <p class=\"text-weft-dim\">{{ props.title }}</p>\n      <p v-if=\"props.description\" class=\"text-weft-faint\">{{ props.description }}</p>\n    </div>\n    <slot name=\"action\" />\n  </div>\n</template>\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/vue/0.11.0/empty-state.json",
        "files": [
          {
            "path": "components/ui/empty-state/EmptyState.vue",
            "hash": "sha256-jI3cGpdcyAp8+1Qw36cijUNPmFy4V5kGOdJC83Myv2g=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "field",
      "type": "registry:ui",
      "title": "Field",
      "dependencies": [
        "@ark-ui/vue@^5.38.2"
      ],
      "registryDependencies": [
        "cx",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/field/Field.vue",
          "target": "components/ui/field/Field.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Field as ArkField } from '@ark-ui/vue/field'\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n</script>\n\n<template>\n  <ArkField.Root :class=\"cx('flex flex-col gap-1', props.class)\">\n    <slot />\n  </ArkField.Root>\n</template>\n"
        },
        {
          "path": "components/ui/field/FieldError.vue",
          "target": "components/ui/field/FieldError.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Field as ArkField } from '@ark-ui/vue/field'\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n</script>\n\n<template>\n  <ArkField.ErrorText :class=\"cx('text-madder', props.class)\">\n    <slot />\n  </ArkField.ErrorText>\n</template>\n"
        },
        {
          "path": "components/ui/field/FieldHint.vue",
          "target": "components/ui/field/FieldHint.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Field as ArkField } from '@ark-ui/vue/field'\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n</script>\n\n<template>\n  <ArkField.HelperText :class=\"cx('text-weft-faint dense:sr-only', props.class)\">\n    <slot />\n  </ArkField.HelperText>\n</template>\n"
        },
        {
          "path": "components/ui/field/FieldInput.vue",
          "target": "components/ui/field/FieldInput.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Field as ArkField } from '@ark-ui/vue/field'\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n\nconst value = defineModel<string>()\n</script>\n\n<template>\n  <ArkField.Input v-model=\"value\" :class=\"cx('ctl ctl-entry h-(--ctl-h)', props.class)\" />\n</template>\n"
        },
        {
          "path": "components/ui/field/FieldLabel.vue",
          "target": "components/ui/field/FieldLabel.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Field as ArkField } from '@ark-ui/vue/field'\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n</script>\n\n<template>\n  <ArkField.Label :class=\"cx('flex items-center gap-1 text-weft-dim', props.class)\">\n    <slot />\n    <ArkField.RequiredIndicator class=\"text-weft-faint\" />\n  </ArkField.Label>\n</template>\n"
        },
        {
          "path": "components/ui/field/FieldSet.vue",
          "target": "components/ui/field/FieldSet.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Fieldset } from '@ark-ui/vue/fieldset'\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{\n  legend: string\n  disabled?: boolean\n  class?: HTMLAttributes['class']\n}>()\n</script>\n\n<template>\n  <Fieldset.Root :disabled=\"props.disabled\" :class=\"cx('flex flex-col gap-(--stack)', props.class)\">\n    <Fieldset.Legend class=\"reed-edge w-full pb-(--stack) font-medium\">{{ props.legend }}</Fieldset.Legend>\n    <slot />\n  </Fieldset.Root>\n</template>\n"
        },
        {
          "path": "components/ui/field/FieldTextarea.vue",
          "target": "components/ui/field/FieldTextarea.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Field as ArkField } from '@ark-ui/vue/field'\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n\nconst value = defineModel<string>()\n</script>\n\n<template>\n  <ArkField.Textarea\n    v-model=\"value\"\n    :class=\"cx('ctl ctl-entry min-h-20 resize-y py-(--stack) leading-relaxed', props.class)\"\n  />\n</template>\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/vue/0.11.0/field.json",
        "files": [
          {
            "path": "components/ui/field/Field.vue",
            "hash": "sha256-5LyXvYnngilv1HnzLOx6f309TO1kO7Y4/7gbvL1MwE4=",
            "client": false
          },
          {
            "path": "components/ui/field/FieldError.vue",
            "hash": "sha256-hrVlQtpnhlBUKqlRye8AGdXidCDF/IgmzLSa7WDTXWM=",
            "client": false
          },
          {
            "path": "components/ui/field/FieldHint.vue",
            "hash": "sha256-LZ+ad3YjiGorFTApZ6SRwLpq3CRnJ9bt82Q/UHupRC4=",
            "client": false
          },
          {
            "path": "components/ui/field/FieldInput.vue",
            "hash": "sha256-Oh0uPl9Lr/bhtI0f5xlg9Tc4L0kZ6E6atSOhO8IemGs=",
            "client": false
          },
          {
            "path": "components/ui/field/FieldLabel.vue",
            "hash": "sha256-iGBM2zzdn8gwg046uL6iCg265AG8bOGDBRa9dRmwkJM=",
            "client": false
          },
          {
            "path": "components/ui/field/FieldSet.vue",
            "hash": "sha256-S7CW/tgmmSdb9qTcdDKdcHLwVzGSjD3SlBbQ7ZL9wCg=",
            "client": false
          },
          {
            "path": "components/ui/field/FieldTextarea.vue",
            "hash": "sha256-sAe4n82ACAj4UUYRlAeco9ACWSI1lQkhfwWOvvFNHh0=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "figure",
      "type": "registry:ui",
      "title": "Figure",
      "dependencies": [],
      "registryDependencies": [
        "cx",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/figure/Elapsed.vue",
          "target": "components/ui/figure/Elapsed.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport type { HTMLAttributes } from 'vue'\n\nconst props = defineProps<{\n  minutes: number\n  class?: HTMLAttributes['class']\n}>()\n</script>\n\n<!-- a head cannot hold two units, so a duration carries them in the cell -->\n<template>\n  <span :class=\"props.class\"\n    >{{ Math.trunc(props.minutes / 60) }}<span class=\"text-weft-faint\">h </span\n    >{{ String(props.minutes % 60).padStart(2, '0') }}<span class=\"text-weft-faint\">m</span></span\n  >\n</template>\n"
        },
        {
          "path": "components/ui/figure/Figure.vue",
          "target": "components/ui/figure/Figure.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { computed, type HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = withDefaults(\n  defineProps<{\n    value: number\n    digits?: number\n    low?: boolean\n    class?: HTMLAttributes['class']\n  }>(),\n  { digits: 1, low: false },\n)\n\n/* anything that is not a digit reads one step back */\nconst parts = computed(() => {\n  const [whole, fraction] = props.value.toFixed(props.digits).split('.')\n  return { whole, fraction }\n})\n</script>\n\n<template>\n  <span v-if=\"props.value === 0\" :class=\"cx('reed-mark', props.class)\" aria-hidden=\"true\" />\n  <span v-else :class=\"cx(props.low && 'reed-under', props.class)\"\n    >{{ parts.whole }}<span v-if=\"parts.fraction !== undefined\" class=\"text-weft-faint\">.{{ parts.fraction }}</span\n    ><span v-if=\"props.low\" class=\"sr-only\"> below threshold</span></span\n  >\n</template>\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/vue/0.11.0/figure.json",
        "files": [
          {
            "path": "components/ui/figure/Elapsed.vue",
            "hash": "sha256-30p2u2D+UKK6qQEutV7ppKXd9/fwjya38NHxL6bQDc8=",
            "client": false
          },
          {
            "path": "components/ui/figure/Figure.vue",
            "hash": "sha256-xptdvIr3tMXXSgNTocd2UpPmlvKNLFKTYHV7gN1nZOQ=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "filter-bar",
      "type": "registry:ui",
      "title": "Filter Bar",
      "dependencies": [
        "@ark-ui/vue@^5.38.2"
      ],
      "registryDependencies": [
        "cx",
        "icons",
        "select",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/filter-bar/FilterBar.vue",
          "target": "components/ui/filter-bar/FilterBar.vue",
          "type": "registry:ui",
          "content": "<script lang=\"ts\">\nexport interface FilterGroup {\n  readonly key: string\n  readonly label: string\n  readonly options: readonly string[]\n}\n\nexport type FilterValues = Readonly<Record<string, readonly string[]>>\n</script>\n\n<script setup lang=\"ts\">\nimport { createListCollection } from '@ark-ui/vue/select'\nimport { computed, type HTMLAttributes } from 'vue'\nimport CloseIcon from '@/components/ui/icons/CloseIcon.vue'\nimport SearchIcon from '@/components/ui/icons/SearchIcon.vue'\nimport Select from '@/components/ui/select/Select.vue'\nimport SelectContent from '@/components/ui/select/SelectContent.vue'\nimport SelectOption from '@/components/ui/select/SelectOption.vue'\nimport SelectTrigger from '@/components/ui/select/SelectTrigger.vue'\nimport { cx } from '@/lib/cx'\n\nconst props = withDefaults(\n  defineProps<{\n    groups: readonly FilterGroup[]\n    searchLabel?: string\n    placeholder?: string\n    class?: HTMLAttributes['class']\n  }>(),\n  { searchLabel: 'Search', placeholder: 'Search' },\n)\n\nconst query = defineModel<string>('query', { required: true })\nconst values = defineModel<FilterValues>('values', { required: true })\n\nconst collections = computed(() =>\n  props.groups.map((group) => ({\n    group,\n    collection: createListCollection({ items: group.options.map((value) => ({ label: value, value })) }),\n  })),\n)\n\nconst chips = computed(() =>\n  props.groups.flatMap((group) => (values.value[group.key] ?? []).map((value) => ({ group, value }))),\n)\n\nconst selectedIn = (key: string) => values.value[key] ?? []\n\nconst setGroup = (key: string, next: readonly string[]) => {\n  values.value = { ...values.value, [key]: next }\n}\n\nconst removeChip = (key: string, value: string) => {\n  setGroup(\n    key,\n    selectedIn(key).filter((entry) => entry !== value),\n  )\n}\n</script>\n\n<template>\n  <!-- vue does not know the `search` element yet, and a dynamic tag reaches it without a warning -->\n  <component :is=\"'search'\" :class=\"cx('flex flex-col gap-(--stack)', props.class)\">\n    <div class=\"flex flex-wrap items-center gap-(--stack)\">\n      <div class=\"ctl focus-ring h-(--ctl-h) min-w-55 flex-1\">\n        <SearchIcon class=\"size-3.5 text-weft-faint\" />\n        <input\n          v-model=\"query\"\n          type=\"search\"\n          name=\"search\"\n          :placeholder=\"props.placeholder\"\n          :aria-label=\"props.searchLabel\"\n          class=\"w-full bg-transparent py-0 placeholder:text-weft-faint focus:outline-none\"\n        />\n      </div>\n\n      <Select\n        v-for=\"entry in collections\"\n        :key=\"entry.group.key\"\n        :collection=\"entry.collection\"\n        multiple\n        :model-value=\"[...selectedIn(entry.group.key)]\"\n        @update:model-value=\"(next: string[] | undefined) => setGroup(entry.group.key, next ?? [])\"\n      >\n        <SelectTrigger>\n          <span class=\"inline-flex items-baseline gap-1.5\">\n            <span :class=\"selectedIn(entry.group.key).length > 0 ? 'text-weft' : 'text-weft-dim'\">\n              {{ entry.group.label }}\n            </span>\n            <span v-if=\"selectedIn(entry.group.key).length > 0\" class=\"tnum font-data text-indigo\">\n              {{ selectedIn(entry.group.key).length }}\n            </span>\n          </span>\n        </SelectTrigger>\n        <SelectContent>\n          <SelectOption v-for=\"item in entry.collection.items\" :key=\"item.value\" :item=\"item\" />\n        </SelectContent>\n      </Select>\n    </div>\n\n    <ul v-if=\"chips.length > 0\" class=\"flex flex-wrap items-center gap-(--stack)\">\n      <li v-for=\"chip in chips\" :key=\"`${chip.group.key}:${chip.value}`\">\n        <button\n          type=\"button\"\n          class=\"inline-flex h-(--ctl-h) cursor-pointer items-center gap-1.5 border border-indigo/40 bg-indigo-wash px-(--cell-x) text-weft transition-colors duration-(--dur-instant) ease-(--ease-beat) hover:border-indigo\"\n          @click=\"removeChip(chip.group.key, chip.value)\"\n        >\n          <span class=\"inline-flex items-baseline gap-1.5\">\n            <span class=\"font-data text-weft-dim\">{{ chip.group.label.toLowerCase() }}</span>\n            <span>{{ chip.value }}</span>\n          </span>\n          <CloseIcon class=\"size-3 text-weft-faint\" />\n        </button>\n      </li>\n      <li>\n        <button\n          type=\"button\"\n          class=\"h-(--ctl-h) cursor-pointer px-(--cell-x) text-weft-dim underline-offset-4 hover:text-weft hover:underline\"\n          @click=\"values = {}\"\n        >\n          Clear filters\n        </button>\n      </li>\n    </ul>\n  </component>\n</template>\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/vue/0.11.0/filter-bar.json",
        "files": [
          {
            "path": "components/ui/filter-bar/FilterBar.vue",
            "hash": "sha256-mnIJPDcntWJi8Dpxyag3NjrX3/+3wYamrXMxGqlPFE0=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "icons",
      "type": "registry:ui",
      "title": "Icons",
      "dependencies": [],
      "registryDependencies": [
        "cx",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/icons/CheckIcon.vue",
          "target": "components/ui/icons/CheckIcon.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n</script>\n\n<template>\n  <svg :class=\"cx('shrink-0', props.class)\" viewBox=\"0 0 16 16\" fill=\"none\" aria-hidden=\"true\" focusable=\"false\">\n    <path d=\"M3.5 8.5 6.5 11.5 12.5 5\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"square\" />\n  </svg>\n</template>\n"
        },
        {
          "path": "components/ui/icons/ChevronIcon.vue",
          "target": "components/ui/icons/ChevronIcon.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n</script>\n\n<template>\n  <svg :class=\"cx('shrink-0', props.class)\" viewBox=\"0 0 16 16\" fill=\"none\" aria-hidden=\"true\" focusable=\"false\">\n    <path d=\"m4 6.5 4 4 4-4\" stroke=\"currentColor\" stroke-width=\"1.5\" stroke-linecap=\"round\" stroke-linejoin=\"round\" />\n  </svg>\n</template>\n"
        },
        {
          "path": "components/ui/icons/CloseIcon.vue",
          "target": "components/ui/icons/CloseIcon.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n</script>\n\n<template>\n  <svg :class=\"cx('shrink-0', props.class)\" viewBox=\"0 0 16 16\" fill=\"none\" aria-hidden=\"true\" focusable=\"false\">\n    <path d=\"m4 4 8 8M12 4l-8 8\" stroke=\"currentColor\" stroke-width=\"1.5\" stroke-linecap=\"round\" />\n  </svg>\n</template>\n"
        },
        {
          "path": "components/ui/icons/SearchIcon.vue",
          "target": "components/ui/icons/SearchIcon.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n</script>\n\n<template>\n  <svg :class=\"cx('shrink-0', props.class)\" viewBox=\"0 0 16 16\" fill=\"none\" aria-hidden=\"true\" focusable=\"false\">\n    <circle cx=\"7\" cy=\"7\" r=\"4.25\" stroke=\"currentColor\" stroke-width=\"1.5\" />\n    <path d=\"M10.5 10.5 14 14\" stroke=\"currentColor\" stroke-width=\"1.5\" stroke-linecap=\"round\" />\n  </svg>\n</template>\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/vue/0.11.0/icons.json",
        "files": [
          {
            "path": "components/ui/icons/CheckIcon.vue",
            "hash": "sha256-VK3rxcxw5gdn6WN0QMVVdhcyGesulxsZefoWe8pHbwk=",
            "client": false
          },
          {
            "path": "components/ui/icons/ChevronIcon.vue",
            "hash": "sha256-4MW/oPjOosBhiFIv2fUihF2Vf9Th0MUinGmfQp5zHW0=",
            "client": false
          },
          {
            "path": "components/ui/icons/CloseIcon.vue",
            "hash": "sha256-c8kywFxwEjcDoqZnCGYpimGh1GuKA52wB8GOWL6vEvo=",
            "client": false
          },
          {
            "path": "components/ui/icons/SearchIcon.vue",
            "hash": "sha256-l/ott6twzaKlmTzGjO+WL+yhd77+cNYmKAEkrDqzWGc=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "panel",
      "type": "registry:ui",
      "title": "Panel",
      "dependencies": [
        "@ark-ui/vue@^5.38.2"
      ],
      "registryDependencies": [
        "cx",
        "icons",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/panel/Panel.vue",
          "target": "components/ui/panel/Panel.vue",
          "type": "registry:ui",
          "content": "<script lang=\"ts\">\nexport type PanelSide = 'start' | 'end'\n</script>\n\n<script setup lang=\"ts\">\nimport { Dialog } from '@ark-ui/vue/dialog'\nimport { onMounted, onUnmounted, ref, watch, type HTMLAttributes } from 'vue'\nimport CloseIcon from '@/components/ui/icons/CloseIcon.vue'\nimport { cx } from '@/lib/cx'\n\n/*\n * not modal while the table stays live: no focus trap, no scroll lock, no backdrop, and\n * a click outside picks the next row rather than closing. on a screen it covers, it locks\n * the page.\n */\nconst props = withDefaults(\n  defineProps<{\n    title: string\n    description?: string\n    side?: PanelSide\n    class?: HTMLAttributes['class']\n  }>(),\n  { side: 'end' },\n)\n\nconst open = defineModel<boolean>('open', { required: true })\n\n/* below this the panel covers the page and has to behave as a modal */\nconst SHEET = '(max-width: 640px)'\n\n/* the positioner holds the edge; the border belongs to the part that animates */\nconst SIDE: Record<PanelSide, string> = {\n  start: 'start-0 [--slide-from:-100%]',\n  end: 'end-0 [--slide-from:100%]',\n}\n\n/* read after mount. a server has no matchMedia. */\nconst sheet = ref(false)\nlet query: MediaQueryList | undefined\n\nconst apply = () => {\n  sheet.value = query?.matches === true\n}\n\nonMounted(() => {\n  query = window.matchMedia(SHEET)\n  apply()\n  query.addEventListener('change', apply)\n})\n\nonUnmounted(() => query?.removeEventListener('change', apply))\n\n/*\n * zag returns focus from inside its focus trap, and it only runs that trap when the dialog is\n * modal. so a panel that leaves the page live drops the reader on the body when it closes, and\n * has to put focus back itself. closing is always started from inside the panel here, by escape\n * or by the close button, so there is no other claim on focus to weigh.\n */\nconst origin = ref<HTMLElement | null>(null)\n\nwatch([open, sheet], ([isOpen, isSheet]) => {\n  if (isSheet) return\n\n  if (isOpen) {\n    const active = document.activeElement\n    origin.value = active instanceof HTMLElement ? active : null\n    return\n  }\n\n  const element = origin.value\n  origin.value = null\n  if (element?.isConnected) element.focus()\n})\n</script>\n\n<template>\n  <Dialog.Root\n    v-model:open=\"open\"\n    :modal=\"sheet\"\n    :close-on-interact-outside=\"false\"\n    unmount-on-exit\n    lazy-mount\n  >\n    <Teleport to=\"body\">\n      <Dialog.Positioner :class=\"cx('fixed inset-y-0 z-(--z-panel) flex', SIDE[props.side])\">\n        <Dialog.Content\n          :class=\"\n            cx(\n              'layer flex h-full w-[min(420px,100vw)] flex-col border-reed-lit data-[state=open]:animate-[slide_var(--dur-overlay)_var(--ease-beat)] data-[state=closed]:animate-[slide-out_var(--dur-local)_var(--ease-exit)]',\n              props.class,\n            )\n          \"\n        >\n          <header class=\"reed-edge flex items-start justify-between gap-4 px-(--cell-x) py-(--stack)\">\n            <div class=\"flex min-w-0 flex-col gap-0.5\">\n              <Dialog.Title class=\"truncate font-medium\">{{ props.title }}</Dialog.Title>\n              <Dialog.Description v-if=\"props.description\" class=\"truncate text-weft-dim\">\n                {{ props.description }}\n              </Dialog.Description>\n            </div>\n            <Dialog.CloseTrigger\n              aria-label=\"Close the panel\"\n              class=\"-mr-1 cursor-pointer p-1 text-weft-faint transition-colors duration-(--dur-instant) ease-(--ease-beat) hover:text-weft\"\n            >\n              <CloseIcon class=\"size-3.5\" />\n            </Dialog.CloseTrigger>\n          </header>\n\n          <div class=\"reed-scroll flex-1 overflow-auto\">\n            <slot />\n          </div>\n\n          <footer\n            v-if=\"$slots.footer\"\n            class=\"flex items-center justify-end gap-(--stack) border-t border-reed px-(--cell-x) py-(--stack)\"\n          >\n            <slot name=\"footer\" />\n          </footer>\n        </Dialog.Content>\n      </Dialog.Positioner>\n    </Teleport>\n  </Dialog.Root>\n</template>\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/vue/0.11.0/panel.json",
        "files": [
          {
            "path": "components/ui/panel/Panel.vue",
            "hash": "sha256-ZsvfxHNwbCNf5Ibh4rYeUJ0jgQqn9w+pc1dV43j7Wso=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "popover",
      "type": "registry:ui",
      "title": "Popover",
      "dependencies": [
        "@ark-ui/vue@^5.38.2"
      ],
      "registryDependencies": [
        "cx",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/popover/Popover.vue",
          "target": "components/ui/popover/Popover.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Popover as ArkPopover } from '@ark-ui/vue/popover'\n</script>\n\n<!--\n  a bare ark root. every option it takes, `positioning` and `v-model:open` among them, arrives as\n  an attribute and vue forwards it, which is also why the file declares no props of its own.\n-->\n<template>\n  <ArkPopover.Root>\n    <slot />\n  </ArkPopover.Root>\n</template>\n"
        },
        {
          "path": "components/ui/popover/PopoverContent.vue",
          "target": "components/ui/popover/PopoverContent.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Popover as ArkPopover } from '@ark-ui/vue/popover'\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n</script>\n\n<!-- the positioner has no rank here: the stylesheet reaches ark's inline value -->\n<template>\n  <Teleport to=\"body\">\n    <ArkPopover.Positioner>\n      <ArkPopover.Content\n        :class=\"\n          cx(\n            'layer min-w-52 py-1 focus:outline-none data-[state=open]:animate-[rise_var(--dur-local)_var(--ease-beat)]',\n            props.class,\n          )\n        \"\n      >\n        <slot />\n      </ArkPopover.Content>\n    </ArkPopover.Positioner>\n  </Teleport>\n</template>\n"
        },
        {
          "path": "components/ui/popover/PopoverDescription.vue",
          "target": "components/ui/popover/PopoverDescription.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Popover as ArkPopover } from '@ark-ui/vue/popover'\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n</script>\n\n<template>\n  <ArkPopover.Description :class=\"cx('px-(--cell-x) py-1 text-weft-dim', props.class)\">\n    <slot />\n  </ArkPopover.Description>\n</template>\n"
        },
        {
          "path": "components/ui/popover/PopoverTitle.vue",
          "target": "components/ui/popover/PopoverTitle.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Popover as ArkPopover } from '@ark-ui/vue/popover'\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n</script>\n\n<template>\n  <ArkPopover.Title :class=\"cx('reed-edge px-(--cell-x) pt-1 pb-(--stack) font-medium', props.class)\">\n    <slot />\n  </ArkPopover.Title>\n</template>\n"
        },
        {
          "path": "components/ui/popover/PopoverTrigger.vue",
          "target": "components/ui/popover/PopoverTrigger.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Popover as ArkPopover } from '@ark-ui/vue/popover'\n</script>\n\n<template>\n  <ArkPopover.Trigger>\n    <slot />\n  </ArkPopover.Trigger>\n</template>\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/vue/0.11.0/popover.json",
        "files": [
          {
            "path": "components/ui/popover/Popover.vue",
            "hash": "sha256-pYFOQCI2izsFywr/PSNkcWyZEYFUVO5BrMwCacnqwJA=",
            "client": false
          },
          {
            "path": "components/ui/popover/PopoverContent.vue",
            "hash": "sha256-LPe9pbm8MfLJBqX7klSNKfP5YpcDjShxYloAFVFb00g=",
            "client": false
          },
          {
            "path": "components/ui/popover/PopoverDescription.vue",
            "hash": "sha256-VrifTxUskNCSXbIAsRQzXQ69Rka8dMrasKuw5T2Mgtw=",
            "client": false
          },
          {
            "path": "components/ui/popover/PopoverTitle.vue",
            "hash": "sha256-sgqCzBRhFKvOGVTwppGruN+exBwVCCsFJUlTEvWMU9w=",
            "client": false
          },
          {
            "path": "components/ui/popover/PopoverTrigger.vue",
            "hash": "sha256-Y4wQcFzAABwcJuFzCs4N5DKp2lWNd5UwpOb68SHm/5o=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "select",
      "type": "registry:ui",
      "title": "Select",
      "dependencies": [
        "@ark-ui/vue@^5.38.2"
      ],
      "registryDependencies": [
        "cx",
        "icons",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/select/Select.vue",
          "target": "components/ui/select/Select.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Select as ArkSelect, type ListCollection } from '@ark-ui/vue/select'\nimport type { SelectOptionItem } from '@/components/ui/select/SelectOption.vue'\n\n/*\n * build the collection with `createListCollection` from @ark-ui/vue/select. every other\n * root option, `multiple` and `close-on-select` included, reaches ark as an attribute.\n */\nconst props = defineProps<{ collection: ListCollection<SelectOptionItem> }>()\n\nconst value = defineModel<string[]>()\n</script>\n\n<template>\n  <ArkSelect.Root v-model=\"value\" :collection=\"props.collection\">\n    <slot />\n  </ArkSelect.Root>\n</template>\n"
        },
        {
          "path": "components/ui/select/SelectContent.vue",
          "target": "components/ui/select/SelectContent.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Select as ArkSelect } from '@ark-ui/vue/select'\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n</script>\n\n<!-- rank comes from the stylesheet; ark sets it inline on the positioner -->\n<template>\n  <Teleport to=\"body\">\n    <ArkSelect.Positioner>\n      <ArkSelect.Content :class=\"cx('layer min-w-42 py-1 focus:outline-none', props.class)\">\n        <slot />\n      </ArkSelect.Content>\n    </ArkSelect.Positioner>\n  </Teleport>\n</template>\n"
        },
        {
          "path": "components/ui/select/SelectOption.vue",
          "target": "components/ui/select/SelectOption.vue",
          "type": "registry:ui",
          "content": "<script lang=\"ts\">\nexport interface SelectOptionItem {\n  readonly label: string\n  readonly value: string\n}\n</script>\n\n<script setup lang=\"ts\">\nimport { Select as ArkSelect } from '@ark-ui/vue/select'\nimport type { HTMLAttributes } from 'vue'\nimport CheckIcon from '@/components/ui/icons/CheckIcon.vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{\n  item: SelectOptionItem\n  class?: HTMLAttributes['class']\n}>()\n</script>\n\n<!-- the shed follows the keyboard, the selvedge marks the choice -->\n<template>\n  <ArkSelect.Item\n    :item=\"props.item\"\n    :class=\"\n      cx(\n        'selvedge flex cursor-pointer items-center justify-between gap-3 px-(--cell-x) py-1 text-weft-dim data-highlighted:bg-shed data-highlighted:text-weft data-[state=checked]:bg-indigo-wash data-[state=checked]:text-weft data-[state=checked]:selvedge-on',\n        props.class,\n      )\n    \"\n  >\n    <ArkSelect.ItemText>{{ props.item.label }}</ArkSelect.ItemText>\n    <ArkSelect.ItemIndicator>\n      <CheckIcon class=\"size-3 text-indigo\" />\n    </ArkSelect.ItemIndicator>\n  </ArkSelect.Item>\n</template>\n"
        },
        {
          "path": "components/ui/select/SelectTrigger.vue",
          "target": "components/ui/select/SelectTrigger.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Select as ArkSelect } from '@ark-ui/vue/select'\nimport type { HTMLAttributes } from 'vue'\nimport ChevronIcon from '@/components/ui/icons/ChevronIcon.vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n</script>\n\n<template>\n  <ArkSelect.Control>\n    <ArkSelect.Trigger :class=\"cx('ctl h-(--ctl-h) cursor-pointer', props.class)\">\n      <slot />\n      <ChevronIcon class=\"size-3 text-weft-faint\" />\n    </ArkSelect.Trigger>\n  </ArkSelect.Control>\n</template>\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/vue/0.11.0/select.json",
        "files": [
          {
            "path": "components/ui/select/Select.vue",
            "hash": "sha256-RKWjjPGQd5fZY9eR7DCCFEzLMn40yFBRIfsfrzvG3dk=",
            "client": false
          },
          {
            "path": "components/ui/select/SelectContent.vue",
            "hash": "sha256-XqFRkuZfS8yfGF9vbpYRad8peLM+zg2QjUQnzWEq8Hw=",
            "client": false
          },
          {
            "path": "components/ui/select/SelectOption.vue",
            "hash": "sha256-bQNurmytLuPd0VUEZ8Nrm17VH21u2Dd91zlv7L298ow=",
            "client": false
          },
          {
            "path": "components/ui/select/SelectTrigger.vue",
            "hash": "sha256-MqsIFvmjAwVjYiKpNewxgaOHdmCVSnpVZSOCu+PCnpM=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "table",
      "type": "registry:ui",
      "title": "Table",
      "dependencies": [],
      "registryDependencies": [
        "checkbox",
        "cx",
        "empty-state",
        "tokens",
        "tooltip"
      ],
      "files": [
        {
          "path": "components/ui/table/ColumnGrip.vue",
          "target": "components/ui/table/ColumnGrip.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { ref } from 'vue'\n\nconst props = defineProps<{ label: string }>()\n\nconst emit = defineEmits<{ resize: [next: number] }>()\n\nconst KEY_STEP = 8\n\nconst grip = ref<HTMLButtonElement | null>(null)\nconst originX = ref(0)\nconst originWidth = ref(0)\nconst dragging = ref(false)\n\n/* the width on screen. a column that never moved holds no px of its own. */\nconst cellWidth = () => grip.value?.parentElement?.offsetWidth ?? 0\n\nconst onPointerDown = (event: PointerEvent) => {\n  grip.value?.setPointerCapture(event.pointerId)\n  originX.value = event.clientX\n  originWidth.value = cellWidth()\n  dragging.value = true\n}\n\nconst onPointerMove = (event: PointerEvent) => {\n  if (!dragging.value) return\n  emit('resize', originWidth.value + event.clientX - originX.value)\n}\n\nconst onKeyDown = (event: KeyboardEvent) => {\n  if (event.key !== 'ArrowLeft' && event.key !== 'ArrowRight') return\n  event.preventDefault()\n  const from = cellWidth()\n  emit('resize', event.key === 'ArrowLeft' ? from - KEY_STEP : from + KEY_STEP)\n}\n</script>\n\n<!-- a button: the keyboard reaches the width this way, and the capture needs no window listener -->\n<template>\n  <button\n    ref=\"grip\"\n    type=\"button\"\n    :aria-label=\"`Resize the ${props.label} column`\"\n    class=\"reed-grip\"\n    :data-dragging=\"dragging ? '' : undefined\"\n    @pointerdown=\"onPointerDown\"\n    @pointermove=\"onPointerMove\"\n    @lostpointercapture=\"dragging = false\"\n    @keydown=\"onKeyDown\"\n  />\n</template>\n"
        },
        {
          "path": "components/ui/table/ColumnHead.vue",
          "target": "components/ui/table/ColumnHead.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\" generic=\"T\">\nimport { computed } from 'vue'\nimport { cx } from '@/lib/cx'\nimport type { Column, SortDirection } from '@/components/ui/table/Table.vue'\n\nconst props = defineProps<{\n  column: Column<T>\n  direction?: SortDirection\n}>()\n\nconst emit = defineEmits<{ sort: [] }>()\n\nconst sortable = computed(() => props.column.sortValue !== undefined)\n\nconst shell = computed(() =>\n  cx(\n    'flex h-full w-full items-center gap-1.5 text-weft-dim',\n    props.column.numeric && 'justify-end',\n    sortable.value && 'cursor-pointer transition-colors duration-(--dur-instant) ease-(--ease-beat) hover:text-weft',\n  ),\n)\n</script>\n\n<template>\n  <component\n    :is=\"sortable ? 'button' : 'div'\"\n    :type=\"sortable ? 'button' : undefined\"\n    :class=\"shell\"\n    @click=\"sortable && emit('sort')\"\n  >\n    <!-- the mark sits inside the baseline group, standing on the baseline of the label -->\n    <span class=\"inline-flex min-w-0 items-baseline gap-1.5\">\n      <span class=\"truncate\">{{ props.column.label }}</span>\n      <span v-if=\"props.column.unit\" class=\"font-data text-weft-faint\">{{ props.column.unit }}</span>\n      <span\n        v-if=\"props.direction\"\n        :class=\"cx('reed-sort text-indigo', props.direction === 'desc' && 'reed-sort-down')\"\n        aria-hidden=\"true\"\n      />\n    </span>\n  </component>\n</template>\n"
        },
        {
          "path": "components/ui/table/Table.vue",
          "target": "components/ui/table/Table.vue",
          "type": "registry:ui",
          "content": "<script lang=\"ts\">\nexport type SortDirection = 'asc' | 'desc'\n\nexport interface Column<T> {\n  readonly key: string\n  readonly label: string\n  readonly unit?: string\n  readonly hint?: string\n  /* the widest plausible value, in characters. the density turns it into a width. */\n  readonly chars: number\n  readonly numeric?: boolean\n  readonly sortValue?: (row: T) => string | number\n}\n\nexport type RowNoun = string | readonly [one: string, many: string]\n</script>\n\n<script setup lang=\"ts\" generic=\"T\">\nimport { computed, getCurrentInstance, ref, shallowRef, watch, watchEffect, type HTMLAttributes } from 'vue'\nimport Checkbox, { type CheckedState } from '@/components/ui/checkbox/Checkbox.vue'\nimport EmptyState from '@/components/ui/empty-state/EmptyState.vue'\nimport ColumnGrip from '@/components/ui/table/ColumnGrip.vue'\nimport ColumnHead from '@/components/ui/table/ColumnHead.vue'\nimport Tooltip from '@/components/ui/tooltip/Tooltip.vue'\nimport { cx } from '@/lib/cx'\n\nconst props = withDefaults(\n  defineProps<{\n    rows: readonly T[]\n    columns: readonly Column<T>[]\n    rowId: (row: T) => string\n    title: string\n    noun?: RowNoun\n    emptyMessage?: string\n    loading?: boolean\n    class?: HTMLAttributes['class']\n  }>(),\n  { noun: () => 'rows', emptyMessage: 'No row matches the filters.', loading: false },\n)\n\nconst emit = defineEmits<{ selectionChange: [selected: ReadonlySet<string>]; rowActivate: [row: T] }>()\n\n/*\n * a declared emit is stripped out of $attrs, so the vnode props are the only place left that\n * says whether the caller listens. the row draws a pointer only when one does.\n */\nconst instance = getCurrentInstance()\nconst activates = computed(() => instance?.vnode.props?.onRowActivate !== undefined)\n\n/* a cell draws from the slot named after its column: the caller keeps its markup in a template */\ndefineSlots<{\n  actions?: () => unknown\n  [cell: `cell-${string}`]: (slotProps: { row: T }) => unknown\n}>()\n\nconst MIN_WIDTH = 56\nconst MAX_WIDTH = 420\n\n/* the 7px mark and its 6px gap: two characters cover both in every density */\nconst SORT_CHARS = 2\n\n/*\n * below this many rows the whole body is rendered. the widest viewport shows about 20\n * rows at the dense height, so this leaves several screens before a window is worth it.\n */\nconst WINDOW_MIN = 100\nconst OVERSCAN = 6\n\n/*\n * the row height arrives from a measurement, which leaves the first commit of a long\n * batch without one. 40 rows covers the 520px body cap at the dense height with the\n * overscan on top.\n */\nconst FIRST_ROWS = 40\n\n/* doubles as the left offset of the pinned column beside it */\nconst GUTTER = 'calc(var(--cell-x) * 2 + var(--ctl-box))'\n\n/*\n * a pinned cell hides what travels under it, so it needs an opaque background and a\n * transition of its own. a parent transition does not animate an inherited value.\n */\nconst PINNED = 'warp-line-end bg-inherit px-(--cell-x) transition-colors duration-(--dur-instant) ease-(--ease-beat)'\n\nconst SKELETON_IDS: readonly string[] = ['a', 'b', 'c', 'd', 'e', 'f', 'g', 'h'].map((id) => `warp-${id}`)\n\nconst NAV_KEYS: ReadonlySet<string> = new Set(['ArrowDown', 'ArrowUp', 'Home', 'End'])\n\n/* a control in a cell owns its own click, and the checkbox sits inside a label */\nconst INTERACTIVE = 'a, button, input, select, textarea, label'\n\nfunction compare(a: string | number, b: string | number) {\n  if (typeof a === 'number' && typeof b === 'number') return a - b\n  return String(a).localeCompare(String(b))\n}\n\n/* a plural noun alone reads \"1 rows\" at a count of one, so a caller can give both forms */\nfunction countNoun(noun: RowNoun, count: number) {\n  if (typeof noun === 'string') return noun\n  return count === 1 ? noun[0] : noun[1]\n}\n\nfunction clampWidth(value: number) {\n  return Math.min(MAX_WIDTH, Math.max(MIN_WIDTH, Math.round(value)))\n}\n\n/*\n * the head counts too, because a label and its unit can run longer than the value\n * under it. the half character covers the rounding a table layout applies.\n *\n * the head is drawn in the interface face and the value under it in the data face,\n * so the head takes whichever of the two advances is wider. `1ch` is the interface\n * face's own digit advance, read off this cell. where the data face is the wider of\n * the pair the expression collapses to what it always was.\n */\nfunction intrinsicWidth(column: Column<T>, sorted: boolean) {\n  const unit = column.unit ? column.unit.length + 1 : 0\n  const head = column.label.length + unit + (sorted ? SORT_CHARS : 0)\n  const value = `${column.chars + 0.5} * var(--data-adv)`\n  const label = `${head + 0.5} * max(var(--data-adv), 1ch)`\n  return `calc(max(${value}, ${label}) + var(--cell-x) * 2)`\n}\n\n/* a css length needs its unit; vue writes a bare number as it stands */\nconst px = (value: string | number) => (typeof value === 'number' ? `${value}px` : value)\n\nconst selected = shallowRef<ReadonlySet<string>>(new Set())\n/* only a dragged column holds a px width; the rest follow the density */\nconst widths = ref<Record<string, number | undefined>>({})\nconst sort = shallowRef<{ key: string; direction: SortDirection } | null>(null)\nconst cursor = ref<string | null>(null)\n\n/* raised by a key press, so the focus chase below ignores a scroll the pointer made */\nlet chasing = false\n\nconst span = computed(() => props.columns.length + 2)\n\nconst resizeColumn = (key: string, next: number) => {\n  widths.value = { ...widths.value, [key]: clampWidth(next) }\n}\n\n/* third click restores the order the rows arrived in */\nconst toggleSort = (key: string) => {\n  const current = sort.value\n  if (current?.key !== key) {\n    sort.value = { key, direction: 'asc' }\n    return\n  }\n  sort.value = current.direction === 'asc' ? { key, direction: 'desc' } : null\n}\n\nconst ordered = computed(() => {\n  const chosen = sort.value\n  const column = props.columns.find((entry) => entry.key === chosen?.key)\n  const value = column?.sortValue\n  if (chosen === null || value === undefined) return props.rows\n  const factor = chosen.direction === 'asc' ? 1 : -1\n  return [...props.rows].sort((a, b) => factor * compare(value(a), value(b)))\n})\n\nconst directionOf = (column: Column<T>) => (sort.value?.key === column.key ? sort.value.direction : undefined)\n\nconst ariaSort = (column: Column<T>) => {\n  if (column.sortValue === undefined) return undefined\n  const direction = directionOf(column)\n  if (direction === undefined) return 'none'\n  return direction === 'asc' ? 'ascending' : 'descending'\n}\n\n/*\n * a filter can take a selected row off the screen. the count and the head control\n * read only what is on screen, and the set keeps the rest for when it comes back.\n */\nconst onScreen = computed(() => new Set(props.rows.map(props.rowId)))\nconst active = computed(() => new Set([...selected.value].filter((id) => onScreen.value.has(id))))\n\nconst headerState = computed<CheckedState>(() => {\n  if (active.value.size === 0) return false\n  if (active.value.size === props.rows.length) return true\n  return 'indeterminate'\n})\n\n/*\n * every row is exactly --row-h, so the window is arithmetic and needs no per row\n * measurement. the height is read off a real row rather than the token, which keeps\n * it right when the density changes under the component.\n */\nconst scroller = ref<HTMLDivElement | null>(null)\nconst headRow = ref<HTMLTableRowElement | null>(null)\nconst metrics = ref({ rowHeight: 0, viewport: 0 })\nconst scrollTop = ref(0)\n\nconst long = computed(() => ordered.value.length > WINDOW_MIN)\n\n/*\n * the head row carries the same --row-h and keeps its identity for the life of the\n * table, so it is the one element that can be observed. a body row is re-keyed on\n * every scroll, which leaves the observer watching a detached node.\n */\nwatchEffect((onCleanup) => {\n  const box = scroller.value\n  const head = headRow.value\n  if (!box || !head || !long.value) return\n\n  const read = () => {\n    metrics.value = { rowHeight: head.getBoundingClientRect().height, viewport: box.clientHeight }\n  }\n  read()\n\n  const observer = new ResizeObserver(read)\n  observer.observe(box)\n  observer.observe(head)\n  onCleanup(() => observer.disconnect())\n})\n\nconst view = computed(() => {\n  const total = ordered.value.length\n  if (!long.value) return { start: 0, end: total, before: 0, after: 0 }\n  /* the spacer height is unknown until the row height arrives, and a short body corrects itself */\n  if (metrics.value.rowHeight === 0) return { start: 0, end: Math.min(total, FIRST_ROWS), before: 0, after: 0 }\n\n  const visible = Math.ceil(metrics.value.viewport / metrics.value.rowHeight)\n  const start = Math.max(0, Math.floor(scrollTop.value / metrics.value.rowHeight) - OVERSCAN)\n  const end = Math.min(total, start + visible + OVERSCAN * 2)\n  return {\n    start,\n    end,\n    before: start * metrics.value.rowHeight,\n    after: (total - end) * metrics.value.rowHeight,\n  }\n})\n\nconst windowed = computed(() =>\n  ordered.value.slice(view.value.start, view.value.end).map((row, offset) => ({\n    row,\n    id: props.rowId(row),\n    position: view.value.start + offset,\n  })),\n)\n\nconst onScroll = () => {\n  if (long.value) scrollTop.value = scroller.value?.scrollTop ?? 0\n}\n\nconst cursorIndex = computed(() => {\n  if (cursor.value === null) return -1\n  return ordered.value.findIndex((row) => props.rowId(row) === cursor.value)\n})\n\n/*\n * the row the cursor is on holds the only tab stop in the body, so leaving the table\n * and coming back returns to it. a pointer scroll can carry that row out of the\n * window, and the first rendered row takes the stop until an arrow moves it again.\n */\nconst stop = computed(() =>\n  cursorIndex.value >= view.value.start && cursorIndex.value < view.value.end ? cursorIndex.value : view.value.start,\n)\n\nconst moveCursor = (index: number) => {\n  const next = ordered.value[Math.min(Math.max(index, 0), ordered.value.length - 1)]\n  if (next === undefined) return\n  chasing = true\n  cursor.value = props.rowId(next)\n}\n\nconst navigate = (from: number, key: string) => {\n  if (key === 'ArrowDown') moveCursor(from + 1)\n  if (key === 'ArrowUp') moveCursor(from - 1)\n  if (key === 'Home') moveCursor(0)\n  if (key === 'End') moveCursor(ordered.value.length - 1)\n}\n\n/*\n * a row outside the window does not exist to focus, so the scroll goes first and the\n * watcher runs again on the view it produces. `chasing` keeps it off a pointer scroll,\n * which must not pull focus.\n */\nwatch(\n  [cursorIndex, view, metrics],\n  () => {\n    if (!chasing || cursorIndex.value === -1) return\n\n    const box = scroller.value\n    const row = box?.querySelector<HTMLTableRowElement>(`tbody tr[aria-rowindex='${cursorIndex.value + 2}']`)\n    if (row) {\n      chasing = false\n      row.focus({ preventScroll: true })\n      row.scrollIntoView({ block: 'nearest' })\n      return\n    }\n\n    if (box && metrics.value.rowHeight > 0) {\n      const top = cursorIndex.value * metrics.value.rowHeight\n      const above = top < box.scrollTop\n      box.scrollTop = above ? top : top - metrics.value.viewport + metrics.value.rowHeight\n    }\n  },\n  { flush: 'post' },\n)\n\nconst applySelection = (next: ReadonlySet<string>) => {\n  selected.value = next\n  emit('selectionChange', new Set([...next].filter((id) => onScreen.value.has(id))))\n}\n\nconst toggleRow = (id: string) => {\n  const next = new Set(selected.value)\n  if (!next.delete(id)) next.add(id)\n  applySelection(next)\n}\n\nconst toggleAll = () => {\n  const next = new Set(selected.value)\n  if (active.value.size === props.rows.length) {\n    onScreen.value.forEach((id) => next.delete(id))\n  } else {\n    onScreen.value.forEach((id) => next.add(id))\n  }\n  applySelection(next)\n}\n\nconst onRowClick = (event: MouseEvent, row: T) => {\n  if (!activates.value) return\n  if (event.target instanceof Element && event.target.closest(INTERACTIVE) !== null) return\n  emit('rowActivate', row)\n}\n\nconst onRowKeyDown = (event: KeyboardEvent, id: string, position: number, row: T) => {\n  /* a key pressed on the checkbox inside the row belongs to the checkbox */\n  if (event.target !== event.currentTarget) return\n\n  if (event.key === ' ') {\n    event.preventDefault()\n    toggleRow(id)\n    return\n  }\n\n  if (event.key === 'Enter' && activates.value) {\n    event.preventDefault()\n    emit('rowActivate', row)\n    return\n  }\n\n  if (!NAV_KEYS.has(event.key)) return\n  event.preventDefault()\n  navigate(position, event.key)\n}\n</script>\n\n<template>\n  <section :class=\"cx('@container isolate border border-reed bg-raised', props.class)\">\n    <header class=\"flex items-center justify-between gap-(--stack) border-b border-reed px-(--cell-x) py-(--stack)\">\n      <h2 class=\"font-medium\">{{ props.title }}</h2>\n      <div class=\"flex items-center gap-(--stack)\">\n        <p v-if=\"!props.loading\" class=\"tnum text-weft-dim\">\n          {{\n            active.size > 0\n              ? `${active.size} of ${props.rows.length} selected`\n              : `${props.rows.length} ${countNoun(props.noun, props.rows.length)}`\n          }}\n        </p>\n        <slot name=\"actions\" />\n      </div>\n    </header>\n\n    <div ref=\"scroller\" class=\"reed-scroll max-h-[var(--table-body,32.5rem)] overflow-auto\" @scroll=\"onScroll\">\n      <!-- separate borders: a pinned cell paints its background over a collapsed one -->\n      <table\n        :aria-busy=\"props.loading\"\n        :aria-rowcount=\"ordered.length + 1\"\n        class=\"w-full table-fixed border-separate border-spacing-0 text-left\"\n      >\n        <thead class=\"sticky top-0 z-(--z-sticky)\">\n          <tr ref=\"headRow\" :aria-rowindex=\"1\" class=\"bg-raised select-none\">\n            <th\n              scope=\"col\"\n              class=\"reed-edge warp-line-end sticky left-0 z-(--z-pinned) bg-raised px-(--cell-x)\"\n              :style=\"{ height: 'var(--row-h)', width: GUTTER }\"\n            >\n              <Checkbox\n                :checked=\"headerState\"\n                :label=\"`Select all ${countNoun(props.noun, 2)}`\"\n                @update:checked=\"toggleAll\"\n              />\n            </th>\n            <th\n              v-for=\"(column, index) in props.columns\"\n              :key=\"column.key\"\n              scope=\"col\"\n              :aria-sort=\"ariaSort(column)\"\n              :class=\"\n                cx('reed-edge px-(--cell-x) font-medium', index === 0 && 'warp-line-end sticky z-(--z-pinned) bg-raised')\n              \"\n              :style=\"{\n                height: 'var(--row-h)',\n                left: index === 0 ? GUTTER : undefined,\n                width: px(widths[column.key] ?? intrinsicWidth(column, sort?.key === column.key)),\n              }\"\n            >\n              <Tooltip v-if=\"column.hint\" :content=\"column.hint\">\n                <ColumnHead :column=\"column\" :direction=\"directionOf(column)\" @sort=\"toggleSort(column.key)\" />\n              </Tooltip>\n              <ColumnHead v-else :column=\"column\" :direction=\"directionOf(column)\" @sort=\"toggleSort(column.key)\" />\n              <ColumnGrip :label=\"column.label\" @resize=\"(next: number) => resizeColumn(column.key, next)\" />\n            </th>\n            <th scope=\"col\" class=\"reed-edge\">\n              <span class=\"sr-only\">Spare width</span>\n            </th>\n          </tr>\n        </thead>\n\n        <tbody>\n          <template v-if=\"props.loading\">\n            <tr v-for=\"(id, index) in SKELETON_IDS\" :key=\"id\">\n              <td\n                :colspan=\"span\"\n                class=\"reed-warp reed-warp-beat\"\n                :style=\"{ height: 'var(--row-h)', animationDelay: `calc(var(--dur-instant) * ${index})` }\"\n              />\n            </tr>\n          </template>\n\n          <tr v-else-if=\"ordered.length === 0\">\n            <td :colspan=\"span\" class=\"border-t border-reed/60 p-0\">\n              <EmptyState :title=\"props.emptyMessage\" />\n            </td>\n          </tr>\n\n          <template v-else>\n            <!-- the rows outside the window still take their height, so the scrollbar tells the truth -->\n            <tr v-if=\"view.before > 0\" aria-hidden=\"true\">\n              <td :colspan=\"span\" :style=\"{ height: px(view.before), padding: 0, border: 0 }\" />\n            </tr>\n\n            <tr\n              v-for=\"entry in windowed\"\n              :key=\"entry.id\"\n              :aria-rowindex=\"entry.position + 2\"\n              :tabindex=\"entry.position === stop ? 0 : -1\"\n              :class=\"\n                cx(\n                  'focus-row bg-raised transition-colors duration-(--dur-instant) ease-(--ease-beat) hover:bg-shed data-selected:bg-indigo-wash data-selected:hover:bg-indigo-wash',\n                  activates && 'cursor-pointer',\n                )\n              \"\n              :data-selected=\"selected.has(entry.id) ? '' : undefined\"\n              @keydown=\"onRowKeyDown($event, entry.id, entry.position, entry.row)\"\n              @click=\"onRowClick($event, entry.row)\"\n            >\n              <td\n                :class=\"\n                  cx(\n                    'selvedge sticky left-0 z-(--z-pinned) border-t border-reed/60',\n                    PINNED,\n                    selected.has(entry.id) && 'selvedge-on',\n                  )\n                \"\n                :style=\"{ height: 'var(--row-h)' }\"\n              >\n                <Checkbox\n                  :checked=\"selected.has(entry.id)\"\n                  :label=\"`Select row ${entry.id}`\"\n                  @update:checked=\"toggleRow(entry.id)\"\n                />\n              </td>\n              <td\n                v-for=\"(column, index) in props.columns\"\n                :key=\"column.key\"\n                :class=\"\n                  cx(\n                    'truncate border-t border-reed/60 px-(--cell-x) text-weft-dim',\n                    index === 0 && `sticky z-(--z-pinned) ${PINNED}`,\n                    index > 1 && 'warp-line',\n                    column.numeric && 'tnum text-right font-data text-weft',\n                  )\n                \"\n                :style=\"{ height: 'var(--row-h)', left: index === 0 ? GUTTER : undefined }\"\n              >\n                <slot :name=\"`cell-${column.key}`\" :row=\"entry.row\" />\n              </td>\n              <td class=\"warp-line border-t border-reed/60\" />\n            </tr>\n\n            <tr v-if=\"view.after > 0\" aria-hidden=\"true\">\n              <td :colspan=\"span\" :style=\"{ height: px(view.after), padding: 0, border: 0 }\" />\n            </tr>\n          </template>\n        </tbody>\n      </table>\n    </div>\n  </section>\n</template>\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/vue/0.11.0/table.json",
        "files": [
          {
            "path": "components/ui/table/ColumnGrip.vue",
            "hash": "sha256-NHesQQ1sM3PYYQ/W1EJkjJzMwlkhRwIKx5EOv4oWlAk=",
            "client": false
          },
          {
            "path": "components/ui/table/ColumnHead.vue",
            "hash": "sha256-R/p/VBjBG1mfVBUSAeruZoTFGy/j/NZK5JCGH8x918g=",
            "client": false
          },
          {
            "path": "components/ui/table/Table.vue",
            "hash": "sha256-h/jpr5ssWo+PGvOvhWCs89myuuSZ6t2ZT3GvZl3H7j0=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "tabs",
      "type": "registry:ui",
      "title": "Tabs",
      "dependencies": [
        "@ark-ui/vue@^5.38.2"
      ],
      "registryDependencies": [
        "cx",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/tabs/Tabs.vue",
          "target": "components/ui/tabs/Tabs.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Tabs as ArkTabs } from '@ark-ui/vue/tabs'\n</script>\n\n<template>\n  <ArkTabs.Root>\n    <slot />\n  </ArkTabs.Root>\n</template>\n"
        },
        {
          "path": "components/ui/tabs/TabsList.vue",
          "target": "components/ui/tabs/TabsList.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Tabs as ArkTabs } from '@ark-ui/vue/tabs'\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n</script>\n\n<!-- ark moves the mark between tabs, and the list is the frame it is placed against -->\n<template>\n  <ArkTabs.List :class=\"cx('relative flex items-center border-b border-reed', props.class)\">\n    <slot />\n    <ArkTabs.Indicator class=\"selvedge selvedge-bottom selvedge-on tab-mark bottom-0 h-0.5 w-(--width)\" />\n  </ArkTabs.List>\n</template>\n"
        },
        {
          "path": "components/ui/tabs/TabsPanel.vue",
          "target": "components/ui/tabs/TabsPanel.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Tabs as ArkTabs } from '@ark-ui/vue/tabs'\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{\n  value: string\n  class?: HTMLAttributes['class']\n}>()\n</script>\n\n<template>\n  <ArkTabs.Content :value=\"props.value\" :class=\"cx('py-(--stack) focus:outline-none', props.class)\">\n    <slot />\n  </ArkTabs.Content>\n</template>\n"
        },
        {
          "path": "components/ui/tabs/TabsTab.vue",
          "target": "components/ui/tabs/TabsTab.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Tabs as ArkTabs } from '@ark-ui/vue/tabs'\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{\n  value: string\n  disabled?: boolean\n  class?: HTMLAttributes['class']\n}>()\n</script>\n\n<template>\n  <ArkTabs.Trigger\n    :value=\"props.value\"\n    :disabled=\"props.disabled\"\n    :class=\"\n      cx(\n        'h-(--ctl-h) cursor-pointer px-(--cell-x) text-weft-dim transition-colors duration-(--dur-instant) ease-(--ease-beat) hover:bg-shed hover:text-weft data-selected:text-weft disabled:cursor-not-allowed disabled:opacity-50',\n        props.class,\n      )\n    \"\n  >\n    <slot />\n  </ArkTabs.Trigger>\n</template>\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/vue/0.11.0/tabs.json",
        "files": [
          {
            "path": "components/ui/tabs/Tabs.vue",
            "hash": "sha256-jhvx0xRwNdkckWm82CjQJmoIfD6eSz5o2+70vrlUCqI=",
            "client": false
          },
          {
            "path": "components/ui/tabs/TabsList.vue",
            "hash": "sha256-BCZ1H9bAmTzgS4rw4pFnFcGxOqTRI1d92QXFVioyxcM=",
            "client": false
          },
          {
            "path": "components/ui/tabs/TabsPanel.vue",
            "hash": "sha256-CNTjReNQHDZZjdqPatuFTF91k7/fQyugMDhBfhuxi1g=",
            "client": false
          },
          {
            "path": "components/ui/tabs/TabsTab.vue",
            "hash": "sha256-SENGgnEPISt/vX4QHm84LCtCsfLVhin/K8OX4rydqwk=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "toast",
      "type": "registry:ui",
      "title": "Toast",
      "dependencies": [
        "@ark-ui/vue@^5.38.2"
      ],
      "registryDependencies": [
        "cx",
        "icons",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/toast/Toaster.vue",
          "target": "components/ui/toast/Toaster.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Toast as ArkToast, Toaster as ArkToaster } from '@ark-ui/vue/toast'\nimport type { HTMLAttributes } from 'vue'\nimport CloseIcon from '@/components/ui/icons/CloseIcon.vue'\nimport { cx } from '@/lib/cx'\n\nconst props = defineProps<{ class?: HTMLAttributes['class'] }>()\n\n/* zag names these five types */\nconst TONE: Record<string, string> = {\n  success: 'text-jade',\n  error: 'text-madder',\n  warning: 'text-weld',\n  info: 'text-indigo',\n  loading: 'text-weft-dim',\n}\n</script>\n\n<!--\n  the `toaster` store reaches ark as an attribute. build it with `createToaster` from\n  @ark-ui/vue/toast. zag gives the group the highest rank in the document, so no token\n  ranks it here.\n-->\n<template>\n  <ArkToaster>\n    <template #default=\"toast\">\n      <ArkToast.Root\n        :class=\"cx('toast layer flex w-[min(22rem,90vw)] items-start gap-2 p-(--cell-x)', props.class)\"\n      >\n        <div class=\"flex min-w-0 flex-1 flex-col gap-0.5\">\n          <div :class=\"cx('flex items-center gap-1.5', TONE[toast.type ?? 'info'])\">\n            <span :class=\"cx('size-1.25 shrink-0 rounded-full bg-current', toast.type === 'loading' && 'beat')\" />\n            <ArkToast.Title class=\"truncate text-weft\">{{ toast.title }}</ArkToast.Title>\n          </div>\n          <ArkToast.Description v-if=\"toast.description\" class=\"pl-2.75 text-weft-dim\">\n            {{ toast.description }}\n          </ArkToast.Description>\n        </div>\n        <ArkToast.CloseTrigger\n          aria-label=\"Dismiss\"\n          class=\"cursor-pointer p-0.5 text-weft-faint transition-colors duration-(--dur-instant) ease-(--ease-beat) hover:text-weft\"\n        >\n          <CloseIcon class=\"size-3\" />\n        </ArkToast.CloseTrigger>\n      </ArkToast.Root>\n    </template>\n  </ArkToaster>\n</template>\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/vue/0.11.0/toast.json",
        "files": [
          {
            "path": "components/ui/toast/Toaster.vue",
            "hash": "sha256-zEMeXB9idOqtA5h6uahCA+shLKD3/wTFBA+RdI278cU=",
            "client": false
          }
        ]
      }
    },
    {
      "$schema": "https://ui.shadcn.com/schema/registry-item.json",
      "name": "tooltip",
      "type": "registry:ui",
      "title": "Tooltip",
      "dependencies": [
        "@ark-ui/vue@^5.38.2"
      ],
      "registryDependencies": [
        "cx",
        "tokens"
      ],
      "files": [
        {
          "path": "components/ui/tooltip/Tooltip.vue",
          "target": "components/ui/tooltip/Tooltip.vue",
          "type": "registry:ui",
          "content": "<script setup lang=\"ts\">\nimport { Tooltip as ArkTooltip } from '@ark-ui/vue/tooltip'\nimport type { HTMLAttributes } from 'vue'\nimport { cx } from '@/lib/cx'\n\n/* faster than the ark default, for an interface with many small questions */\nconst props = withDefaults(\n  defineProps<{\n    content?: string\n    openDelay?: number\n    closeDelay?: number\n    class?: HTMLAttributes['class']\n  }>(),\n  { openDelay: 200, closeDelay: 80 },\n)\n</script>\n\n<template>\n  <ArkTooltip.Root :open-delay=\"props.openDelay\" :close-delay=\"props.closeDelay\">\n    <!-- the trigger becomes whatever the caller put in the slot -->\n    <ArkTooltip.Trigger as-child>\n      <slot />\n    </ArkTooltip.Trigger>\n    <Teleport to=\"body\">\n      <ArkTooltip.Positioner>\n        <ArkTooltip.Content\n          :class=\"\n            cx(\n              'layer max-w-64 px-2 py-1 text-weft data-[state=open]:animate-[fade_var(--dur-instant)_var(--ease-beat)]',\n              props.class,\n            )\n          \"\n        >\n          <slot name=\"content\">{{ props.content }}</slot>\n        </ArkTooltip.Content>\n      </ArkTooltip.Positioner>\n    </Teleport>\n  </ArkTooltip.Root>\n</template>\n"
        }
      ],
      "sley": {
        "version": "0.11.0",
        "url": "https://sley-ui.dev/r/vue/0.11.0/tooltip.json",
        "files": [
          {
            "path": "components/ui/tooltip/Tooltip.vue",
            "hash": "sha256-fvIkzGQESUAcWZkXZAJKWfNe9dKd1qkyokjee2Xl4nc=",
            "client": false
          }
        ]
      }
    }
  ]
}
